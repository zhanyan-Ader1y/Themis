core_schema: themis-core
core_version: 0.4.0
compatibility:
  workspace:
    supported: [themis-workspace]
    writable: themis-workspace
  artifact:
    supported: [themis-artifact]
    writable: themis-artifact
