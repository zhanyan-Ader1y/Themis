# Claude 指南

首先阅读 `AGENTS.md`，了解仓库约定与核验要求。

跨模块的 Themis 治理、架构与生命周期设计以 `docs/superpowers/specs/` 下已批准的规范为起点。**当前主线**是根目录 `.themis/spec/` 重新设计，其已批准行为契约为 `docs/superpowers/specs/2026-08-07-themis-spec-flow-mvp.md`，控制面文件在 `.themis/spec/`。`templates/.themis/` 下的 Plan 35 设计属于旧 core，仅在处理该目录时才是依据。活动实施顺序见 `docs/plan/README.md`。模块的详细合同与当前实现状态见对应的 `templates/.themis/**/README.md`。
