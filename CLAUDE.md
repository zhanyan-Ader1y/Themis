# Claude 指南

首先阅读 `AGENTS.md`，了解仓库约定与核验要求。

跨模块的 Themis 治理、架构与生命周期设计以 `docs/superpowers/specs/` 下已批准的规范为起点，尤其是 current Plan 35 设计。活动实施顺序见 `docs/plan/README.md`。模块的详细合同与当前实现状态见对应的 `templates/.themis/**/README.md`。
