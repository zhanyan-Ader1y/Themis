# Claude Guide

Read `AGENTS.md` first for repository conventions and verification requirements.

For cross-module Themis governance, architecture, and lifecycle design, start with the approved specifications under
`docs/superpowers/specs/`, especially the current Plan 35 design. For active implementation sequencing, read
`docs/plan/README.md`. For a module's detailed contract and current implementation status, read the corresponding
`templates/.themis/**/README.md`.
