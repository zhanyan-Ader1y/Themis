# task/basic.md — 2026-08-28-pipe-escape-handling / step1

> 本文件是详细设计 + 任务节点的实例工件。basic/detail 判定见 `rules.md` §4，结构决策归属见 §6。以上只指向位置，本文件不复述其文字。

## 任务

**本段为空。**

## 判定依据

§4 要求 basic 同时满足两条，**判定依据必须写入本文件**。四条任务逐条判定：

| 任务 | 被本 step 其他任务依赖？ | 单独不产生外部可观测行为？ | 分类 |
| --- | --- | --- | --- |
| T-D1 引号感知的元字符判定 | 否 | **否**——`SPEC-PESC-003` 直接断言其测试通过 | detail |
| T-D2 传输可靠性判定与拒绝 | 否 | **否**——`SPEC-PESC-001`、`004` 直接断言 | detail |
| T-D3 表格行还原 `\|` | 否 | **否**——`SPEC-PESC-005` 直接断言 | detail |
| T-D4 执行失败时清理子进程 | 否 | **否**——`SPEC-PESC-005` 的挂起复核直接验它 | detail |

**四条无一同时满足两条，全部归 detail。**

## 本节点考虑过、最终未拆出的一项基础任务，如实记明

**`design.md` 决策四要求"复用既有的引号扫描，不新写一套"**，落地时会得到一个"判断某位置是否在引号内"的辅助函数。

**该辅助函数按 §4 的字面确实同时满足两条**：被 T-D1 依赖、单独不产生可观测行为。**但本节点不把它拆成独立的 basic 任务**，理由：

- **它只有一个消费者**（T-D1）。拆出来会得到一个"一对一"的 basic 段，**多出 impl/basic 与 verify/basic 两个节点，换不来任何独立可评审的东西**。
- **任务的粒度标准是"能被独立否决"**：评审者无法在否决该辅助函数的同时批准 T-D1——两者是同一个判断。

**故折进 T-D1**。**本判断交 R3 复核**——若 R3 判它应当拆出，本段改为非空，T-D1 相应缩小。

**空段的后果**：不产生落地调用，不改变流程走向。verify/basic 节点据此取 `不适用`（`rules.md` §7 末段）。
