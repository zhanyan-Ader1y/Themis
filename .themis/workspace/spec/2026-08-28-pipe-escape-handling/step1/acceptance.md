# acceptance.md — 2026-08-28-pipe-escape-handling / step1

> 本文件是人工验收节点的实例工件。本节点的前置闸门与产出见 `flow.md`「人工验收」节；孤儿阻断见 `rules.md` §8，结论来源见 §11，追溯链环 4 在本节点检出见 §12。以上只指向位置，本文件不复述其文字。

## 交付视图

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**三类缺陷全部落地**：静默错值改为显式拒绝并指明参数、字符与出路；`\>` 不再被误判为重定向且引号外的元字符照拒；表格行的 `\|` 还原为真管道，**前后对照实跑——旧版被 `timeout 25` 杀掉（`exit status 143`），新版退出码 `0`**。

**六条判据全满足**，`go test ./...` 无失败，**既有工件一字未改**。

**主动呈交的三处**：

1. **规则误拒 `5` 条真实断言**。独立验证称"它们本来传输正常"——**主会话实测推翻该推论**：同类模式（转义星号单独出现）得 `209` 而 bash 为 `108`，反斜杠被剥掉后变成匹配所有行。**那 5 条是碰巧没坏。** 规则不改，代价是改为手工实跑。
2. **"T-D4 修好了挂起"不成立**——挂起实际由 T-D3（表格还原）修好；T-D4 防的是另一条真实但当前触发不到的路径，未删除但不算其功劳。
3. **取舍一的风险已发生**：表格还原后 `3` 条既有断言跑出的值与记录不符。**按已定处置：如实报出，不改工件。**

**执行者依授权自裁的一项**：验证后改强了挂起 fixture，判**不必重新派发**——六条判据复跑值与验证角色所得完全一致，且改动是把判据改强非改弱。**呈现时明写"如不同意，说一声即重派"。**

## 阻断核查

**依 §8，本节点引用 `verify/detail.md` 的结论据以阻断，不重新做代码层判定。**

**孤儿阻断：不触发。** `task/basic.md`「任务」小节为空（`verify/basic.md` 已做空段代码层断言，条目数实跑 `0`），**不存在 basic 改动，因而不存在无消费者的 basic 改动**。

**追溯链环 4（§12，本节点检出）**：本文件引用了 `verify/detail.md` 的结论（见上节与「结论」）。**环 4 只检出"是否引用"**，按 §12 不取集合或计数形态。**引用成立。**

**上游三环**：由验证角色实跑，差集为 `0`，本节点引用不复跑。

**前置闸门核查**：`verify/detail.md` 结论为 `passed`。**该结论有一处已披露的 stale**——见「结论」。

## 用户原话

**所有者答复：「接受」。**

**依 §11 记录对应关系**：该答复对应本节点投影末句"**接受交付吗？**"，取"接受"。

**该答复的覆盖范围**：投影把三处主动呈交项与一项自裁全部写在同一屏、且自裁项明写"如不同意说一声即重派"，**所有者在该披露在场的情况下接受**。

## 结论

**accepted。**

**本结论确定覆盖的**：三类缺陷的落地、六条判据的满足、以及投影中逐条呈交的三处发现与一项自裁。

**一处推断，如实标明**：**所有者未就"是否重新派发验证"单独表态。** 投影已把它写成一项自裁并给出反对通道（"如不同意，说一声我重派"），**接受而未提出异议，读作认可该自裁**。**强度：高**——它不是并列的另一个问题，而是投影中明确标出的、带反对通道的自裁项。

**该 stale 的实质，复述以备复核**：`passed` 写出后实现者改强了 `table-escape.md` 的一条断言（从 `grep -c` 改为 `grep -r` 形态），**该改动是验证角色自己报出的问题的处置**。六条判据由实现者复跑，值与验证角色完全一致；**该复跑出自实现者本人，正是身份独立要求所不采信的那一类**——故记为推断。

### 本次验收未覆盖

1. **规则判错时仍会静默给错值**——`QA.md` 第 2 轮已记为知情接受的代价。**本次新增一条实测边界**：转义星号是规则正确覆盖的（`\*\*` 确实会坏），但同形态的另外 `5` 条被过拒。
2. **`48` 条含致坏形态的既有断言不回头复核**——R2 已列为未覆盖项。
3. **`3` 条跑出值与记录不符的既有断言不改**——`design.md` 取舍一已定处置；其中 `trace-number-scan:64` 的成因是 `template.md` 被另一需求改写，与本 step 无关。
4. **`killStarted` 的触发路径当前无测试覆盖**——它防的泄漏真实存在，但在白名单齐全时打不到。
5. **致坏条件规则未穷举证明**——`16` 点取样固定，R2 已知情批准。
