# design-review.md — 2026-08-28-pipe-escape-handling / step1

> 本文件是 R2 抽象设计评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R2 抽象设计评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。依 §3 只呈本层增量。）

**本层最要紧的增量**：R1 批准时唯一给不出答案的那处不确定——致坏条件能否证出——**现在有答案了**。

**规则**：命令参数里同时出现反斜杠与 `*` `?` `[` `]` `{` `}` `(` `)` 之一时坏，否则正常。**`16` 个模式无一例外**，第二轮是专门挑"规则说会坏但看着无害"的模式做的证伪轮，四条预测全中。**(b) 因此可行，不必回头重取。**

**呈交裁定的两项**：

1. **第三类缺陷由执行者自行判定纳入本 step，请复核。** R1 批准时只知两类；定界时复现出第三类——断言写在表格单元格内、管道按表格规则转义时，工具先报执行失败再挂住，`timeout 25` 触发退出码 `124`。
2. **一条判据给人的保障感强于其实际强度。** `SPEC-PESC-004` 把 `16` 个数据点写成测试要求，**固定的是"这 16 点不许回归"，不是"规则已证明正确"**。穷举证明做不到；不设它则 (b) 完全无防线。

**其余要点**：精确拒绝比保守拒绝少拒 `27` 条断言（占含反斜杠断言的 `36%`），这是 (b) 相对 (a) 的全部收益。另测出**现有 `48` 条断言（`11.6%`）今天用该工具跑就会得错值**。

**未覆盖**：规则判错时照旧静默给错值——所有者上一轮已知情接受。

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本轮投影把两项裁定写在「需要你裁定」之下、以「批准吗？」收尾，**即两项是批准的前置而非并列的独立问题**。「批准」一条完整覆盖：

- **对第 1 项**构成对"第三类缺陷纳入本 step"这一自行判定的**复核通过**；
- **对第 2 项**构成对"该判据强度有限"的**知情确认**。

**这不是执行者的推断**——两项在呈现时即以"批准的前置"框架提出，所有者在该框架下作答。

## 结论

**approved。** 覆盖 `specify.md` 全部六条。

**本次批准同时确认的两项裁定**：

1. **第三类缺陷（表格转义 → 执行失败 + 挂起）纳入本 step**，不越出 R1 范围——依据是 R1 已批准的收敛主张覆盖"工具没能正确执行作者写的命令"这一层。
2. **`SPEC-PESC-004` 的 `≥16` 是取样固定，不是穷举证明**，所有者知情后仍予批准。

**本次批准未覆盖**：

1. **规则判错时仍会静默给错值**——`QA.md` 第 2 轮已记为知情接受的代价。
2. **既有 `48` 条会得错值的断言是否要回头复核**——本 step 只修工具，**不追溯核查既有工件**（R1 两轮均确认不改既有工件）。**该 `48` 条在工具修好后是否逐条重跑，属本 step 之外。**
3. **`SPEC-PESC-005` 的挂起复核取不到判据记法**——退出码不是 stdout，断言形态取不到它，须由验证节点单独实跑 `timeout` 并记录。
