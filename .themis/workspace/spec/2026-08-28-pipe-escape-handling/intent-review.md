# intent-review.md — 2026-08-28-pipe-escape-handling

> 本文件是 R1 意图评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R1 意图评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：修 `themis verify` 把 `\|` 一律当字面处理的缺陷。

**待裁定项**：批准需求与分解。**追问零提问**。

**两个侧面危险程度不同**：表格转义有明显症状（跑不通、极端时挂起）；正则的"或"**无症状——照常返回一个数，只是数错了**，判据若恰好写成 `→ 0` 会静默通过。

**改变原定方向的发现**：执行者在测量该缺陷时**自己又犯了同一个错**——对话中说过的 `432`、`3394` 两次都错，错因是搜索模式里写了 `\|` 而 `grep` 当成正则的或。重测得 `226`／`42`／`27`。

**未覆盖**：不改任何既有工件；修好后须回头重跑比对。

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影只有一个待裁定项（批准需求与 step 分解），「批准」一条即完整覆盖。**追问零提问，故无追问阶段的裁定需要在此对应。**

## 结论

**approved。**

确认需求本身，以及 step 分解为一个 step。

**本次批准同时确认的两项**：

1. **不改任何既有工件**——`42` 处断言保持原样。**是工具去适应工件，不是反过来**；改工件等于把工具的缺陷转嫁给证据。
2. **修好后须回头用新工具重跑既有工件并与手工实跑比对**——否则无从证明修对了。

**本次批准未覆盖**：三种语境之外若还有第四种（本节点未发现，但未穷举证明不存在）。

## 第 2 轮 —— `Intent.md` 改写后重走 R1（2026-08-31）

**为什么重走**：第 1 轮批准所依据的 `Intent.md` 第 1 版，其核心诊断被自身的 step 定界前复核推翻。`Intent.md` 改写为第 2 版，**依 `flow.md` R1 节失效级联，第 1 轮结论 stale**，须重走本节点。

**第 1 轮结论的效力处置**：**不撤销、不改写**，原样保留于上文。它记录的是当时依据当时的事实作出的批准，**其中两项确认（不改既有工件、修好后须回头重跑比对）在第 2 版中仍然成立并被沿用**。

### 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。依 §3 只呈本轮增量，第 1 轮已确认的不重复。）

**需求**：`themis verify` 不得再返回静默错值；拒绝精确到实际会坏的条件——按所有者已取的 (b)。附带修 `\>` 被判成重定向的误拒。

**明确不做**：不改任何既有工件。

**分解**：一个 step——两类缺陷同处一段命令构造与执行代码，且误拒的修法与拒绝规则读同一份分词结果。

**本次批准须连带接受的一处不确定**：**(b) 的前提是致坏条件能被证出，而能否证出要到详细设计阶段才知道**。证不出时不得就地放宽成"大概是这些条件"——那等于把 (b) 偷偷做成更差的 (a)；届时回到所有者处重取 (a) 或 (c)。

**未覆盖**：真值恰为 `0` 的那类模式，在本 step 落地前**无法从工具输出分辨它是算对了还是坏了**。

### 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本轮投影只有一个待裁定项——批准需求与 step 分解，「批准」一条完整覆盖。

**该答复同时覆盖那处不确定的接受**：**投影已把它写成"这次批准要连带接受的一处不确定"**，即它是批准范围的组成部分而非旁注。**这不是执行者的推断**——呈现时即以该框架提出，所有者在知情下批准。

### 结论

**approved（第 2 轮）。**

确认第 2 版需求本身，以及 step 分解为一个 step。

**本轮确认的三项**：

1. **收敛主张按 (b)**——先确证致坏条件，只拒真会坏的；`\<SPEC`、`a\+b` 这类已证能通过的不得被误拒。
2. **类二误拒与 (b) 无关，无论如何都要修。**
3. **机制证不出时的去向**——回到 `QA.md` 第 2 轮重取，由所有者裁定，**执行者不得就地放宽**。

**本轮未覆盖**：

1. **致坏条件能否被证出**——本轮无法预判，是本 step 最大风险。
2. **真值恰为 `0` 的模式在落地前无从分辨**——该窗口一直开到本 step 完成。
3. **第 1 轮遗留的"三种语境之外是否还有第四种"**——第 2 版已不再以"三种语境"为框架，该问题随第 1 版诊断一并作废，**但"缺陷面是否穷举"这一问仍未解决**：第 2 版实测了 `8` 个模式，**不构成穷举证明**。
