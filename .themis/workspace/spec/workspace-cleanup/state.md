# state.md — workspace-cleanup

> 各闸门行内格式：`- <节点名>：<结论> — <证据路径>`。结论取值由 `flow.md` 通用状态条款按节点类型固定，本文件不自建约定。

## 当前节点

**追问（进行中）。** 第 1 轮四个问题已提出，全部待所有者答复；**未主张收敛**——`rules.md` §2 的收敛判据要求无悬而未答的问题，当前四问皆悬，不满足。

未获答复前不得进入 R1（`flow.md`「追问」节）。四个问题都是产品决策，Agent 不代答：处置形态（删除 vs 保留）、8 份有内容文件的去留、`context/` 子树归属、两份入口文档的索引改法。

**本实例与 `core-removal` 无关**：那是"删除 `core/` 本体与其活跃引用"，已闭合；本实例是新需求"清理 `templates/.themis/workspace/` 包"，另起 spec 实例（`template.md` 明写真正无关的需求另起实例，不作为 step）。

## 各闸门

### spec 级

- Intake：已走完 — `Intent.md`「来源引用」小节（8 条命令来源 + Plan 35 退役事实 + 该包自述 + 用户确认；所有数字均由命令得出，无一处凭印象写入）
- 追问：进行中 — `QA.md` 第 1 轮（四问已提出，答栏全为"待所有者答复"，未代答）
- R1 意图评审：未开始 — 无

### step 级

**尚无 step。** step 分解待 R1 确认——分解方式取决于追问闭合的三个未定点，尤其是处置形态（删除与保留的拆法不同），故 `Intent.md`「step 分解」小节当前只写"待 R1 确认"，不预填。

## 当前性

- `Intent.md`：current（Intake 已产出；问题/期望结果/核心链路三节均含明确标注的待确认项，未主张收敛。「step 分解」待 R1 确认，不预填）
- `QA.md`：current（第 1 轮四问，追加写入未覆盖；答栏未代答）
- `intent-review.md`：未产出（R1 未开始）
- step 级工件：未产出（尚无 step）
