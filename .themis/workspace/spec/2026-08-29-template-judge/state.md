# state.md — 2026-08-29-template-judge

> 各闸门行内格式：`- <节点名>：<结论> — <证据路径>`。结论取值由 `flow.md` 通用状态条款按节点类型固定，本文件不自建约定。

## 当前节点

**本需求已作废，停在抽象设计节点。**

**作废依据**：`docs/adr/2026-08-29-template-is-skeleton-not-fact-source.md`——所有者裁定 `template.md` 只提供命名与结构示例、不作为任何文件内容的事实。**本需求的收敛主张（给 `template.md` 配判定者）与该裁定直接冲突**：配判定者等于承认它是事实源。

**已产出的工件保留不删**：它们记录了当时的判断与依据，**其中执行者两次归因错误的过程正是该 ADR 的直接触发**。依「不改证据去迎合判据」（三次裁定），不追溯改写。

## 各闸门

### spec 级

- Intake：已走完 — `Intent.md`（**其现状分析已被 ADR 推翻**：把三处缺陷全部归因为"`template.md` 无判定者"，而实测显示缺 `state.md` 与缺 `verify/basic.md` 的真正规定在 `flow.md` 各节点「产出」项）
- 追问：已走完 — `QA.md` 第 1 轮（一问：判定者写进哪份文件。**该问本身预设了"应当配判定者"，而 ADR 推翻的正是这个前提**）
- R1 意图评审：approved — `intent-review.md`（所有者原话"b"。**该批准基于已被推翻的前提，随本需求作废**）

### step 级

### step1 — 给 template.md 配判定者（**作废**）

- step 定界：已走完 — `step1/scope.md`
- 抽象设计：已走完 — `step1/specify.md`（五条条目，落地前实跑均为 `0`；**未落地**）
- 其余节点：**未开始，且不再开始**

## 当前性

- 全部工件：**stale**——依 ADR 作废，不再是任何后续需求的依据。**保留仅为记录当时的判断过程。**

## 本需求的教训

**执行者三次诊断，前两次都在给一个错误的前提找补。**

| 次 | 诊断 | 性质 |
| --- | --- | --- |
| 一 | 根本原因是 `template.md` 无判定者 | 归因错误 |
| 二 | 前两处归 `flow.md`，只有 `T-D` 标题需要判 | **仍在维护"应当判 template"这个前提** |
| 三 | —— | 由所有者一句裁定推翻前提 |

**第二次尤其值得记**：它是在所有者追问"为什么需要判 template"之后作出的，**执行者收窄了范围却没有回头问前提本身是否成立**。追问指向的是前提，回答的却是范围。
