# scope.md — 2026-08-29-template-judge / step1

> 本文件是 step 定界节点的实例工件。本节点的前置闸门与产出见 `flow.md`「step 定界」节。以上只指向位置，本文件不复述其文字。
>
> **本文件的全部数字与存在性断言按 `template.md`「断言形态」记法书写，由本节点当场跑出。**

## 承担的上层分解项

`Intent.md`「step 分解」的唯一一项：给 `template.md` 的规定配判定者，并定判定发生的时机。

## 本 step 边界

`rules.md` 新增一节；`template.md` 留一行索引指向该节。**不改任何既有工件；不追溯扫描既有实例是否合规。**

## 与其他 step 的关系

**本需求只有一个 step。**

## 会触及或放宽的既有裁定与安全边界

| 既有裁定 | 本 step 是否触及 |
| --- | --- |
| 「不改证据去迎合判据」（三次确认） | **不触及**——不扫既有工件、不批量修正 |
| 验证须由独立子代理承担（`2026-08-28` 起） | **触及**——本 step 的 verify 须派发。**照办，不例外** |
| `themis` 不在只读白名单 | **触及**——判据要跑 `themis verify`。**照旧人工判** |

## 核实到的、影响做法的三处事实

### （1）§9 是形态先例，且与新节不重叠

`rules.md` §9 与本需求同类——**都是贯穿全部产出工件节点、不依附单一节点的通用规则**。它的判定者写作"该工件所属节点的下一个评审或验证角色；链尾工件在产出时由作者自检，并在下一次被引用时由引用方复核"。

**两者管的东西不同，不重叠**：

- **§9 管工件内部的阐述配比**——控制事实与人类语义是否都在。
- **新节管工件本身是否合规**——该有的文件齐不齐、固定小节齐不齐、写法对不对。

**沿用 §9 的判定者形态，不另发明。**

### （2）当前就有两个实例缺 `state.md`，其中一个是本实例

本节点实跑核对九个必备工件位：**`2026-08-28-pipe-escape-handling` 与本实例 `2026-08-29-template-judge` 各缺 `state.md`**。

**本实例缺失是正常的**——`state.md` 按 `flow.md` 第 35 行要求每节点后更新，而本 step 尚未走到需要它的节点。**但 `pipe-escape-handling` 那个不是**：它已走完 Intake、追问、R1 三个节点。

**这是新节将要判的东西在当下的真实状态**，也说明该缺口仍在持续产生。

### （3）判定时机必须写死，否则与现状无异

`Intent.md` 已写明"只说'谁判'而不说'什么时候判'，与现状无异"。

**本节点补一条依据**：三处缺陷中的 `T-D` 标题层级，**规定得极明确**（`### T-D<n>`）却违反 `7` 次——**不是没人能判，是没有任何一个时点被指定为"此刻该判它"**。

## 本节点新暴露、不在 R1 已批准内容里的一处

**有一处**：事实（2）——**本实例自身当前即处于新节将判为不合规的状态**（缺 `state.md`）。

**本节点主张**：本 step 走到相应节点时补齐即可，**不因此把"本实例合规"写成判据**——那会与 `2026-08-28-verify-identity-dispatch` 的判据 004 同型（自我适用），而那次已记明其循环性。

**不自行认定，交 R2 判定**：是否需要一条判据确保本 step 自身合规。

## 命令证据

- `rules.md` 现有节数：`grep -c '^## §' .themis/spec/rules.md` → `12`
- §9 的判定者形态：`grep -c '链尾工件（摘要）在产出时由摘要作者自检' .themis/spec/rules.md` → `1`
- 缺 `state.md` 的实例数：`2`（本节点逐实例实跑核对四份根目录必备工件）
- `T-D` 标题层级现状：`grep -rl '^### T-D' .themis/workspace/spec/*/step*/task/detail.md | wc -l` → `12`（已由上一需求归一）
