# specify.md — 2026-08-29-template-judge / step1

> 本文件是抽象设计（specify）节点的实例工件。本节点的前置闸门与产出见 `flow.md`「抽象设计（specify）」节；`[basic]` 见 `rules.md` §5，`[横切]` 见 §12。以上只指向位置，本文件不复述其文字。
>
> **本文件的判据一律按 `template.md`「断言形态」记法书写，并由本节点当场实跑。**

## 行为条目

### SPEC-TPLJUDGE-001

`rules.md` 必须新增一节，规定 `template.md` 所定工件形态的判据。

**验收判据**：

`grep -c '^## §13' .themis/spec/rules.md` → `1`

**依据**：所有者裁定取 (b)——判定者以 `rules.md` 新增一节承载，与其余十二节同形。

### SPEC-TPLJUDGE-002

该节必须**指名判定者与判定时机**，时机须落到具体节点，不得写成"应当检查"。

**验收判据**：

`awk '/^## §13/,0' .themis/spec/rules.md | grep -c '判定者'` → `≥1`

**语义部分由 R2 判定**：写下的时机是否真的落到具体节点。**这是本 step 最容易落空的一条**——`Intent.md` 已写明"只说谁判而不说什么时候判，与现状无异"；`scope.md` 事实（3）补了依据：`T-D` 标题层级规定得极明确却违反 `7` 次，**不是没人能判，是没有任何一个时点被指定为"此刻该判它"**。

### SPEC-TPLJUDGE-003

该节的判据必须覆盖 `template.md` 三处无判定者的规定：**工件是否齐备、固定小节是否齐、编号引用写法是否合规**。

**验收判据**：

`awk '/^## §13/,0' .themis/spec/rules.md | grep -cE '齐备|小节|写法'` → `≥3`

**依据**：`Intent.md` 实测——四节中三节无判定者，唯一有的「断言形态」自带判定者（§7），不在本条覆盖范围。

### SPEC-TPLJUDGE-004

`template.md` 必须留一行索引指向新节，使读规定的人能找到判定者。

**验收判据**：

`grep -c '§13' .themis/spec/template.md` → `≥1`

**依据**：R1 裁定的配套处置——取 (b) 的代价是"判定与被判对象分家，读 `template.md` 的人看不到谁判它"，**索引是对该代价的部分补偿**。

### SPEC-TPLJUDGE-005 [横切]

新增不得改变 `rules.md` 既有十二节的任何判据，也不得使既有工件失效。

**验收判据**：

`grep -c '^## §' .themis/spec/rules.md` → `13`（原 `12` 节 + 新增 `1` 节）

`go test ./... 2>&1 | grep -c '^FAIL'` → `0`

**依据**：防回归。**本条带 `[横切]` 标识**——验证对象是既有十二节与既有测试，不是本 step 的产出。

## 来源覆盖

| 条目 | 覆盖的达成状态 | 事实来源 |
| --- | --- | --- |
| 001 | 新增一节 | 用户确认#追问取 (b) |
| 002 | 判定者与时机 | 代码#`scope.md` 事实（3）；代码#`Intent.md` 三处追溯表 |
| 003 | 覆盖三处规定 | 代码#`Intent.md` 四节判定者实测表 |
| 004 | `template.md` 留索引 | 用户确认#R1 的配套处置 |
| 005 | 防回归（横切保护，非达成状态之一） | 代码#`rules.md` 既有十二节与既有测试 |

**`[basic]` 标识：五条均不带。**

## 本节点标出、须 R2 判定的一处

**是否需要一条判据确保本 step 自身合规。**

`scope.md` 事实（2）报出：**本实例当前即缺 `state.md`**，处于新节将判为不合规的状态。

**本节点主张不写成判据**，理由：那会与 `2026-08-28-verify-identity-dispatch` 的判据 004 同型（自我适用），**而那次已记明其循环性——由被约束者自己给出"我合规"的结论**。本 step 走到相应节点时补齐 `state.md` 即可，属正常流程，不需判据强制。

**反面理由须记明**：上一个需求正是靠那条自我适用的判据，才真的派了一次子代理、才发现三处问题。**若本 step 不设同类判据，新节可能像它所修的规定一样——写着但没人在本 step 内验它是否可执行。**

**不自行认定，交 R2 裁定。**
