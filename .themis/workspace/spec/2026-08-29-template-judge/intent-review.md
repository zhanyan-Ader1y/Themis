# intent-review.md — 2026-08-29-template-judge

> 本文件是 R1 意图评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R1 意图评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：给 `template.md` 的规定配判定者，使它从"写着但没人判"变成真闸门。

**实测把根本原因量化了**：`template.md` 四节里三节无判定者——实例目录树规定 `15` 份文件、文件小节表 `16` 行、编号引用写法 `3` 种，**三者全无判定者**。

**追问一问已闭合**：判定者写进 `rules.md` 新增一节（b）。

**未覆盖**：不追溯扫描既有工件。

## 未解决反馈

无。

## 用户原话

**所有者答复：「b」。**

**依 §11 记录对应关系**：该答复对应 `QA.md` 第 1 轮问 1，取选项 (b)——判定者以 `rules.md` 新增一节承载。

**该答复同时构成对需求本身的批准**：选择落法以需求成立为前提。**本节点如实标明这是一处推断**——但其强度较高：**所有者此前已单独指示"修复根本原因，保证后续不再犯错"**，本轮是在该指示之下选具体归属。

**执行者的倾向与裁定一致，但倾向明写不构成推荐**——`QA.md` 原文"(a) 的理由同样成立，且本需求的教训恰恰是'规定与判定分离会导致无人判'"。

## 结论

**approved。**

确认需求本身，以及 step 分解为一个 step。

**本次批准同时确认的一项裁定**：判定者写进 `rules.md` 新增一节，与其余十二节同形。

**该裁定接受的代价已记明**：**判定与被判对象分家——读 `template.md` 的人看不到谁判它**。这是本需求所修问题的一个残余，所有者知情后仍取此路。

**配套处置**：`template.md` 应留一行索引指向新节，使读规定的人能找到判定者。**该处置属详细设计范畴，R1 不预判。**

**本次批准未覆盖**：

1. **不追溯扫描既有工件**——已发现的三处已在上一个需求修复；其余未发现的**正该由本需求建立的机制去发现**。
2. **新机制本身无机器兜底**——判定仍由人类或 Agent 角色在引用时完成，与 §9 同处境。
