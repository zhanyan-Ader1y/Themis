# summary.md — 2026-08-26-workspace-cleanup / step1

> 本文件是摘要节点的实例工件，本 step 的链尾产出。本节点的前置闸门与产出见 `flow.md`「摘要」节；工件阐述方式见 `rules.md` §9——链尾工件由作者自检，下一次被引用时由引用方复核。以上各处只指向位置，本文件不复述其文字。
>
> **前置已满足**：`acceptance.md`「结论」为 accepted。本文件绑定该结论所放行的实际交付（`SPEC-SUMMARY-001`），不绑定任何未经验收的内容。

## 交付摘要

**做了什么。** 取消 `.themis` 的"包源 + 安装副本"两层模型，控制面改为仓库根 `.themis/` 单份入库；同时删除 `templates/.themis/`，其中包含已退役 Plan 35 体系遗留的 workspace 骨架。

**实际交付。**

| 对象 | 结果 |
| --- | --- |
| `templates/.themis/` | 整体删除，目录不存在 |
| `.gitignore` | 四条控制面忽略规则及其理由注释删除 |
| `.themis/spec/` 四份 | 由未跟踪转为入库，内容与落地前基线逐字相同 |
| `.themis/skills/` 两份、`.themis/AGENTS.md` | 由包源迁入并入库，git 记为 rename |
| 六处活引用 | 改写完毕 |

`task/detail.md` 六条任务（T-D1 至 T-D6）全部落地，无遗留。`task/basic.md` 为空段，本 step 未产生 basic 落地。

**验证结论。** `verify/detail.md` 判 `passed`：`specify.md` 五条行为条目逐条断言、五条全部满足；孤儿判定为不存在（空段，无可判对象）；越界边界成立，`templates/.themico/` 只被改动一行。

**一处判据缺陷经三法互证后判为不影响交付。** `SPEC-WSCLEAN-003` 的比对命令实跑报四份全部 DIFF；验证角色未照字面判 `failed`、也未采信实现角色的说法，而是独立跑了忽略行尾比对、去 CR 后字节计数（1940/14041/10846/4709，与基线全部相等）、blob 哈希三种方法，确认内容逐字未变。差异源于本仓库 Windows autocrlf 生效。**判为判据写法缺陷，不是落地缺陷。**

**两处偏差经所有者裁定后放行。**

1. 改动 `internal/themico/integration/skill_contract_test.go` 一处路径断言——该文件不在六条任务的对象清单内。所有者逐字裁定"接受"，效力仅限该处断言。
2. `AGENTS.md`「安装包与项目工作区的边界」一节取改写而非删除——R3 已授权在实现期定夺，理由是该节所述边界概念中 Themico 那一半仍然成立（其 27 份 reference 仍在、仍需安装动作）。

## 绑定的验收结论

- **验收结论**：accepted — `step1/acceptance.md`「结论」。
- **所有者原话**：`step1/acceptance.md`「用户原话」，两条逐字录入（`批准` / `接受`）并附录入说明——首条未对 Go 测试越界表态，该项由单独追问取得，未由执行体推断补足。
- **所依据的证据**：`step1/verify/detail.md`「结论」`passed` 与其「命令证据」V1–V14；三项阻断核查见 `step1/acceptance.md`「阻断核查」。
- **授权边界**：本摘要绑定的交付以 R3 批准的六条任务，加所有者在验收中明确接受的那一处越界为界。

本文件不重述上述结论的判定过程。**若后续引用本摘要，应回到上列各工件核对，不得以本文件为事实源。**

## 中性工件说明

本摘要是**与 Themico 无关的中性工件**。本 step 从 Intake 到摘要全程未调用 Themico；`.themis/spec/` 控制面四份文件不含任何 Themico 概念。

是否把本摘要喂给 Themico 作为经验候选，由**可选 adapter** 决定，该决定在本流程之外；无论是否接入，都**不构成本流程的运行前提**（`SPEC-THEMICO-002`）。Themico 缺席时，本 step 的流程完整性、验收有效性与本摘要的可用性均不受影响。

本 step 未产生任何失败预算计数，也未把任何失败转为经验学习——这两项属 Themico，不在 Themis 实现（`SPEC-FAIL-001`）。

**须辨明一处**：本 step 确实改动了 `templates/.themico/AGENTS.md` 一行、以及一个 themico 的 Go 测试文件。这两处是 Themico **包内文件因路径变化而需同步**，不是 Themis 对 Themico 的运行时依赖——两者松耦合的边界未被本 step 触动。

## 本 step 带出、未随交付关闭的三项

1. **三份迁移文件无判据保护。** R2 裁定不补 `SPEC-WSCLEAN-006`，验证角色明确不判其通过，只记录了内容指纹与基线一致、`AGENTS.md` 三节写作约束完整保留。**本次验收依据的是这些记录，不是自动判据。**

2. **`rules.md` §7 的身份独立在本 step 两段均不成立。** impl 与 verify 由同一次会话承担。该缺口的修复对象是派发层而非控制面条款；本次 soft 执行器下无派发层可依。**验收放行不等于缺口已解决。**

3. **活引用清点存在一处已暴露的方法缺陷。** 清点范围限定 `--include='*.md'`、且搜索模式对 `filepath.Join` 分段拼接的路径无效，两者叠加漏掉了 Go 源码里的第七处引用，直到判据 005 跑出 `ok:9/FAIL:3` 才暴露。**下一次同类需求须同时扩大范围与改换模式**——单改其一仍会漏检。

## 本 step 对控制面的一处验证价值

本 step 是 `verify/basic` 结论取值 **`不适用`** 的首个使用者。该取值于 2026-08-24 加入控制面，用以修复上一个需求（删除旧 core 目录）暴露的缺口——当时空段下结论既非 `passed` 也非 `failed`，控制面无第三种取值，需控制器两次裁定才能走出。

**本次两处均由条款直接给出答案，未动用任何裁定。** 该修复经此实例证实有效。
