# verify/basic.md — 2026-08-26-workspace-cleanup / step1

> 本文件是 verify/basic 节点的实例工件。本节点的前置闸门、产出、结论取值与失败去向见 `flow.md`「verify/basic」节；身份独立、basic 三项判据与拒绝条件见 `rules.md` §7。以上各处只指向位置，本文件不复述其文字。

## 执行身份

- **身份**：Claude Opus 5（模型标识 `claude-opus-5`），本次 spec 流程执行者会话。工作树 `C:/Coding/Themis`，分支 `main`。
- **与 `impl/basic.md`「执行身份」比对的结果：相同——`rules.md` §7 的身份独立在本段不成立。** 两份工件由同一次 agent 会话产出，派发未把这两个节点拆开。
- **这一事实不被本文件掩饰。** 它与结论取值无关（见「结论」），但它是 §7 在 soft 执行器下的一处真实失守，如实记录以供后续引用。`2026-08-19-core-removal` 实例的 basic 段曾发生同型失守，其 detail 段因派发拆分而成立——两段对照证明该闸门的开合取决于派发层，节点自身无法创造第二个身份。

## 断言与实际结果

`rules.md` §7 规定 basic 段只判三项。逐项如下。

| 判据 | 断言对象 | 实际结果 |
| --- | --- | --- |
| 结构存在 | **无断言对象** | `impl/basic.md` 零落地调用，`task/basic.md` 的 `### T-B` 条目数为 0（命令 V2）。没有任何结构被落地，因而没有"存在与否"可判 |
| 可构建 | 有对象但无因果关系 | `BUILD_OK`（命令 V3）。但本段未改动任何代码，该结果与本段无因果关系——它反映的是落地前基线状态 |
| 既有测试无回归 | 有对象但无因果关系 | `ok` 计数 10、`FAIL` 计数 0（命令 V4）。同上，本段未改动任何代码 |

**三项中一项无断言对象，两项虽可跑但与本段无因果关系。**

## 命令证据

**命令 V1** — 本节点起始状态：

```
$ git rev-parse --short HEAD
7a068dc
```

**命令 V2** — basic 段条目数（本节点自行数出，不采信上游自述）：

```
$ grep -c '^### T-B' .themis/workspace/spec/2026-08-26-workspace-cleanup/step1/task/basic.md
0
```

**命令 V3** — 构建：

```
$ go build ./...
（无错误输出）
BUILD_OK
```

**命令 V4** — 既有测试：

```
$ go test ./... | grep -c '^ok'
10
$ go test ./... | grep -c '^FAIL'
0
```

## 结论

**不适用。**

依 `flow.md`「verify/basic」节，该取值仅在 basic 段为空时成立——三项判据没有断言对象，既非通过也非不通过。本段正是此情形：`### T-B` 条目数为 0（命令 V2，本节点自行数出）。

取该值的三项后果，逐条对照本节点实际情况：

1. **不解锁 impl/detail** —— impl/detail 在空段下的前置闸门另有取值（R3 approved），不以本结论为依据。
2. **不触发失败去向、不构成 fail-closed 停靠** —— 本结论不是失败，流程不停靠。
3. **必须附本节点自行做出的空段代码层断言** —— 即命令 V2，已附。

**本结论不表示"验证通过"。** 判据二、三虽输出正向结果，但本段未改动任何代码，那两个结果与本段无因果关系，不构成任何下游节点的通过凭据。

## 说明

**本 step 是 `不适用` 这一取值的第一个使用者。** 该取值于 2026-08-24 加入控制面，用以修复 `2026-08-19-core-removal` 实例暴露的一处缺口——当时空段下结论既非 `passed` 也非 `failed`，控制面无第三种取值，执行者只能借用状态取值"已证"，导致同一字面值同时表示"本节点已走完"与被误读为"验证通过"（该缺口记在 `docs/plan/spec-replay/drift-log.md`「verify/basic」条目）。

**本次使用证实该修复有效**：本节点无需任何裁定即可给出合法结论，也无需借用其他取值。`2026-08-19-core-removal` 当时需要控制器两次裁定（一次判定能否进入 impl/detail，一次判定 fail-closed 是否适用）才能走出同一处；本次两处均由条款直接给出答案。

**一处未因该修复而解决的问题**：身份独立在本段仍然失守（见「执行身份」）。该缺口的修复对象是派发层而非控制面条款——`hard-enforcement-list.md` 第 6 项已记明"机器不应去校验两个小节的文字是否不同，那太容易伪造；应在派发验证节点时拒绝把它交给已承担对应实现节点的执行体"。本次 soft 执行器下无派发层可依，如实记录，不掩饰。
