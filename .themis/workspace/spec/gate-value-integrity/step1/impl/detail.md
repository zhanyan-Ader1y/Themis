# impl/detail.md — gate-value-integrity / step1

> 本节点的前置闸门、产出与失败去向见 `flow.md`「impl/detail」节。以上只指向位置，本文件不复述其文字。
>
> **本文件不含验证结论。**

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话，分支 `main`，动手前 HEAD `deecb44`。
- **授权来源**：R3 approved（`task/review.md`「结论」）。**本 step 三道闸门（R1、R2、R3）均为正式 approved**，与上一个需求形成对照。

## 实际改动

三条任务全部落地。

- **T-D1** — `flow.md` 取值表两行各补 `已授权代行`；表下新增说明段，写明含义、**不等于通过**、**下游不得据它开始**、后续处置（停在本节点，须评审者另行表态）、以及取该值时的记录要求。
- **T-D2** — `rules.md` §11 拒绝条件末补一条「把非授权答复写成授权取值」，并新增一段说明它与前四条的分工：**前四条管记录是否齐备自洽，本条管取值本身选得对不对**，两者可互不相干地失效。
- **T-D3** — 三处合同外取值改为 `**已授权代行。**`；第四处（`citation-overlap-check/step1/acceptance.md`）**结论字段未改**，在其处置说明后补一段指向 R1 第 2 轮追认记录。

## 与批准范围的偏差

**一处，落地后通读发现并当场处置。**

**三处工件里描述该取值的散文与新取值名不一致**：正文写"结论记作**授权代行**"，而取值已改为 `已授权代行`。

**`design.md` 结构决策三定的是"其后原有的说明段落一字不动"**，理由是那些说明记的是当时的答复与判断，属证据。**但"结论记作 X"这一句不是证据，它是在描述取值本身**——取值改了，这句就成了错的描述。

**处置**：只改这一句，并在其中写明"本文件原写作『授权代行——非 approved』，属合同外自创取值，已按本需求回填"。**其余说明段落一字未动。**

**这处区分是本节点当场作出的判断**：`design.md` 未预见到"说明段里也有描述取值的句子"。**如实记出，交 verify 与人工验收判定该区分是否成立。**

## 命令记录

- 落地前 HEAD：`deecb44`。**历史点位**，不按可重跑断言书写。
- 五条判据落地后实跑：001 `1`、002 `1`、003 `1`、004 `0`、005 的 `FAIL` 数 `0`。
- 控制面四份文件 `themis verify` 报告数：各 `0`。
- `themis overlap .themis/spec` → **未发现需人工复核的重合片段**（本次新增文字未引入复述）。

## 尚未关闭、留给 verify/detail 与人工的

1. **新取值的误读风险无机器兜底**——`已授权代行` 与 `approved` 字面都像"可以往下走"，全靠"不得解锁下游"那条限制挡着，而该限制靠人读。
2. **本次落地的一处区分未经批准**——"描述取值的句子"与"记录当时答复的句子"的分界由本节点当场判定。
3. **机器强制部分未做**——授权取自结构化表决、原话由机器落盘，需改人机交互通道。
4. **三处实发按新规则当时应停下**——本 step 不追溯判定，`design.md` 结构决策二已记明该张力。
