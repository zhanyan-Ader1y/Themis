# design-review.md — trace-number-scan / step1

> 本文件是 R2 抽象设计评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R2 抽象设计评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：把检查规则改成认得三种编号写法，并给横切保护判据一个正当出路。

**待裁定项：只有"批准与否"一项。** 上一版曾提三项，其中两项在所有者指出过度提问后由执行者复核并收回——复核确认两者均有依据可自行判定。

**改变原定方向的发现**：三种写法展开后五实例中四个的误报消失、无残留，证明两个盲区恰好是全部原因。

**未覆盖**：判据 005 是弱判据（只护现有实例）；执行者三次同型错误已记入工件；核实过程中又被盲区二绊一次；`state.md` 漏建、五节点后补上。

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影**只有一个待裁定项**（批准与否），「批准」一条即完整覆盖，**无并列裁定项需要追问**。

**与本 spec 前一轮的对照须记明**：R2 投影初版曾含三个裁定项，其中两项（横切标识边界、自证是否有效）在所有者就过度提问提出质疑后由执行者复核收回。**收回的依据不是所有者的指示内容，而是复核发现两者本就有依据可自行判定**——所有者的质疑触发了复核，判定本身由执行者作出并写入 `specify.md`。**本次「批准」覆盖的是收窄后的方案，含那两项自行判定的结论。**

## 结论

**approved。**

五条行为条目获授权，进入详细设计与任务分解。

**本次批准同时确认的两项自行判定**（详见 `specify.md`「本节点标出的两处」）：

1. **横切标识的边界取「验证对象是否为本 step 的产出」**——非本 step 产出、只要求"别弄坏"的才算横切。该边界经既有 `24` 条判据逐条检验，全部分对，横切仅 `SPEC-WSCLEAN-005` 一条。
2. **判据 005 的自证有效但不作为规则成立的依据**——规则正确性由那 24 条的分类结果背书；005 只证明该标识用得上。

**本次批准未覆盖**：判据 005 的弱点（只护现有五个实例）；`rules.md` §2 提问资格缺口（另开需求）。
