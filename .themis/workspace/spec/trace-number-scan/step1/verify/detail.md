# verify/detail.md — trace-number-scan / step1

> 本文件的断言范围、身份独立与拒绝条件见 `rules.md` §7，孤儿判定见 §8，追溯链环 2/3 检出见 §12。以上只指向位置，本文件不复述其文字。
>
> **本文件里的每个数字与存在性断言均由本节点当场跑命令得出，并按 `template.md`「断言形态」记法书写。** `impl/detail.md` 的自述在本文件中只是**被核验的对象**，不作为证据使用。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程会话，分支 `main`。
- **与 `impl/detail.md`「执行身份」比对：相同——身份独立在本段不成立。** `impl/detail.md` 已预先指出并把判定留给本节点，本节点如实确认：**不成立**。
- **结构性偏袒如实标出**：本结论由与实现者同一会话的角色作出，**证据强度低于独立验证**。这是本项目第五次出现同一情形，soft 执行器下无派发层，非本 step 可解。

## 断言与实际结果

`specify.md` 五条行为条目逐条断言，不挑拣。**每条断言注明其验证的任务编号**（`template.md` 环 3 落点）。

| 条目 | 任务 | 判据与实跑 | 结论 |
| --- | --- | --- | --- |
| 001 | T-D1 | `awk '/^环 2 的扫描形态/,/^\*\*本节的能力边界/' .themis/spec/rules.md \| grep -cE '完整\|逗号\|区间'` → `5`（判据 `≥3`） | **满足** |
| 002 | T-D2、T-D3 | `grep -c '横切' .themis/spec/rules.md` → `7`；`grep -c '横切' .themis/spec/template.md` → `1`（判据各 `≥1`） | **满足** |
| 003 | T-D1 | `awk '/^环 2 的扫描形态/,/^\*\*本节的能力边界/' .themis/spec/rules.md \| grep -cE '环 3.*(缩写\|三种写法\|同一套)'` → `1`（判据 `≥1`） | **满足** |
| 004 | T-D3 | `awk '/^## 编号引用写法/,0' .themis/spec/template.md \| grep -cE 'SPEC-\|T-D'` → `5`（判据 `≥1`） | **满足** |
| 005 `[横切]` | 无 | 六实例环 2 重跑：五个差集为空，含 `core-removal/step1` | **满足**（见下方说明） |

### 001 的语义部分另作核查

判据只能证明「完整/逗号/区间」三个词在位，**证明不了三种写法各自真有可执行的定义**。本节点另跑一条：

`awk '/^\*\*三种写法\*\*/,/^\*\*三种写法的共同前提/' .themis/spec/rules.md | grep -c '^| \*\*'` → `3`

三行表格，每行一种写法，各自写明形态与展开结果。**语义成立，不只是词在位。**

### 005 的判定与其残留

判据 005 原文要求：**不得使任何当前真通过的实例转为报断裂**。

按新规则重跑六个 step 实例的环 2 差集（上游排除 `[横切]`、下游按三种写法展开）：

| 实例 | 改动前差集 | 改动后差集 |
| --- | --- | --- |
| `core-removal/step1` | 空 | 空 |
| `authorization-traceability/step1` | `002 005` | 空 |
| `claim-command-evidence/step1` | `002 003 004 005` | 空 |
| `claim-command-evidence/step2` | `004 005` | 空 |
| `trace-number-scan/step1` | —（本 step，改动前不存在） | 空 |
| `workspace-cleanup/step1` | `005` | `005` |

**005 判满足**：改动前唯一真通过的实例是 `core-removal/step1`，改动后仍为空。**无任何实例由通过转为断裂**；另有三个实例由误报转为通过。

**`workspace-cleanup/step1` 的残留如实报出，不判 failed**：

- 它**改动前就报 `5`**（`scope.md` 事实（2）），不属"当前真通过的实例"，因而不在 005 的保护范围内。
- 残留原因是 `SPEC-WSCLEAN-005` 未带 `[横切]` 标识，而该标识是本 step 刚定义的。
- **`impl/detail.md` 有意不去补标**，理由三条：该 step 已 accepted 闭合；按本 step 自己刚定的边界它属"不该扰动的既有东西"；且回头改已验收工件使新规则扫描好看，正是所有者两次裁定确立的"不改证据去迎合判据"所禁止的。

**本节点核验该理由并认为成立**，但**是否接受这一残留属人工验收的裁定**，本节点不代判。

## 命令证据

- `awk '/^环 2 的扫描形态/,/^\*\*本节的能力边界/' .themis/spec/rules.md | grep -cE '完整|逗号|区间'` → `5`
- `grep -c '横切' .themis/spec/rules.md` → `7`
- `grep -c '横切' .themis/spec/template.md` → `1`
- `awk '/^环 2 的扫描形态/,/^\*\*本节的能力边界/' .themis/spec/rules.md | grep -cE '环 3.*(缩写|三种写法|同一套)'` → `1`
- `awk '/^## 编号引用写法/,0' .themis/spec/template.md | grep -cE 'SPEC-|T-D'` → `5`
- `awk '/^\*\*三种写法\*\*/,/^\*\*三种写法的共同前提/' .themis/spec/rules.md | grep -c '^| \*\*'` → `3`
- 六实例环 2 差集：见上表，本节点逐实例实跑

## 追溯链检出（§12 环 2）

**本节点是新展开规则的第一个使用者**，按新规则判本 step 自身：

上游取 `specify.md` 的 `### SPEC-` 条目编号，**排除带 `[横切]` 的 005**，得 `001 002 003 004`；下游取 `task/detail.md` 中的编号并按三种写法展开，得 `001 002 003 004`。差集为空 → **通**。

**须记明**：按旧的字面扫法，本 step 的 `task/detail.md` 会漏掉 `003`——它在 `SPEC-TRACESCAN-001、003` 的逗号缩写里。**本 step 要修的缺口，在本 step 自己的工件上被新规则修好了**，这是规则可用性的一次实证（但不是正确性的证明，正确性由既有 24 条判据的分类结果背书，见 `specify.md`）。

## 结论

**passed。**

五条判据全部满足。追溯链环 2 通。**新规则使三个实例由误报转为通过，无任何实例由通过转为断裂。**

**五项如实带出，不掩饰**：

1. **身份独立不成立**（第五次），本结论证据强度低于独立验证。
2. **`workspace-cleanup/step1` 残留一条差集**——有理由不修，理由本节点核验成立，但**是否接受属人工验收裁定**。
3. **落地中三条判据的 `awk` 范围写错并订正**——同一缺陷本项目第三次实发，三次都靠实跑抓到。订正后经回退复跑确认仍可证伪。
4. **`[横切]` 标注错了无机器兜底**——判定者是 R2 评审者（人），与 `[basic]` 同风险。
5. **展开规则无机器实现**——§12 明写本节不由机器执行；`themis` CLI 只校验断言形态，不校验追溯链。**本 step 定的是规则，不是工具。**
