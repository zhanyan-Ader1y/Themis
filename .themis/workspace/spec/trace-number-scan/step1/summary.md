# summary.md — trace-number-scan / step1

> 本节点的前置闸门与产出见 `flow.md`「摘要」节。本文件绑定 accepted 的交付，不复述工件文字。

## 交付摘要

**追溯链的检查规则修好了两个盲区。**

- **盲区一：编号缩写取不到。** `rules.md` §12 的扫描形态改为"先展开、再取差集"，认得三种写法：完整（`SPEC-X-001`）、逗号并列（`SPEC-X-002、004`）、区间（`SPEC-X-001 至 005`）。环 2 与环 3 适用同一套规则。
- **盲区二：横切保护判据被当作漏引。** 新增 `[横切]` 标识与其判定规则——边界取**「验证对象是否为本 step 的产出」**，非本 step 产出、只要求"别弄坏"的才算横切。标注形态与判定者均与 `[basic]` 同构。
- **`template.md` 补两处**：`specify.md` 结构项补可选标识位；新增「编号引用写法」小节，规定三种写法及"裸数字须紧跟完整编号"的前提。

**实际效果**：五个已完成 step 实例中，**四个原本报误报，改动后三个转为通过**；无任何实例由通过转为断裂。

## 绑定的验收结论

`step1/acceptance.md`「结论」判 **accepted**，所有者原话「批准」。

五条判据全部满足（`verify/detail.md` 判 passed），追溯链环 2 通。

**一条裁定随验收生效**：`workspace-cleanup/step1` 的残留差集**接受，不补标**——**"不改证据去迎合判据"第三次确认**，并扩展到新情形：不仅不为让命令通过而改本次证据，**也不为让新规则好看而改既往已闭合的工件**。代价是该实例持续报一条噪音。

## 中性工件说明

本 step 只改控制面两份文件（`rules.md`、`template.md`），未写代码、未新增任何文件。

## 本 step 带出、未随交付关闭的五项

1. **`workspace-cleanup/step1` 的残留差集**——已裁定接受，噪音持续。若该 step 将来因其他原因重开，可顺带补标。
2. **`[横切]` 标注错了无机器兜底**——判定者是 R2 评审者（人），与 `[basic]` 同风险。
3. **展开规则无机器实现**——§12 明写本节不由机器执行，`themis` CLI 只校验断言形态。**本 step 定的是规则，不是工具。** 这是最值得下一步盯住的一处：规则再准，靠人执行就仍会漏。
4. **判据 005 的弱点**——只护住已存在的实例，护不住将来的新写法。
5. **身份独立不成立**（第五次）——soft 执行器下无派发层。

## 本 step 内发生的同型错误，如实带出

| 次 | 落点 | 内容 | 由谁抓到 |
| --- | --- | --- | --- |
| 19 | `scope.md` | `grep -c '编号'` 写作 `3`，实为 `2`——从更早一轮的显示里抄，未重跑 | **`themis verify`** |
| 20 | `scope.md` | 用 `grep -n` 断言行号，但它输出整行——**记法本身不成立** | **`themis verify`** |
| 21 | `specify.md` | 判据 003 恒真；**第一次订正后仍恒真**（改用的关键词「同样」在现文里已存在） | 落地前实跑 |
| 22 | `specify.md` | 三条判据的 `awk` 范围写作 `/起点/,/^$/`，在第一个空行即终止 | 落地后实跑 |

**第 22 次值得单独记**：`awk '/x/,/^$/'` 这个写法在本项目已是**第三次**出错（前两次在 `authorization-traceability/step1` 的判据 002 与 005）。**三次都靠实跑抓到，没有一次靠人读出来。**

**订正的自查方式也记下**：第 22 次订正后，用 `git stash` 回退到改动前复跑三条判据，全部输出 `0`——**确认订正没有把错误命令换成恒真命令**。这一步不可省，第 21 次的教训正是"订正本身也可能是错的"。

## 一条方法上的经验

**本 step 要修的缺口，在本 step 自己身上绊了执行者两次**：

- 核实横切边界时，用 `grep -n 'AUTHTRACE-005'` 查该编号在不在任务文件里，结果为空，一度误判它也是横切——实际它藏在 `SPEC-AUTHTRACE-001、002、005` 的逗号缩写里。
- 本 step 自己的 `task/detail.md` 按旧扫法会漏掉 `003`（在 `SPEC-TRACESCAN-001、003` 里），换新规则后找回。

**这两次不是巧合，是缺口普遍性的直接证据**——连正在修它的人，在修它的过程中都会被它绊住。
