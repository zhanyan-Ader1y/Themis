# task/basic.md — 2026-08-26-authorization-traceability / step1

> 本文件是详细设计 + 任务节点的产出之一。本节点的前置闸门与产出见 `flow.md`「详细设计 + 任务」节；basic/detail 判定见 `rules.md` §4，`[basic]` 标识见 §5，孤儿阻断见 §8。以上各处只指向位置，本文件不复述其文字。

## 基础任务

**本段为空——零条 basic 任务。**

## 判定依据

依 `rules.md` §4，basic 任务必须**同时**满足两条：被本 step 其他任务依赖，且单独不产生外部可观测行为。**本 step 四个候选任务无一同时满足。**

| 候选 | 被其他任务依赖？ | 单独不产生外部可观测行为？ | 判定 |
| --- | --- | --- | --- |
| `rules.md` 新增 §11 | 否——没有任务依赖它先落地 | **否**——判据 001/002/005 直接观测该节 | detail |
| `template.md` 三份评审工件加「用户原话」 | 否 | **否**——判据 003 直接观测小节划分表 | detail |
| `spec-review-presentation` 补提问配方 | 否 | **否**——判据 004 直接观测该文件 | detail |
| 三处产物的一致性自查 | 否——它在最后，无人依赖 | **否**——它要产出可核验的结论 | detail |

**两条判据均不满足的候选占多数，且四个候选无一被其他候选依赖。**

**与前一个实例的差别值得记**：`2026-08-26-workspace-cleanup` 的候选虽都产生可观测改动，但彼此**有严格次序依赖**（先入库再删源），当时须专门说明"次序依赖不等于 basic 关系"。**本 step 连次序依赖都没有**——三个文件的改动互不影响，可任意顺序落地。四个候选在 §4 的两条判据上**都只差第二条，且第一条也不成立**。

因此本段为空。分类由 R3 判定（§4 判定者条款）。

## 孤儿阻断（§8）

本段为空，**无 basic 任务，因而无可判的孤儿**。`task/detail.md` 四条任务的依赖项均为"无"。该结论供 verify/detail 引用——**但该节点仍须自行数出本文件 `### T-B` 条目数为 0，不得仅凭本行自述判空**（§8 拒绝条件）。
