# verify/basic.md — 2026-08-26-authorization-traceability / step1

> 本文件是 verify/basic 节点的实例工件。本节点的前置闸门、结论取值与失败去向见 `flow.md`「verify/basic」节；判据与拒绝条件见 `rules.md` §7。以上各处只指向位置，本文件不复述其文字。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话。工作树 `C:/Coding/Themis`，分支 `main`。
- **与 `impl/basic.md`「执行身份」比对的结果：相同——`rules.md` §7 的身份独立在本段不成立。** 同一次会话产出两份工件，派发未把节点拆开。
- **不掩饰**：这与前两个实例的 basic 段同型。`2026-08-19-core-removal` 的 detail 段曾因派发拆分而成立，两相对照证明该闸门取决于派发层，节点自身无法创造第二个身份（`hard-enforcement-list.md` 第 6 项）。

## 断言与实际结果

`rules.md` §7 规定 basic 段只判三项。

| 判据 | 断言对象 | 实际结果 |
| --- | --- | --- |
| 结构存在 | **无断言对象** | `impl/basic.md` 零落地调用，`task/basic.md` 的 `### T-B` 条目数为 0（命令 V1）。没有结构被落地 |
| 可构建 | **无断言对象** | 本 step 全部改动为 Markdown 条款，无可构建产物。Go 代码未被触碰 |
| 既有测试无回归 | 有对象但无因果关系 | `ok` 计数 10、`FAIL` 计数 0（命令 V2）。本段未改动任何代码，该结果与本段无因果关系 |

**三项中两项无断言对象，一项虽可跑但与本段无因果关系。**

## 命令证据

**命令 V1** — basic 段条目数（本节点自行数出，不采信上游自述）：

```
$ grep -c '^### T-B' .themis/workspace/spec/2026-08-26-authorization-traceability/step1/task/basic.md
0
```

**命令 V2** — 既有测试：

```
$ go test ./... | grep -c '^ok'    → 10
$ go test ./... | grep -c '^FAIL'  → 0
```

## 结论

**不适用。**

依 `flow.md`「verify/basic」节，该取值仅在 basic 段为空时成立。本段正是此情形：`### T-B` 条目数为 0（命令 V1，本节点自行数出）。

三项后果对照本节点情况：不解锁 impl/detail（其空段前置另有取值 R3 approved）；不触发失败去向、不构成 fail-closed 停靠；已附本节点自行做出的空段代码层断言（命令 V1）。

**本结论不表示"验证通过"。** 判据三虽输出正向结果，但本段未改动任何代码，与本段无因果关系。

## 说明

**本 step 是 `不适用` 取值的第二个使用者**（首个为 `2026-08-26-workspace-cleanup`）。与首次一样，本次无需任何裁定即可给出合法结论——该取值 2026-08-24 加入控制面前，同一处需控制器两次裁定才能走出。

**一处与前两个实例不同的观察**：本 step 的 basic 三项中**有两项无断言对象**（前两个实例只有一项）。原因是本 step 全部改动为 Markdown 条款，连"可构建"都没有对象可判。这不影响结论取值，但说明 §7 的 basic 三项是为有代码产物的 step 设计的；纯条款类 step 下它退化得更彻底。**此观察不构成对 §7 的修改主张**，仅如实记录。
