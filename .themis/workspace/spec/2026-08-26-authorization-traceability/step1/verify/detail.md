# verify/detail.md — 2026-08-26-authorization-traceability / step1

> 本文件是 verify/detail 节点的实例工件。本节点的断言范围、身份独立与拒绝条件见 `rules.md` §7，孤儿判定见 §8。以上各处只指向位置，本文件不复述其文字。
>
> **本文件里的每个数字与每条存在性断言，都由本节点当场跑命令得出。** `impl/detail.md` 的自述在本文件中只是**被核验的对象**，不作为证据使用。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程会话。工作树 `C:/Coding/Themis`，分支 `main`，起始 HEAD `df98437`（命令 V1）。
- **与 `impl/detail.md`「执行身份」比对：相同——`rules.md` §7 的身份独立在本段不成立。** 同一次会话产出两份工件。`impl/detail.md` 已预先指出并把判定留给本节点，本节点如实确认：**不成立**。
- **结构性偏袒如实标出**：本结论由与实现者同一会话的角色作出，**证据强度低于独立验证**。

## 断言与实际结果

`specify.md` 五条行为条目逐条断言，不挑拣。

| 条目 | 断言 | 实际结果 | 判定 |
| --- | --- | --- | --- |
| 001 | §11 存在且四字段齐备 | `1`；`4`（命令 V2） | **满足** |
| 002 | 判定者指名引用者且写明时机 | 判定者字段原文为"下一次引用该结论的角色，在引用发生时判定"（命令 V3） | **满足** |
| 003 | 三份评审工件有原话落点 | `3`（命令 V5） | **满足** |
| 004 | 呈现规范含配方两行 | `2`（命令 V6） | **满足** |
| 005 | 不声称机器强制 | 声称性短语命中 `0`；`unavailable` 声明未改动（命令 V7） | **满足** |

**语义部分的判定，本节点只做能做的那部分，其余如实交出：**

- **001 的语义**（判据是否真表达了追溯要求）与 **004 的语义**（形态是否为配方而非禁令）——`specify.md` 明写由 **R2 评审者**判。R2 已 approved，且投影中已呈现这两处的做法，**本节点确认 R2 的批准覆盖了它们**，不重复判定。
- **002 的语义**（判定者是否可执行）——这是本 step 最大不确定点。本节点能核实的是：判定者字段指名了具体角色与时机（命令 V3），且 §11 正文写明了该角色判什么（两件可见之事）。**能否真的判得动，须由第一个实际引用者验证**，本节点无法代判。
- **005 的第二条**按订正后形态**只列候选、不给通过与否**：唯一命中为 `**本节不由机器执行。**`（命令 V7 明细），是**否定式陈述**——正是应写的内容，非违规。**此判定由本节点作出并写明依据，供后续复核。**

## 命令证据

**V1** 起始：`git rev-parse --short HEAD` → `df98437`

**V2** 判据 001：`grep -c '^## §11'` → `1`；`awk '/^## §11/,0' | grep -cE '适用节点|判据|拒绝条件|判定者'` → `4`

**V3** 判据 002：判定者字段原文 —— `- **判定者**：**下一次引用该结论的角色**，在引用发生时判定——判两件事：对应关系记录是否齐备，所引原话是否支持该结论。`

**V4** 固有弱点显式写明：`awk '/^## §11/,0' | grep -c '固有弱点'` → `1`

**V5** 判据 003：三份评审工件行中含"原话"者 → `3`

**V6** 判据 004：`grep -cE '独立成条|批准本身'` → `2`

**V7** 判据 005：`grep -cE '机器强制|自动拦截|由 CLI 强制'` → `0`；`grep -c 'unavailable' README.md` → `1`；宽词候选唯一命中为否定式陈述

**V8** R3 裁定的附带义务：`testing-notes.md` 含"未经测试" → `1`、含"不具有同等可靠性" → `1`，**义务已履行**

**V9** 孤儿判定：`### T-B` 计数 → `0`；四条 detail 任务的"依赖：无" → `4`，**无一指向 basic 任务**

**V10** 落地范围：`git diff --stat` 显示改动恰为四个文件——`rules.md`(+13)、`template.md`(6 行改)、`SKILL.md`(+2)、`testing-notes.md`(+16)，**全部在 R3 批准的任务对象清单内，无清单外文件被触碰**

## 结论

**passed。**

- `specify.md` 五条行为条目逐条断言，**五条全部满足**。
- **孤儿判定：不存在孤儿**——`### T-B` 条目数为 0（本节点自行数出），四条 detail 任务依赖项均为"无"，无一指向 basic。**前提须随结论一并读**：这是"没有可判对象"，不是"逐个 basic 任务都找到了消费者"。
- **落地范围无越界**：恰四个文件，全在批准清单内（命令 V10）。
- **R3 裁定的附带义务已履行**：`testing-notes.md` 已如实标注新增内容未经测试（命令 V8）。

**本结论不覆盖的三处，必须随结论一并读：**

1. **`impl/detail.md` 记载的两处判据订正**——修改的是 `specify.md`（已批准工件）的判据命令。本节点核实两处订正确未改变判据语义与行为条目要求，且订正后判据能真判失败；**但"是否接受订正已批准工件的判据命令"属人工验收裁量，不是验证角色能判的。**
2. **002 的语义能否成立**——判定者"下一次引用者"是否真判得动，须由第一个实际引用者验证。本节点只能核实字段齐备与正文写明了判什么。
3. **身份独立不成立**——本结论由与实现者同一会话的角色作出，证据强度低于独立验证。

## 说明

**1. 本 step 出现四处判据缺陷，是三个实例中最多的一次。** 前两处在抽象设计节点自查时改正，后两处到落地才暴露。四处的共同根因是**判据写下时未实跑**；第四处（判据与条款各自正确但互不对齐）是前三个实例未出现过的新形态。

**这一事实值得下游知道**：本 step 改的是条款而非文件，判据必须锚在"条款写了什么"上，而**条款措辞在写判据时尚不存在**——判据与条款之间天然存在先后差。前两个实例改的是文件与目录，对象在写判据时已存在，不存在这个差。

**2. 本 step 的产出即将约束本 spec 自身的后续节点。** §11 一旦生效，本 step 的人工验收就须按它记录对应关系。**本节点不预判该节点如何履行**，仅指出：`task/review.md`「用户原话」已记录了一次"追问被拒后按投影全文范围理解批准"的读法，那正是 §11 判定者将来要复核的对象类型。

**3. 本节点核验不动的一样，如实写出**：§11 的判据是否**有效**——即它是否真能让推断补足留下痕迹。这需要真实的推断补足事件来检验，而本 step 未发生（R3 那次是追问被拒后的解释，已如实记录、非推断补足）。**有效性未经检验，本结论只证条款已按设计落地，不证它管用。**
