# acceptance.md — 2026-08-28-verify-identity-dispatch / step1

> 本节点的前置闸门、阻断核查与结论取值见 `flow.md`「人工验收」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 交付视图

**验证角色从此由独立子代理承担，未派发就不许给结论。**

- **`flow.md` 两个验证节点**：写明派发要求、交接见 §7、**未由独立子代理承担时不得给出结论**（`state.md` 取 `进行中`、流程停在实现节点）、**不设"记明理由即可放行"的出路**。
- **`rules.md` §7**：交接最小集四项（验哪些判据、工件在哪、在哪个工作树或分支上操作、结论写进哪个文件）、生效时点、以及四项的事故来源。

**本条款已实际执行过一次**——本 step 自己的 `verify/detail.md` 由派发的独立子代理写出，本会话未代写。

## 阻断核查

| 核查项 | 结论 | 依据 |
| --- | --- | --- |
| verify/detail 是否 passed | **不阻断** | `step1/verify/detail.md`「结论」判 `passed`，**由独立子代理给出** |
| 五条判据是否逐条有断言 | **不阻断** | 同上「断言与实际结果」A 组，`SPEC-VDISP-001` 至 `005` 逐条列出实跑值 |
| 孤儿阻断（§8） | **不阻断** | basic 段为空——`grep -c '^### T-B' step1/task/basic.md` → `0`；子代理另做代码层断言确认本 step 改动 `.go` 文件 `0` 份 |
| 追溯链环 4（§12） | **不阻断** | 本文件上列各项均**引用** `verify/detail.md` 的结论，未重做代码层判定 |

四项均不阻断。

## 独立验证报出的三处问题及其处置

**均不在五条判据内，子代理如实移交；本会话逐条核实全部属实。**

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | 本实例缺 `state.md`、本 step 缺 `verify/basic.md` | **已补齐** |
| 2 | `rules.md` §12 两处历史断言失真（记 `4` 实为 `12`、记 `24` 实为 `54`） | **已修**——改为带日期的点位值，不再写成可重跑断言 |
| 3 | `task/detail.md` 的 `T-D` 标题层级漂移（`7` 份用 `## T-D`） | **已修**——`12` 份全部归一为 `### T-D` |

**第 3 处最危险，须单独记明**：若严格按 `^### T-D` 扫描，那 `7` 份的上游集合会得 `0`——**空集减任何东西都是空集，追溯链会静默"通过"**。**正是 §12 明文警示的"把漏引变成漏报"，而漏报不会被人发现。**

## 三处的根本原因，已定位

**`template.md` 规定的东西没有判定者。**

| 文件 | 判定者处数 |
| --- | --- |
| `rules.md` | `14` |
| `flow.md` | `4` |
| `template.md` | `2`（且均为引用别处的规则） |

`rules.md` 十二节的「适用节点」中，**提到 `template` 的为 `0` 节**。

三处逐个追溯：工件齐备性、控制面自身断言、`T-D` 标题层级——**全部规定在 `template.md`，全控制面无一节判它**。而 `rules.md` 开篇即写「任何判据没有判定者就不是闸门」，**该文件整份处在那个状态**。

**修复根本原因不在本 step 范围内**（R3 已批准的三条任务不含它），所有者已指示先收尾本 spec 再开新需求。

## 用户原话

**待所有者答复。**

**本节点不代行**：`flow.md` 明写人工验收是流程中唯一的授权点，**而本 step 建立的正是"实现者不得为自身产出背书"**。执行者在此自行 `accepted`，是在建立该条款的同一次流程里违反它。

**须记明的是**：本次验证确由独立子代理完成，**身份独立第一次真正成立**——但那只解决了"验证"的独立性，**没有解决"授权"**。子代理是 Agent，不是 Human。

## 结论

**待所有者答复。**

**执行者的自判**：四项阻断核查均不阻断，`verify/detail.md` 由独立子代理判 `passed`，前置闸门满足，**交付在技术上可接受**。

**须所有者表态后方可写入 `accepted` 或 `退回`。**

**本次未覆盖的四项**（无论此后取何结论均成立）：

1. **根本原因未修**——`template.md` 无判定者，已定位、待另开需求。
2. **判据 004 的循环性**——它由验证者自己给出，而验证者正是被约束者。R2 已裁定接受并记明其实际强度。
3. **交接最小集不是穷举**——四项从一次事故反推。
4. **既有十次不追溯重验**——§7 已补生效时点。
