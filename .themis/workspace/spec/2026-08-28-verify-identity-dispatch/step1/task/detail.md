# task/detail.md — 2026-08-28-verify-identity-dispatch / step1

> 本文件是详细设计+任务节点的实例工件之一。本节点的前置闸门与产出见 `flow.md`「详细设计+任务」节；每个 `### T-D<n>` 的固定三项见 `template.md`。编号引用写法见 `template.md`「编号引用写法」。以上只指向位置，本文件不复述其文字。

### T-D1 — `flow.md` 两个 verify 节点写明派发要求与拦截

**行为目标**：两个 verify 节点各含"验证由独立子代理承担"，并写明未派发时不得给出结论。

**做法**：

1. 两节各补一句派发要求。
2. **拦截写成"不得给出结论"**，并写明此时 `state.md` 该行取 `进行中`、流程停在 impl（`design.md` 结构决策二）。
3. **不留"记明理由即可放行"的余地**——所有者已明确拒绝该出路，留余地等于架空裁定。

**对应 `specify.md` 条目**：判据 SPEC-VDISP-001、002。

**依赖的基础任务**：无。

### T-D2 — `rules.md` §7 补交接最小集与生效时点

**行为目标**：§7 含交接四项与生效时点一句。

**做法**：

1. **交接最小集四项**：验哪些判据、工件在哪、在哪个工作树或分支上操作、结论写进哪个文件。
2. **写明这四项的来源是一次实际事故**（子代理把提交打到 `main`），**并写明它不是穷举**。
3. **生效时点**：补一句"本要求自 `2026-08-28` 修订起适用"，**紧邻要求本身**。
4. **不改 §7 现有的三项 basic 判据与 detail 断言范围**（判据 005 护住）。

**对应 `specify.md` 条目**：判据 SPEC-VDISP-003。

**依赖的基础任务**：无。

### T-D3 — 本 step 的 verify 由独立子代理承担

**行为目标**：`step1/verify/detail.md` 由派发的子代理写出，其「执行身份」与 `impl/detail.md` 不同。

**做法**：

1. **在 impl 完成后派发子代理**，交接内容按 T-D2 定的四项给全。
2. **子代理独立跑五条判据并写出 `verify/detail.md`**，含其自身的执行身份记录。
3. **本会话不代写该文件**——代写即违反本 step 正在建立的条款。

**对应 `specify.md` 条目**：判据 SPEC-VDISP-004。

**依赖的基础任务**：无。**但须在 T-D1、T-D2 之后**——交接四项要先定下来才能照它派。

## 判据 005 无任务承担，理由

`SPEC-VDISP-005` 带 `[横切]` 标识，验证对象是 §7 既有内容与既有测试，**不是本 step 的产出**。

## 执行次序

**T-D1 → T-D2 → T-D3**，**有实质依赖**：T-D3 的交接内容由 T-D2 定义。
