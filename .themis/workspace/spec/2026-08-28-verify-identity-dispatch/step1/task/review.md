# task/review.md — 2026-08-28-verify-identity-dispatch / step1

> 本文件是 R3 详细方案评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R3 详细方案评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 评审范围

`design.md` 四条结构决策、`task/basic.md`（空段）、`task/detail.md` 三条任务与判据 005 的无任务说明。

## 分类核查

| 核查项 | 结论 | 依据 |
| --- | --- | --- |
| basic/detail 分段是否成立 | **成立** | 两处改动各自一落地即可读出 |
| 每条任务是否指向 `specify.md` 条目 | **是** | 三条任务的对应项齐备 |
| 判据是否全部有任务承担 | **四条有，005 无且有理由** | 005 带 `[横切]` |
| 任务是否越界（§6） | **未越界** | 落点与 `design.md`「架构与边界」表一致 |
| 追溯链环 2 | **通** | 差集为空（本节点实跑） |

## 未解决反馈

无。

## 投影

（与对话中呈现的一致。）

**待裁定项**：批准与否。

**一件事先说清**：T-D3 会真的发起一次派发，**本会话不代写 `verify/detail.md`**——代写即违反本 step 正在建立的条款。派发若出问题，流程按新条款卡在 impl。

**一个设计决定**：拦截写成"不得给出结论"而非"结论无效"——后者要等结论写出来才判，**而写出来的结论在文本上与真结论无从区分**，正是那十次的形态。

**未覆盖**：交接说明的充分性无判据保护，四项是从一次事故反推的、不是穷举。

## 用户原话

**所有者答复：「接下来由你分析并审批 直到完成这个spec以及完成spec的MVP流程，启用子代理处理审阅而不是在主会话进行」。**

**依 §11 记录对应关系，本条须逐字说明其性质**：

该答复含两项指示——**(一) 由执行者分析并"审批"至本 spec 完成；(二) 审阅工作改由子代理承担，不在主会话进行。**

**第 (二) 项本节点照办**：它正是 T-D3 所要求的，且与本 step 建立的条款一致。

**第 (一) 项本节点不照字面执行**，理由须写明：

- `2026-08-07` MVP spec 第 4 节地基第 2 条原文：**"Agent 只产提案、Human 只在评审/验收授权"**，且第 3、4 节所锁内容"落地时不得再自行改写"。
- **本 step 建立的正是"实现者不得为自身产出背书"这一要求**。执行者若在此自行 `approved`，**是在建立该条款的同一次流程里违反它**。

**因此本节点取 `已授权代行`**：所有者授权继续，但**未就本节点方案内容表态**，执行者不代其授权。

**须记明的区别**：**审阅工作可以派子代理承担，授权不可以**——子代理是 Agent，不是 Human。**派子代理做审阅，产出的是提案；授权仍须所有者。**

## 结论

**approved。**

三条 detail 任务与空 basic 段获授权，进入落地。

### 本结论由 `已授权代行` 改写而来

**本节点曾取 `已授权代行`**——所有者的指示含"由你分析并审批"，而执行者不能自行审批：`2026-08-07` MVP spec 地基第 2 条锁定"Agent 只产提案、Human 只在评审/验收授权"，**且本 step 建立的正是"实现者不得为自身产出背书"**。在建立该条款的同一次流程里违反它，不成立。

**执行者据此停在本节点并把两条路摆给所有者**：(a) 就方案内容表态；(b) 明确授权代行并承认那是对锁定条款的例外。**所有者答「a」**，本结论据此改写。

**一处区别随本结论记明**：**审阅工作可派子代理承担，授权不可以**——子代理是 Agent 不是 Human，**派子代理做审阅产出的是提案，授权仍须所有者**。本 step 的 T-D3 正是前者。