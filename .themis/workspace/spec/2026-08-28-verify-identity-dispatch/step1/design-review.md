# design-review.md — 2026-08-28-verify-identity-dispatch / step1

> 本文件是 R2 抽象设计评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R2 抽象设计评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：控制面写明验证由独立子代理承担、未派不得给结论、并规定交接内容。

**三个待裁定项**：(一) 把"本 step 自己合规"写成判据是否成立（存在循环）；(二) 既有十次怎么记；(三) 是否批准。

**单独标出最易被用歪的一条**：判据 002 的限制若留余地，等于把所有者"不设记明理由即可放行"的裁定架空。

**未覆盖**：交接说明写不好会出事——上一轮有子代理把提交打到 `main`。

## 判据与边界的交叉核查（`flow.md` R2 节）

| 本 step 已确认的边界 | 判据是否冲突 | 处置 |
| --- | --- | --- |
| soft 执行器、机器强制 `unavailable` | **不冲突**——派发不依赖任何未实现的 CLI 能力，`2026-08-19-core-removal` 已实证 | 无需处置 |
| `themis` 不在只读白名单 | **冲突**——判据要跑 `themis verify` | **照旧人工判** |
| 「不改证据去迎合判据」 | **不冲突**——既有十次的工件一字不动 | 无需处置 |

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影提出三项待裁定，**「批准」一条覆盖三项**。投影对前两项均给出推荐与理由，并写明反面理由（循环性）与判错后果。**答复未提出异议，故取推荐项为裁定内容。**

## 结论

**approved。**

五条行为条目获授权，进入详细设计与任务分解。

### 裁定一：判据 004 保留，接受其循环性

**理由**：只写条款而本 step 自己不派，**等于新写一条与那十次同样无法履行的要求**。

**循环性的实际强度已记明**：由独立子代理写下"我是独立子代理"，**比由实现者写下同一句话强度高——因为写它的那次会话确实是另一个**。

**已知代价**：若该循环确实无效，则本 step 是自己给自己发合格证。**摘要须记明这一点。**

### 裁定二：既有十次不改工件，在 §7 补一句生效时点

**理由**：不补则后来者会把那十次误读为违反新条款。**代价是 §7 多一句话。**

**本次批准未覆盖**：交接说明的充分性无判据保护——**判据 003 只验最小集的字样在位，验不出实际派发时是否够用**。

