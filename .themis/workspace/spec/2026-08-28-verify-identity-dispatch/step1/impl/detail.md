# impl/detail.md — 2026-08-28-verify-identity-dispatch / step1

> 本节点的前置闸门、产出与失败去向见 `flow.md`「impl/detail」节。以上只指向位置，本文件不复述其文字。
>
> **本文件不含验证结论。** 五条判据是否满足属 verify/detail，**而该节点按本 step 新立的条款须由独立子代理承担**。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话，分支 `main`，动手前 HEAD `3034289`。
- **授权来源**：R3 approved（`task/review.md`「结论」）。**该结论由 `已授权代行` 改写而来**——所有者最初的指示含"由你分析并审批"，执行者停下并说明不能自行审批，所有者其后答「a」（就方案内容表态）。
- **本节点不承担 verify/detail**：**这是本 step 的核心要求，也是它第一次自我适用**。

## 实际改动

T-D1、T-D2 已落地；**T-D3 尚未执行**（见下）。

- **T-D1** — `flow.md` 两个 verify 节点各补：验证角色由独立子代理承担、交接见 §7、**未由独立子代理承担时不得给出结论**（`state.md` 取 `进行中`、流程停在 impl）、不设"记明理由即可放行"的出路。**verify/basic 节另补一句拦截点的理由**：判"已写出的结论无效"拦不住任何东西，因为由实现者写出的结论在文本上与独立验证者写出的无从区分。
- **T-D2** — `rules.md` §7 补：派发要求、生效时点（`2026-08-28` 修订起适用，在此之前的实例不因本条被判违反）、**交接最小集四项**及其事故来源，并写明四项不是穷举、派发不是机器强制。

## T-D3 的执行方式与已知风险

**T-D3 是"派发子代理写出本 step 的 `verify/detail.md`"**，本节点执行到此为止——**下一步由本会话发起派发，但 `verify/detail.md` 的内容不由本会话撰写**。

**代写即违反本 step 正在建立的条款**，且会使判据 004 失去意义。

**交接内容按 T-D2 定的四项给全**：验五条判据、工件在本实例 step1 下、在 `main` 分支的主工作树上操作、结论写入 `step1/verify/detail.md`。

**已知风险如实记明**：上一轮派发时出过子代理把提交打到 `main` 而非工作树的事故。**本次交接明确了第三项（在哪操作）**，但**四项不是穷举**，仍可能有未预见的交接缺口。**若派发失败，按新条款流程停在本节点**——这是所有者选 (a) 硬性拦截时已知悉的代价。

## 命令记录

- 落地前 HEAD：`3034289`。**历史点位**，不按可重跑断言书写。
- 四条判据落地后实跑：001 `2`、002 `2`、003 `2`、005 的第一条 `1`。
- 全仓 `go test ./...` 的 `FAIL` 数：`0`。
- 控制面四份 `themis verify` 报告数：各 `0`；`themis overlap` 未发现重合片段。

## 尚未关闭、留给 verify/detail 与人工的

1. **判据 004 只能由子代理自己给出**——存在循环，R2 已裁定接受并记明其实际强度。
2. **交接说明的充分性无判据保护**——四项从一次事故反推，不是穷举。
3. **既有十次不追溯重验**——§7 已补生效时点，使它们不被误读为违反新条款。
