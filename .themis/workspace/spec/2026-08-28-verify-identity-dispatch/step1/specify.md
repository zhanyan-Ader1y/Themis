# specify.md — 2026-08-28-verify-identity-dispatch / step1

> 本文件是抽象设计（specify）节点的实例工件。本节点的前置闸门与产出见 `flow.md`「抽象设计（specify）」节；`[basic]` 见 `rules.md` §5，`[横切]` 见 §12。以上只指向位置，本文件不复述其文字。
>
> **本文件的判据一律按 `template.md`「断言形态」记法书写，并由本节点当场实跑。**

## 行为条目

### SPEC-VDISP-001

`flow.md` 的两个 verify 节点必须写明**验证角色由独立子代理承担**。

**验收判据**：

`grep -c '独立子代理' .themis/spec/flow.md` → `≥2`

**依据**：`scope.md` 事实（2）——派发不依赖任何未实现的 CLI 能力，`2026-08-19-core-removal/step1` 已实证可行。

### SPEC-VDISP-002

必须写明**未派发时不得给出结论**，流程停在 impl。

**验收判据**：

`grep -c '未由独立子代理承担' .themis/spec/flow.md` → `≥1`

**语义部分由 R2 判定**：写下的限制是否真的挡住"记明理由即可放行"。**这是本 step 最容易被用歪的一条**——所有者已明确拒绝那条出路，条款若留余地，等于把裁定架空。

**依据**：追问闭合取 (a) 硬性拦截。

### SPEC-VDISP-003

`rules.md` §7 必须规定**交接的最小集**：验哪些判据、工件在哪、在哪个工作树或分支上操作、结论写进哪个文件。

**验收判据**：

`awk '/^## §7/,/^## §8/' .themis/spec/rules.md | grep -c '交接'` → `≥1`

**依据**：`scope.md` 事实（3）——上一轮派发时子代理把提交打到了 `main` 而非工作树，**那正是交接说明不足的后果**。子代理冷启动，拿不到实现者的推理过程。

### SPEC-VDISP-004

本 step 自身的 verify 节点**必须由独立子代理承担**。

**验收判据**：

`step1/verify/detail.md` 的「执行身份」小节记明其为独立子代理，且与 `impl/detail.md` 不同。

**依据**：`scope.md` 事实（1）——**这是本条款的第一次自我适用，其可行性即本需求要证的东西**。若连本 step 都派不出去，条款不成立。

**本条的验收方式如实说明**：它由**验证节点自己**给出，而验证节点正是被约束的对象。**该循环无法在本 step 内消除**——但由独立子代理写下的"我是独立子代理"，比由实现者写下的同一句话强度高，因为**写它的那次会话确实是另一个**。

### SPEC-VDISP-005 [横切]

改动不得使既有工件失效，也不得改变 §7 现有的三项 basic 判据与 detail 断言范围。

**验收判据**：

`awk '/^## §7/,/^## §8/' .themis/spec/rules.md | grep -c '结构存在、可构建'` → `1`

`go test ./... 2>&1 | grep -c '^FAIL'` → `0`

**依据**：防回归。**本条带 `[横切]` 标识**——验证对象是 §7 既有内容与既有测试，不是本 step 的产出。

## 来源覆盖

| 条目 | 覆盖的达成状态 | 事实来源 |
| --- | --- | --- |
| 001 | 写明派发要求 | 用户确认#「恢复效力」；代码#`scope.md` 事实（2） |
| 002 | 硬性拦截 | 用户确认#追问取 (a) |
| 003 | 交接最小集 | 代码#`scope.md` 事实（3）实际事故 |
| 004 | 本 step 自我适用 | 代码#`scope.md` 事实（1） |
| 005 | 防回归（横切保护，非达成状态之一） | 代码#§7 现有内容与既有测试 |

**`[basic]` 标识：五条均不带。**

## 本节点标出、须 R2 判定的两处

### 一、判据 004 把"本 step 自己合规"写成了判据

`scope.md` 事实（1）主张这么写，**本节点采纳但标出交 R2**。

**理由**：条款的可行性只能靠实际派一次来证。**若只写条款而本 step 自己不派，等于新写一条与那十次同样无法履行的要求。**

**反面理由须记明**：判据 004 由验证节点自己给出，**而验证节点正是被约束者**——存在循环。**若 R2 认为该循环不可接受，判据 004 应删去，改为在摘要中记明"本条款尚未经实测"。**

### 二、既有十次的处置

R1「未覆盖」写明不追溯重验，**但未定该怎么记**。

**本节点主张**：不改既有工件，**在 §7 补一句说明该要求自本次修订起生效**，使读者不会把既有十次误读为违反新条款。

**不自行认定**，交 R2 裁定。
