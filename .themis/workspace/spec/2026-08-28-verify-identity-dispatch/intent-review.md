# intent-review.md — 2026-08-28-verify-identity-dispatch

> 本文件是 R1 意图评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R1 意图评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：在控制面写明验证角色怎么来，使身份独立的要求可被履行。

**改变原定方向的发现**：**缺的不是条款，是机制**——`flow.md` 与 `rules.md` 提及派发的处数各为 `0`。条款完整（§7 有拒绝条件、有判定者），但控制面从未说明验证角色从哪来。**这解释了十次"有记录地绕过"**：条款要求了一件控制面没给出路径的事。

**追问一问已闭合**：取硬性拦截（a），不设"记明理由即可放行"的出路。

**未覆盖**：既有十次不追溯重验。

## 未解决反馈

无。

## 用户原话

**所有者答复：「a」。**

**依 §11 记录对应关系**：该答复对应 `QA.md` 第 1 轮问 1，取选项 (a) 硬性拦截。

**该答复同时构成对需求本身的批准**：选择落法以需求成立为前提。**本节点如实标明这是一处推断**——但其强度较高：**所有者此前已单独裁定"恢复效力"而非"认可现状"**，本轮是在该裁定之下选具体落法。

## 结论

**approved。**

确认需求本身，以及 step 分解为一个 step。

**本次批准同时确认的一项裁定**：**取硬性拦截**——验证未由独立子代理承担时 verify 节点不得给出结论，流程停在 impl。**不设"记明理由即可放行"的出路。**

**代价已由所有者知悉并承担**：每个 step 多一次派发；派发失败时流程卡住。

**本次批准未覆盖**：

1. **既有十次不追溯重验**——那属「不改证据」，且十次的记录本身完整。**如何处置交 R2 判定。**
2. **本需求自身的验证怎么办**——按新条款，本 step 的 verify 也须派子代理。**这是本条款的第一次自我适用**，其可行性即本需求的实测。
