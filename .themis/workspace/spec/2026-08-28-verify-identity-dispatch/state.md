# state.md — 2026-08-28-verify-identity-dispatch

> 各闸门行内格式：`- <节点名>：<结论> — <证据路径>`。结论取值由 `flow.md` 通用状态条款按节点类型固定，本文件不自建约定。

## 当前节点

**本需求已完成。** step1 走完十四个节点并经人工验收。**这是本项目第一次由独立子代理完成验证、再由所有者授权的验收**——验证的独立性与授权的人类性两者同时成立。

**本文件是补写的**：产出时机晚于 verify/detail，**由独立验证子代理在核验时报出缺失**——它无法从 `state.md` 读出自己的前置闸门，只能靠 `impl/detail.md` 推断。**这正是 `flow.md` 第 35 行那条要求所防的情形**，本会话第二次同型遗漏。

## 各闸门

### spec 级

- Intake：已走完 — `Intent.md`（**定位出缺的是机制不是条款**：`flow.md` 与 `rules.md` 提及派发的处数各为 `0`）
- 追问：已走完 — `QA.md` 第 1 轮（一问：硬性拦截还是记明理由。**执行者倾向前者但明写不构成推荐**——代价落在所有者时间上）
- R1 意图评审：approved — `intent-review.md`（所有者原话"a"，取硬性拦截）

### step 级

### step1 — 派发要求与交接最小集（已完成）

- step 定界：已走完 — `step1/scope.md`（暴露一处 R1 未定处置：**本 step 自己的 verify 也受新条款约束**）
- 抽象设计：已走完 — `step1/specify.md`（五条条目；判据 004 把"本 step 自我适用"写成判据，**循环性一并标出**）
- R2 抽象设计评审：approved — `step1/design-review.md`（两项裁定：判据 004 保留并接受循环；既有十次不改工件、在 §7 补生效时点）
- 详细设计+任务：已走完 — `step1/design.md` 与两份任务文件（四条结构决策；**拦截写成"不得给出结论"而非"结论无效"**——后者要等结论写出才判，而写出的结论在文本上与真结论无从区分）
- R3 详细方案评审：approved — `step1/task/review.md`（**该结论由 `已授权代行` 改写而来**：所有者初次指示含"由你分析并审批"，执行者停下说明不能自行审批，所有者其后答"a"就方案内容表态）
- impl/basic：不适用 — basic 段为空
- verify/basic：不适用 — `step1/verify/basic.md`（**补写**；空段三项无断言对象。**未派发子代理并写明理由**：派人去验"没有东西可验"不产生证据，属空段固有形态）
- impl/detail：已走完 — `step1/impl/detail.md`（T-D1、T-D2 落地；**T-D3 为派发本身**，由本会话发起、内容不由本会话撰写）
- verify/detail：passed — `step1/verify/detail.md`（**由独立子代理写出，本 step 条款的第一次实际执行**。五条判据全满足，两环差集为 `0`。**另报出三处不在判据内的问题**，见下）
- 人工验收：accepted — `step1/acceptance.md`「结论」（所有者原话"批准"；三处已修问题与根本原因的定位属知情披露，所有者知悉后仍批准）
- 摘要：已走完 — `step1/summary.md`（绑定 accepted 的交付；带出未关闭的四项，其中最要紧的是根本原因未修）

## 独立验证子代理报出的三处问题

**均不影响 passed，本会话已核实全部属实**：

1. **本实例缺 `state.md`、本 step 缺 `verify/basic.md`**——**已补齐**（即本文件与 `step1/verify/basic.md`）。**判据只验条目内容，不验工件齐备**，两次同型遗漏都不是判据抓到的。
2. **`rules.md` §12 两处历史断言失真**：记 `4` 实为 `12`、记 `24` 实为 `54`。**成因是实例数增长，本 step 未碰 §12**。`themis verify` 报告数为 `0`——**那两行含管道与 glob，CLI 识别不到**，与在办的 `2026-08-28-pipe-escape-handling` 相关。
3. **`task/detail.md` 的 `T-D` 标题层级漂移**：`template.md` 规定 `### T-D<n>`，实际 `7` 份用 `## T-D`、`5` 份用 `### T-D`。**若严格按 `^### T-D` 扫，本 step 上游集合会得 `0`——正是 §12 警示的"把漏引变成漏报"。** 既存漂移，非本 step 引入。

## 当前性

- `Intent.md`、`QA.md`、`intent-review.md`：current
- `step1` 各工件：current
