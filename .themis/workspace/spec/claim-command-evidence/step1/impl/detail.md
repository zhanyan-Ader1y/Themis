# impl/detail.md — claim-command-evidence / step1

> 本节点的前置闸门、产出与失败去向见 `flow.md`「impl/detail」节。以上各处只指向位置，本文件不复述其文字。
>
> **本文件不含验证结论。** 五条判据是否满足属 verify/detail。**本文件的全部数字断言按新落地的形态书写**——这是形态的第一次实际使用。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话，分支 `main`，动手前 HEAD `2dcfe44`。
- **授权来源**：R3 approved。basic 段为空，本节点前置闸门取 R3 approved。
- **本节点不承担 verify/detail**：对方那栏由对方填写。**须先说明**：soft 执行器下无派发层，verify/detail 很可能仍由本会话承担，届时身份独立不成立——由该节点自行如实记录。

## 实际改动

`task/detail.md` 两条任务全部落地。

- **T-D1 — `.themis/spec/template.md` 新增「断言形态」一节。** 写明五件事：两种记法（两段式为常态、三段式在有推导时用）、适用于全部数字与存在性断言、单个断言写在一行内、判定者为验证角色、以及 CLI 的比对规则（供 step2 消费）。**未改小节划分表**——形态是写法约定，不属于任何一行。
- **T-D2 — 一致性自查。** 五条判据的命令部分逐条跑出（见「命令记录」），并完成 `specify.md` 开头声明的自证。

## 与批准范围的偏差

**落地范围无偏差**——两条任务的对象与 R3 批准的清单一致，未触碰清单外文件。

**自证结果如实记录（T-D2 要求）**：把 `design.md`「事实依据」四条命令按新形态改写，**四条全部写得下来**，且分布如下——

- **D1 用三段式**：三个实例的出现次数是三个输出，据此写下"逐实例递增"这一断言值。**这是三段式的真实用武之地，不是为了凑形态而造的例子。**
- **D2、D3、D4 用两段式**：输出即断言值。

**该分布佐证了 R2 裁定「允许两段式」是对的**：四条里只有一条需要三段，若强制三段，另三条要凭空多写一遍。

## 命令记录

**命令 D1** — 落地前 `template.md` 是否已有该节：

`grep -c '断言形态' .themis/spec/template.md` → `0`

（落地前实跑，证实判据可证伪。）

**命令 D2** — 落地后判据 001 两条：

`grep -c '断言形态' .themis/spec/template.md` → `1`

`awk '/断言形态/,0' .themis/spec/template.md | grep -cE '命令|断言值'` → `3`

**命令 D3** — 落地后判据 002：

`awk '/断言形态/,0' .themis/spec/template.md | grep -c '→'` → `2`

**命令 D4** — 落地后判据 003：

`awk '/断言形态/,0' .themis/spec/template.md | grep -cE '输出|原样'` → `2`

**命令 D5** — 落地后判据 004：

`awk '/断言形态/,0' .themis/spec/template.md | grep -c '一行'` → `1`

**命令 D6** — 落地后判据 005：

`awk '/断言形态/,0' .themis/spec/template.md | grep -c '判定者'` → `1`

**命令 D7** — 自证：四条命令按新形态改写后仍可书写：

`按新形态改写 design.md 四条命令` → `D1 三段式、D2/D3/D4 两段式` → `四条全部可书写`

**本节点全部七条命令记录均按新形态书写**，其中 D7 用三段式（改写结果是中段、可书写性是断言值）。
