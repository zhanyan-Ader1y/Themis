# task/basic.md — claim-command-evidence / step1

> 本文件是详细设计 + 任务节点的产出之一。basic/detail 判定见 `rules.md` §4，`[basic]` 标识见 §5，孤儿阻断见 §8。以上各处只指向位置，本文件不复述其文字。

## 基础任务

**本段为空——零条 basic 任务。**

## 判定依据

依 `rules.md` §4，basic 任务须**同时**满足两条：被本 step 其他任务依赖，且单独不产生外部可观测行为。**本 step 两个候选无一同时满足。**

| 候选 | 被其他任务依赖？ | 单独不产生外部可观测行为？ | 判定 |
| --- | --- | --- | --- |
| `template.md` 新增「断言形态」一节 | 否——无任务依赖它先落地 | **否**——五条判据全部直接观测该节 | detail |
| 一致性自查 | 否——它在最后，无人依赖 | **否**——它要产出可核验的结论 | detail |

**两条判据均不满足，且两个候选之间无依赖关系。**

**本 step 的候选数是三个实例中最少的**（前三个实例分别为 6、4、4 个候选），因为它只改一份文件的一节。候选少不改变判定方式：仍逐条对照 §4 两条判据，不因"就一个候选"而略过判定。

## 孤儿阻断（§8）

本段为空，**无 basic 任务，因而无可判的孤儿**。`task/detail.md` 两条任务的依赖项均为"无"。该结论供 verify/detail 引用——**但该节点仍须自行数出本文件 `### T-B` 条目数为 0**（§8 拒绝条件）。
