# task/review.md — claim-command-evidence / step1

> 本文件是 R3 详细方案评审节点的实例工件。分类判定见 `rules.md` §4（本节点是判定者），结构决策归属见 §6。以上各处只指向位置，本文件不复述其文字。
>
> 「用户原话」与「结论」在所有者答复前保持空缺。「给评审者的呈现」按 `.themis/skills/spec-review-presentation/SKILL.md` 写成；其下的「评审范围」「分类核查」是给执行者与验证角色看的完整记录，评审者不必读。

## 给评审者的呈现

### 这一步要做什么

把上一轮批准的写法写进规则文件——加一节，说明数字该怎么记、谁来判记得对不对。改一份文件的一节，然后自查。

### 需要你裁定

**无。** 两条任务全部可回溯到上一轮已批准的设计，没有引入新决定。若你对下面任何一处不认可，直接说不批准并指出即可。

### 改变了原定方向的发现

**无。**

### 其余要点

- **"准备工作"那一栏是空的。** 规则要求准备工作必须**同时**满足"被别的任务依赖"和"自己不产生外部可见改动"两条，这次两个候选**两条都不满足**——都直接改受判据观测的文件，且互不依赖。

- **自查里加了一条上一轮设计稿没写的**：新写法定完后，**拿这一步自己的工件试一遍**——把设计稿里那四条命令按新写法改写，看写不写得下来。**写不下来则写法不成立。** 这是这一步唯一能在自己内部完成的验证，抽象设计那份文件开头就声明了。

### 我知道但还没解决的

上一轮已确认的三处仍然成立（拦不住"输出对但推导错"、判定者是人判人写的东西、以及更早两处），本轮无新增。

**本次批准是实现授权**——批准后我才开始改规则文件。

---

## 评审范围

`design.md`（已随 R2 批准锁定的部分不重复）+ `task/basic.md`（空段）+ `task/detail.md`（两条任务）。**本节点是实现授权点。**

## 分类核查

### 1. basic 段为空，判定成立

§4 要求同时满足被依赖与单独无外部可观测行为两条。`task/basic.md` 逐个列出两个候选的判定：**两条均不满足**——都直接改受判据观测的文件，且两者之间无依赖。

**核查结论：空段成立。**

**本 step 的候选数是四个实例中最少的**（前三个分别 6、4、4 个，本次 2 个），因为它只改一份文件的一节。`task/basic.md` 已写明：候选少不改变判定方式，仍逐条对照，不因"就一个候选"而略过。

### 2. 任务未替 `design.md` 引入新结构（§6）——一处例外须标出

逐条核对：T-D1 出自「结构决策」第 1、2、3、4 条，T-D2 出自 `specify.md` 五条判据。**两条均可回溯。**

**例外**：T-D2 中"取 `design.md` 四条命令改写为新形态、看写不写得下来"这一自证项，**`design.md` 未写**，是任务节点新增。

**本节点的处理**：该项**不引入新结构**——它不改变 T-D1 要写什么，只增加一次对产物的检验。其依据在 `specify.md` 开头：本 step 规定的正是"断言该怎么写"，**若形态连自己都写不下来，那它就不成立**；该文件已声明这是本 step 唯一可在内部完成的自证。

**允许该项存在**，但因属任务层新增，**已在「给评审者的呈现」的「其余要点」中如实告知评审者**。本节点判其不需单列为裁定项——它只增加检验、不改变任何做法，且不做的代价是失去唯一的内部自证。

### 3. 无孤儿（§8）

basic 段 `### T-B` 条目数为 0，两条 detail 任务依赖项均为"无"。**无可判的孤儿。** verify/detail 仍须自行数出该条目数。

### 4. 判据的可执行性已在抽象设计节点验证

六条判据（001 有两条命令）已逐条实跑，落地前全部输出 `0`，处于未通过侧。**其中一条经当场订正**：002 原写 `grep -c '→' template.md` → `≥1`，实跑输出 `3`（既有表格的无关箭头），**改之前就已满足、无法证伪**；改为限定在「断言形态」节内取后为 `0`。

**本节点不重复该验证**，只确认订正后六条仍能证伪。

## 用户原话

所有者答复，逐字：

> 批准

### 依 `rules.md` §11 的对应关系记录

- **构成本结论的答复**：上引「批准」二字。
- **是否经追问补齐**：**否**。本次投影「需要你裁定」写的是"无"，无并列裁定项，故该答复本身即构成完整结论。
- **本 spec 三次评审的对照**：R1 有裁定项、一次答全；R2 有裁定项、需追问；R3 无裁定项、一次答全。**三次合起来说明：需不需要追问取决于投影里有没有并列裁定项，以及当次答复是否覆盖它**——不取决于评审者是谁，也不取决于此前几次的表现。

## 结论

**approved。**

### 本次批准锁定的内容

- 两条任务（T-D1 新增「断言形态」一节、T-D2 一致性自查）的划分与内容。
- basic 段为空的判定。
- 无孤儿的判定。
- T-D2 中的自证项（取 `design.md` 四条命令按新形态改写，写不下来则形态不成立）。

### 本次批准未覆盖的内容

R1、R2 已确认的缺口继续成立，本次不重复批准也不因批准而消解：拦不住「输出对但推导错」、判定者是人判人写的东西、拦得住不等于用得上、只做数字这一块。

**approved 是本 step 的实现授权。** impl/basic（空段，零调用）与 impl/detail 自此解锁。
