# verify/basic.md — claim-command-evidence / step1

> 本节点的前置闸门、结论取值与失败去向见 `flow.md`「verify/basic」节；判据与拒绝条件见 `rules.md` §7。以上各处只指向位置，本文件不复述其文字。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话，分支 `main`。
- **与 `impl/basic.md`「执行身份」比对：相同——`rules.md` §7 的身份独立在本段不成立。** 同一次会话产出两份工件。
- **不掩饰**：与前三个实例的 basic 段同型；修复对象是派发层而非控制面条款（`hard-enforcement-list.md` 第 6 项）。

## 断言与实际结果

`rules.md` §7 规定 basic 段只判三项。

| 判据 | 断言对象 | 实际结果 |
| --- | --- | --- |
| 结构存在 | **无断言对象** | 零落地调用，`### T-B` 条目数为 0（命令 V1） |
| 可构建 | **无断言对象** | 本 step 全部改动为 Markdown 条款，无可构建产物 |
| 既有测试无回归 | 有对象但无因果关系 | 命令 V2、V3。本段未改动任何代码，该结果与本段无因果关系 |

**三项中两项无断言对象**——与 `workspace-cleanup` 同型（纯条款类 step 下 §7 的 basic 三项退化更彻底）。

## 命令证据

**命令 V1** — basic 段条目数：

`grep -c '^### T-B' .themis/workspace/spec/claim-command-evidence/step1/task/basic.md` → `0`

**命令 V2** — 既有测试通过数：

`go test ./... | grep -c '^ok'` → `10`

**命令 V3** — 既有测试失败数：

`go test ./... | grep -c '^FAIL'` → `0`

**三条均按新落地的断言形态记法书写**，且均为两段式——输出即断言值。

## 结论

**不适用。**

依 `flow.md`「verify/basic」节，该取值仅在 basic 段为空时成立。本段正是此情形（命令 V1，本节点自行数出）。

三项后果对照：不解锁 impl/detail（其空段前置另有取值 R3 approved）；不触发失败去向、不构成 fail-closed 停靠；已附本节点自行做出的空段代码层断言。

**本结论不表示"验证通过"。**

## 说明

**本 step 是 `不适用` 取值的第三个使用者**，与前两次一样无需任何裁定即可给出合法结论。

**一处本 step 独有的观察**：本节点的三条命令证据是**控制面新增「断言形态」一节之后，第一批按该形态书写的断言**。形态在此得到一次真实使用——但它是执行者自愿遵守的结果，**不构成对该形态的验证**：验证要等 CLI 存在后才可能。
