# acceptance.md — 2026-08-28-spec-id-date-prefix / step1

> 本节点的前置闸门、阻断核查与结论取值见 `flow.md`「人工验收」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 交付视图

九个 spec 实例目录现按创建日期排序：`2026-08-19-core-removal` 起，到 `2026-08-28-spec-id-date-prefix` 止。**这正是加前缀的目的**——此前按字母序排，与实际发生顺序无关。

`template.md` 规定了格式与日期取法；引用同步更新；两处例外保持原文并各加脉络注。

## 阻断核查

| 核查项 | 结论 | 依据 |
| --- | --- | --- |
| verify/detail 是否 passed | **不阻断** | `step1/verify/detail.md`「结论」判 passed |
| 五条判据是否逐条有断言 | **不阻断** | 同上「断言与实际结果」表，001–005 逐条列出实跑值 |
| 孤儿阻断（§8） | **不阻断** | basic 段为空 |
| 追溯链环 4（§12） | **不阻断** | 本文件阻断核查各项均引用 `verify/detail.md` 的结论 |

四项均不阻断。

## 用户原话

**所有者答复：「接下来由你自己判断分析 直到完成这个spec。完成后优化 spec-id 的格式，每个 spec-id 需要在前面加上 yyy-MM-dd」。**

**依 §11 记录对应关系，本条须逐字说明其性质**：

该答复是**本需求的来源指示**，同时**授权执行者自行判断至完成**。**它发出于本 step 存在之前**，因而不可能是对本 step 方案内容的表态。

**本节点因此取 `已授权代行`**，不取 `accepted`——与 `2026-08-28-gate-value-integrity/step1` 同处置。

**须记明的是**：本需求执行期间所有者另有三次逐字表态，**均已用在各自的闸门上**：

- 「改名」→ R1（`intent-review.md`）
- 「改路径断言，两处例外不改」→ 追问第 2 轮（`QA.md`）
- 「批准」三次 → R1 第 2 轮性质的确认、R2、R3

**但无一次是对本 step 验收内容的表态**。执行者不把其中任何一次挪用至此——**挪用即推断补足**。

## 结论

**已授权代行。**

**本节点未获内容层面的批准**，下游（摘要节点）不得开始。

**执行者的自判**：四项阻断核查均不阻断，`verify/detail.md` 判 passed，前置闸门满足，**交付在技术上可接受**。

**须所有者就以下两项表态后，本结论方可改写**：

1. **落地中那处偏差是否可接受**——批量替换改到了描述替换本身的文字，五处订正回原文；**且实际执行用了脚本加例外清单，而非设计所写的逐处人看**。
2. **是否批准本 step 通过验收**。

**本次未覆盖的五项**（无论此后取何结论均成立）：

1. **"改到了不该改的地方"无判据覆盖**——只能证明旧 id 已清零，**证不出没有误改**。五处已发现并订正，同类可能仍有未发现的。
2. **实际执行与设计有出入**——设计写"第二轮逐处确认"，实际用脚本加例外清单。
3. **"验证旧字符串已消失"这类判据天然自指**——本 step 内发生两次，排除清单只能事后逐个补。
4. **git 历史里的旧路径不追溯**——但 `git log --follow` 可跨改名追踪，本节点已抽验：`2026-08-19-core-removal/Intent.md` 可回溯到改名前的 `9` 条提交。
5. **身份独立不成立**（第十次）。
