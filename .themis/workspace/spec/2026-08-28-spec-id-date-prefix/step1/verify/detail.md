# verify/detail.md — 2026-08-28-spec-id-date-prefix / step1

> 本文件的断言范围、身份独立与拒绝条件见 `rules.md` §7，孤儿判定见 §8，追溯链环 2/3 检出见 §12。以上只指向位置，本文件不复述其文字。
>
> **本文件里的每个数字与存在性断言均由本节点当场跑命令得出。**

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程会话，分支 `main`。
- **与 `impl/detail.md`「执行身份」比对：相同——身份独立不成立**（第十次）。
- **但本 step 三道闸门均为正式 approved**，方案经所有者内容审阅。

## 断言与实际结果

| 条目 | 任务 | 判据与实跑 | 结论 |
| --- | --- | --- | --- |
| 001 | T-D1 | `grep -c 'spec-id' .themis/spec/template.md` → `3`（判据 `≥3`） | **满足** |
| 002 | T-D2 | `ls -1 .themis/workspace/spec/` 中不以 `2026-` 开头者 → `0` | **满足** |
| 003 | T-D3 | 三个目录内旧路径形态 → `0` 份文件命中 | **满足**（取法收窄，见下） |
| 004 | T-D3 | 示例原文那一行仍在 → `1` | **满足** |
| 005 `[横切]` | 无 | 控制面四份 `themis verify` 各 `0` 条；`go test ./...` 的 `FAIL` 数 → `0` | **满足** |

### 判据 003 的取法收窄，本节点补跑全树

`specify.md` 已标明该判据只扫三个目录、不覆盖全树，**并明写"verify 须另跑排除本文件的全树扫描"**。

**本节点已补跑**：`grep -rl 'spec/core-removal' .themis/ docs/ .claude/ | grep -vE 'spec-id-date-prefix/step1/(specify|verify/detail).md' | wc -l` → `0`。

**全树无残留**（除写着该搜索串的两份本 step 工件外）。

**自指问题在本节点又发生一次，如实记明**：初稿的排除清单只写了 `specify.md`，实跑得 `1`——**命中的是本文件自己**，因为补跑命令连同其搜索串一并写在这里。

**这已是同一形态的第二次**（`specify.md` 判据 003 是第一次）。**根因是"验证旧字符串已消失"这类判据天然自指**：判据本身必须写出那个字符串，于是它自己成为一处命中。**排除清单只能事后逐个补，无法一次写全**——每加一份记录该判据的工件，就多一处需要排除。

**本项目第 `30` 次同型实发**，且**与前 29 次不同**：前面各次是命令写错或够不到目标，**这一次命令是对的，是判据的性质决定它无法自证**。

### R2 要求补跑的其余七个 id

`specify.md` 的判据 003 只查 `core-removal` 一个，R2 批准时确认"处置为验证节点补跑其余七个"。**本节点逐个跑路径形态**：

`workspace-cleanup`、`authorization-traceability`、`claim-command-evidence`、`trace-number-scan`、`question-eligibility`、`citation-overlap-check`、`gate-value-integrity` —— **七个各 `0` 份文件命中。**

### 两处例外的核查

- **`SKILL.md` 示例原文**：`不合格：core-removal 的 verify/detail 零漂移` 仍在，**一字未改**（判据 004 即此）。
- **历史计划**：`grep -c 'spec-id 固定为 \`core-removal\`' docs/superpowers/plans/2026-08-19-spec-flow-end-to-end-replay.md` → `1`，**原文保留**。
- **两处均已加脉络注**，各写明该实例现名及不改原文的理由。

## 命令证据

- `grep -c 'spec-id' .themis/spec/template.md` → `3`
- 实例目录中不以 `2026-` 开头者 → `0`
- 旧路径形态在三个目录内 → `0`；**全树补跑（排除本判据所在文件）→ `0`**
- 其余七个 id 的路径形态 → 各 `0`
- `grep -c '不合格：core-removal 的 verify/detail 零漂移' .themis/skills/spec-review-presentation/SKILL.md` → `1`
- 控制面四份 `themis verify` 报告数：各 `0`（人工实跑）
- `themis overlap .themis/spec` → 未发现重合片段（人工实跑）

## 落地中那处偏差，本节点的判定

`impl/detail.md` 报出：**批量替换改到了描述替换本身的文字**，五处逐一订正回原文。

**本节点判定该处置成立**，理由：那五处的共同点是**它们叙述的是"该实例曾叫什么"或"判据要查什么"，不是在引用该实例**。前者改了会使记录所陈述的事实变化——**正是 R1 与追问第 2 轮两次划定的边界所禁止的**。

**但另一处须如实报出为不足**：`impl/detail.md` 自承"实际执行时用了脚本 + 例外清单，而非设计所写的逐处人看"。**例外清单挡住了两处已知例外，挡不住"叙述曾叫什么"这一类**——那五处是事后发现并订正的，不是事前拦住的。

**这意味着可能还有未被发现的同类误改。** 本节点已跑全树扫描确认无旧 id 残留，**但"改对了没有"与"改到了不该改的地方"是两件事，后者无判据覆盖**。

## 追溯链检出（§12 环 2、环 3）

- **环 2**：上游排除 `[横切]` 后为 `001 002 003 004`，下游展开后含全部四条，**差集为空 → 通**。
- **环 3**：上游 `T-D1 T-D2 T-D3`，下游为本文件任务栏，三者均出现，**差集为空 → 通**。

## 结论

**passed。**

五条判据全部满足，003 已补跑全树、R2 要求的其余七个 id 亦逐个跑过。追溯链两环均通。

**五项如实带出，不掩饰**：

1. **"改到了不该改的地方"无判据覆盖**——本节点只能证明旧 id 已清零，**证不出没有误改**。五处已发现并订正，**同类可能仍有未发现的**。
2. **实际执行与设计有出入**——设计写"第二轮逐处确认"，实际用脚本加例外清单。
3. **"验证旧字符串已消失"这类判据天然自指**——判据必须写出那个字符串，于是自己成为一处命中。**本 step 内发生两次**（`specify.md` 判据 003、本文件的补跑命令），每次都只能事后往排除清单里补。**每多一份记录该判据的工件，就多一处需要排除**，无法一次写全。
4. **git 历史里的旧路径不追溯**，R1 已确认。
5. **身份独立不成立**（第十次）。
