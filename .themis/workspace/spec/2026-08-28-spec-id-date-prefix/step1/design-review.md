# design-review.md — 2026-08-28-spec-id-date-prefix / step1

> 本文件是 R2 抽象设计评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R2 抽象设计评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：给实例目录名加日期前缀，八个既有的一并改名，引用同步更新。

**待裁定项**：批准与否。

**一处弱点主动标出**：判据 003 只查 `core-removal` 一个 id，不足以证明八个全部更新；处置是验证节点补跑其余七个，判据不改写（改写会膨胀成八条同形判据）。

**未覆盖**：git 历史里的旧路径不追溯。

## 判据与边界的交叉核查（`flow.md` R2 节）

| 本 step 已确认的边界 | 判据是否冲突 | 处置 |
| --- | --- | --- |
| 「不改证据去迎合判据」 | **触及但不冲突** | R1 与追问第 2 轮已两次划定边界：不保护标识符字面形态，但保护记录所陈述的事实 |
| `themis` 不在只读白名单 | **冲突**——判据 005 要跑 `themis verify` | **照旧人工判** |
| 白名单只放行只读命令 | **不冲突** | 无需处置 |

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影只有一个待裁定项，「批准」一条即完整结论。**判据 003 的弱点属主动披露**，投影明写"若你觉得这个弱点不能接受，判据 003 就得改成覆盖八个 id 的形态"——**所有者未要求改写，故该弱点及其处置（verify 补跑其余七个）获知情确认**。

## 结论

**approved。**

五条行为条目获授权，进入详细设计与任务分解。

**本次批准确认的一项**：判据 003 的弱点可接受，**处置为验证节点补跑其余七个 id**，判据本身不改写。

**本次批准未覆盖**：git 历史里的旧路径不追溯；改名过程中若发现某处引用无法机械替换，须单独提出（R1 已写明）。
