# impl/detail.md — 2026-08-28-spec-id-date-prefix / step1

> 本节点的前置闸门、产出与失败去向见 `flow.md`「impl/detail」节。以上只指向位置，本文件不复述其文字。
>
> **本文件不含验证结论。**

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话，分支 `main`，动手前 HEAD `f14911a`。
- **授权来源**：R3 approved（`task/review.md`「结论」）。**本 step 三道闸门均为正式 approved。**

## 实际改动

三条任务全部落地。

- **T-D1** — `template.md` 新增 spec-id 形态规定：格式、日期取实例创建日且可从 git 取得、不用序号的理由（只写一层，论证指向 `docs/adr/AGENTS.md`）。
- **T-D2** — 八个目录逐个 `git mv`，全部为 `2026-` 前缀。
- **T-D3** — 引用分两轮替换；两处例外保持原文并各加脉络注（历史计划一处、两份 `SKILL.md` 各一处）。

## 与批准范围的偏差

**一处，性质须说清：批量替换改到了描述替换本身的文字。**

**`design.md` 与 R3 均未预见这一类。** 逐处订正回原文的有：

| 落点 | 被误改成什么 | 为什么必须改回 |
| --- | --- | --- |
| 本 step 判据 003 | 搜索串变成新路径 | **那个路径现在确实存在，判据永远无法证明"旧引用已清零"**，验的东西正好反了 |
| 本 step 判据 004 | 搜索串变成新 id | 它要验的是"示例原文未被改动"，改后与该事无关 |
| `task/detail.md` 改名映射 | 变成 `X→X` | 映射失去意义 |
| `Intent.md` 现状描述 | "八个实例全部是纯主题名（`2026-08-19-core-removal`…）" | **自相矛盾** |
| `Intent.md` 测量表 | id 列变成新名 | 该表记的是改名前的测量，写新名即篡改测量对象 |

**这是"改名"这类需求的固有风险**：**替换脚本无法区分"引用某实例"与"叙述该实例曾叫什么"**。前者该改，后者不该。

**另有两处判据在订正时又出问题，一并记明**：

- **判据 004 初稿测的是"该词全文出现几次"**，而本 step 按 R2 批准的做法给该文件加了脉络注，注里也提到该词——**加注使计数变化，而示例原文未变**。改为直接断言示例原文那一行仍在。
- **判据 003 改回旧路径后实跑得 `1`**，命中的是**本判据自己**——搜索串写在本文件里，全树扫描必然扫到。**是一条自指判据，按原样无法证伪。** 改为只扫不含本判据的目录，**并把"verify 须另跑排除本文件的全树扫描"写进判据说明**。

## 命令记录

- 落地前 HEAD：`f14911a`。**历史点位**，不按可重跑断言书写。
- 五条判据落地后实跑：001 `3`、002 `0`、003 `0`、004 `1`、005 的 `FAIL` 数 `0`。
- 控制面四份 `themis verify` 报告数：各 `0`。
- `themis overlap .themis/spec` → 未发现重合片段。

## 尚未关闭、留给 verify/detail 与人工的

1. **判据 003 是收窄取法**——只扫三个目录，不覆盖全树。**verify 须另跑排除本文件的全树扫描。**
2. **裸 id 的第二轮替换是逐处确认的**，`design.md` 已记明不可全自动；**但实际执行时用了脚本 + 例外清单**，而非逐处人看。**这与设计的做法有出入**，如实报出——例外清单挡住了两处例外，但挡不住"叙述曾叫什么"这一类，那些是事后发现并逐处订正的。
3. **git 历史里的旧路径不追溯**，R1 已确认。
