# state.md — 2026-08-28-spec-id-date-prefix

> 各闸门行内格式：`- <节点名>：<结论> — <证据路径>`。结论取值由 `flow.md` 通用状态条款按节点类型固定，本文件不自建约定。

## 当前节点

**step1 的人工验收取 `已授权代行`，停在本节点。** 五条判据全满足、verify/detail 判 passed、四项阻断核查均不阻断，**但所有者未就本 step 验收内容表态**。

**摘要节点不得开始。** 本需求执行期间所有者有多次逐字表态，**均已用在各自的闸门上**（改名→R1、两处例外→追问第 2 轮、三次批准→R1 第 2 轮/R2/R3），**无一次是对验收内容的表态**——执行者不挪用。

## 各闸门

### spec 级

- Intake：已走完 — `Intent.md`（**本实例自身即新格式首用**；当场统计跨实例引用 `133` 处，八个实例的 git 创建日期均可取得）
- 追问：已走完 — `QA.md` 两轮（第 1 轮问既有是否改名——**执行者未给推荐，如实写明"倾向不改但那是保守、不构成推荐"**；第 2 轮问两处例外与路径断言，**执行者把具体后果摆出后才问**，未直接执行"一起更改"）
- R1 意图评审：approved — `intent-review.md`（所有者原话"改名"，推翻执行者倾向。**第二次划定「不改证据」边界**：保护记录的内容真实性，不保护标识符的字面形态）

### step 级

### step1 — 格式规定与八个改名（人工验收停在本节点）

- step 定界：已走完 — `step1/scope.md`（**暴露两处 R1 未预见的情形**并交 R2：两处引用改了会篡改记录内容；改名后路径断言会失效）
- 抽象设计：已走完 — `step1/specify.md`（五条条目；**主动标出判据 003 只查一个 id 的弱点**，处置为 verify 补跑其余七个）
- R2 抽象设计评审：approved — `step1/design-review.md`（所有者原话"批准"；确认该弱点可接受）
- 详细设计+任务：已走完 — `step1/design.md` 与两份任务文件（四条结构决策；`git mv` 保历史、分两轮替换、加注不改原文、日期取 git 首次提交日）
- R3 详细方案评审：approved — `step1/task/review.md`（所有者原话"批准"）
- impl/basic、verify/basic：不适用 — basic 段为空
- impl/detail：已走完 — `step1/impl/detail.md`（三条任务落地；**一处偏差**——批量替换改到了描述替换本身的文字，五处订正回原文，根因是脚本无法区分"引用某实例"与"叙述该实例曾叫什么"）
- verify/detail：passed — `step1/verify/detail.md`（五条判据全满足；全树补跑与其余七个 id 均为 `0`。**发现一类此前未见的失效**——"验证旧字符串已消失"这类判据天然自指，本 step 内发生两次）
- 人工验收：**已授权代行** — `step1/acceptance.md`（所有者的指示发出于本 step 存在之前，不可能是对其方案内容的表态；执行期间的多次逐字表态均已用在各自闸门上，**执行者不挪用**）
- 摘要：**未开始** — 前置闸门未满足

## 当前性

- `Intent.md`、`QA.md`、`intent-review.md`：current
- `step1` 各工件：current（验收结论待所有者表态后改写）
