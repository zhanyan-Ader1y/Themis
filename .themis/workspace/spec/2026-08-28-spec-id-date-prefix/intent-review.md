# intent-review.md — 2026-08-28-spec-id-date-prefix

> 本文件是 R1 意图评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R1 意图评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：spec 实例目录名前加创建日期，并把既有八个一并改名。

**追问一问已闭合**：既有八个是否改名——所有者答「改名」，推翻执行者倾向。

**改变原定方向的发现**：实测 `133` 处跨文件引用嵌在已闭合工件正文里；**本次与上一个需求那次不同**——那次改的是合同未定义的无效取值，而 `2026-08-19-core-removal` 这类 id 当时完全合法。

**未覆盖**：改名后 git 历史里的旧路径不追溯（`git log --follow` 可跟踪）。

## 未解决反馈

无。

## 用户原话

**所有者答复：「改名」。**

**依 §11 记录对应关系**：该答复对应 `QA.md` 第 1 轮问 1，取选项 (b)——既有八个一并改名。

**该答复同时构成对需求本身的批准**：改名以需求成立为前提。**本节点如实标明这是一处推断**，但其强度高于同类——**所有者的裁定扩大了范围（把既有八个纳入），而扩大范围的指示不可能来自否决需求本身**。

**执行者的倾向被推翻，如实记明**：`QA.md` 写明"执行者倾向 (a)，但不构成推荐"，所有者裁定取 (b)。

## 结论

**approved。**

确认需求本身，以及 step 分解为一个 step。

**本次批准同时确认的一项裁定**：既有八个实例一并改名，`133` 处引用同步更新。

**该裁定第二次划定「不改证据去迎合判据」的边界**：

- **上一次**（`2026-08-28-gate-value-integrity`）：该原则不保护**合同未定义的无效状态**。
- **本次**：该原则保护的是**记录的内容真实性**，**不保护标识符的字面形态**。改 id 与更新指向它的引用，不改变任何记录所陈述的事实。

**效力边界**：本裁定只覆盖"标识符改名及随之而来的引用更新"。**不构成"已闭合工件的正文可因后续约定变化而批量改写"的一般性授权**——凡改动会使记录所陈述的事实发生变化的，仍属该原则禁止。

**本次批准未覆盖**：git 历史里的旧路径不追溯改写；改名过程中若发现某处引用无法机械替换（例如出现在代码或测试夹具里），须单独提出。
