# intent-review.md — core-removal

> 本文件是 R1 意图评审的实例工件。收敛判据、判定者与投影低负担要求见 `rules.md` §2、§3；本节点前置闸门与产出见 `flow.md` "R1 意图评审"节。以下"投影"是 Agent 提出的收敛主张与呈现，不构成收敛结论（判定者见 `rules.md` §2）。

## 投影

**收敛主张**：`QA.md` 第 1、2 轮问题均已获所有者答复，`Intent.md` 的问题 / 期望结果 / 核心链路三节均已据答复更新，当前无未答问题。据此提出收敛主张，请 R1 评审者判定是否成立。

**为什么要删（Why）**：`templates/.themis/core/` 是 simple/full 双路径实现，与已批准的单一路径契约冲突（来源见 `Intent.md` 问题节与来源引用小节）。所有者已在追问第 2 轮确认整体删除的目标本身，并将触发条件收窄为"核实当前项目内部无外部依赖"（`QA.md` 第 2 轮、第 1 轮问 1）。

**删完什么样（期望结果）**：`templates/.themis/core/`（现 98 个文件）整体从代码库消失。清点到的六处活跃引用中，所有者已就其中两处表过态——`CLAUDE.themis.md`、`README.md` 涉及 core 的内容按确认删节，留待 Themis 更完善后再编写（`QA.md` 第 1 轮问 2）；`templates/.themis/spec/README.md` 沿用既有裁定，保留与只读控制面 `.themis/spec/README.md` 之间的已知分歧，不在本次同步安装（`QA.md` 第 1 轮问 3）。另外三处引用所有者尚未表态，作为确认请求列在"未解决反馈"。`docs/` 下的历史引用已确认排除在本次范围之外（`QA.md` 第 1 轮问 4）。

**经过哪些必要动作（核心链路）**：厘清六处引用各自的处理方式 → 删除 `core/` → 核验无残留引用、无失效的忽略规则。删除动作本身仅在 R3 获批后于 impl 节点执行；本节点批准只解锁抽象设计，不解锁删除。

## 未解决反馈

R1 评审共提出三处确认请求，所有者已在批复中逐条表态（原话见"结论"）：

1. **`.gitignore` 中 `/.themis/core/` 忽略规则是否列入本次处理范围**——**已获表态，关闭**：所有者确认列入本次范围。此前它只是清点六处引用时一并发现、所有者原始要求未提及的追加项，现已由所有者确认闭合（`Intent.md` 来源引用小节已补一条 `用户确认#` 记录）。
2. **`templates/.themis/AGENTS.md` "与 `core/` 的关系"一节（该文件行 47–51）如何处理**——**已获表态，关闭**：所有者确认删除（`Intent.md` 期望结果节已同步更新）。
3. **`templates/.themis/workspace/context/catalog.md:33` 的相对路径引用**（`../../core/protocols/context/references/catalog.md`，删除前目标存在）**处理方式**——**仍未解决**。所有者未给出处理方案本身，而是明确要求延后：待前两条落地后，另行提出处理方案，由所有者单独审阅。本项作为开放决策点带入后续节点（抽象设计需知道它悬而未决），不在本文件内定下做法，不得记为已解决。

## 结论

**approved**

所有者原话（逐字，未转述、未润色）：

> approved。1. .gitignore列入本次范围。2. 删除。3. 完成以上任务后再描述第3点的处理方案由我审阅

---

## 结论追加：step 分解确认（2026-08-26）

**approved。**

本节记录 R1 评审者对 `Intent.md`「step 分解」小节的确认。该小节为 2026-08-24 新增——控制面此前只有 spec 级一层意图，`flow.md`「R1 意图评审」节现规定本节点同时确认需求本身与它的 step 分解，故已有实例需补此确认。

**呈现给评审者的分解**（三项，逐项）：

- **step1** — 删除 `core/` 目录本体与七处活跃引用。已完成，2026-08-22 accepted。
- **step2** — 清除 `templates/.themis/workspace/README.md:5` 一句以 Core 为主语、陈述 Core 与 Workspace 读写关系的话。未开始。**本次新增的即是此项。**
- **不作为 step 存在** — `templates/.themis/workspace/context/catalog.md:33` 的相对路径引用：评审者自 R1 起点名延后，"延后"是明确决定而非待办，不由本需求的完成判定牵制。

**所有者原话**（逐字，未转述、未润色）：

> 确认

**本次确认的效力边界**：

- 确认的是**分解方式**——拆成哪几个 step、各自承担什么、哪一项不作为 step。**不是**对 step2 具体做法的批准；step2 的做法须走它自己的 R2、R3。
- 本次修订按 `flow.md`「R1 意图评审」节的失效级联例外**不使已 accepted 的 step1 失效**：仅新增分解项，step1 的目标、期望结果、核心链路、范围与非做一字未改。上方 2026-08-18 的 approved 结论及其原话同样不受影响，仍然成立。
- 确认后 step2 解锁 step 定界节点。

---

## 结论再追加：step2 撤销（2026-08-26）

**上方 2026-08-26 的 step 分解确认，其中 step2 一项已由所有者撤销。** 上方内容为当时真实发生的确认，逐字保留、不改写。

**撤销经过**：step2 进入 step 定界节点后，执行者核实目标时发现同一文件（`templates/.themis/workspace/README.md`，31 行）内的 Plan 35 残留不止第 5 行——第 9–13 行为 Plan 35 词汇、第 31 行直接以已退役的 Plan 35 为主语；整个 `templates/.themis/workspace/` 包有 39 个文件、11 个子目录，全套 lifecycle-first 结构。执行者按 `flow.md`「step 定界」节"不得引入分解项之外的目标"停下，未擅自扩大范围，就范围取值向所有者提问。

**所有者原话**（逐字，未转述、未润色）：

> 不需要对齐templates/.themis中的结构与规范

> 撤销 step2

**该答复推翻了 step2 的前提**：step2 把那句话当作"待清理的残留"，前提是 `templates/.themis/` 应当与当前 spec 结构对齐；所有者明确不需要对齐，那句话因此不是缺陷，而是一个独立遗留包内自洽的历史记述。

**撤销的效力边界**：

- 撤销的是**分解项**，本需求范围随之收窄为"删除 `core/` 本体与其活跃引用"。
- **不影响 step1**：step1 的目标与批准前提一字未改，其 accepted 结论、上方 2026-08-18 的 R1 approved 结论及原话均继续成立。
- 所有者在 step1 人工验收中"验收后作为新 step 处理"的裁定，由本次答复取代；`step1/acceptance.md` 与 `step1/summary.md` 中对该裁定的记述是**当时的事实**，保留不改。
- 撤销后本 spec 实例只余 step1，已 accepted，**需求达成、流程闭合**。
