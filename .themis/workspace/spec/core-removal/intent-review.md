# intent-review.md — core-removal

> 本文件是 R1 意图评审的实例工件。收敛判据、判定者与投影低负担要求见 `rules.md` §2、§3；本节点前置闸门与产出见 `flow.md` "R1 意图评审"节。以下"投影"是 Agent 提出的收敛主张与呈现，不构成收敛结论（判定者见 `rules.md` §2）。

## 投影

**收敛主张**：`QA.md` 第 1、2 轮问题均已获所有者答复，`Intent.md` 的问题 / 期望结果 / 核心链路三节均已据答复更新，当前无未答问题。据此提出收敛主张，请 R1 评审者判定是否成立。

**为什么要删（Why）**：`templates/.themis/core/` 是 simple/full 双路径实现，与已批准的单一路径契约冲突（来源见 `Intent.md` 问题节与来源引用小节）。所有者已在追问第 2 轮确认整体删除的目标本身，并将触发条件收窄为"核实当前项目内部无外部依赖"（`QA.md` 第 2 轮、第 1 轮问 1）。

**删完什么样（期望结果）**：`templates/.themis/core/`（现 98 个文件）整体从代码库消失。清点到的六处活跃引用中，所有者已就其中两处表过态——`CLAUDE.themis.md`、`README.md` 涉及 core 的内容按确认删节，留待 Themis 更完善后再编写（`QA.md` 第 1 轮问 2）；`templates/.themis/spec/README.md` 沿用既有裁定，保留与只读控制面 `.themis/spec/README.md` 之间的已知分歧，不在本次同步安装（`QA.md` 第 1 轮问 3）。另外三处引用所有者尚未表态，作为确认请求列在"未解决反馈"。`docs/` 下的历史引用已确认排除在本次范围之外（`QA.md` 第 1 轮问 4）。

**经过哪些必要动作（核心链路）**：厘清六处引用各自的处理方式 → 删除 `core/` → 核验无残留引用、无失效的忽略规则。删除动作本身仅在 R3 获批后于 impl 节点执行；本节点批准只解锁抽象设计，不解锁删除。

## 未解决反馈

R1 评审共提出三处确认请求，所有者已在批复中逐条表态（原话见"结论"）：

1. **`.gitignore` 中 `/.themis/core/` 忽略规则是否列入本次处理范围**——**已获表态，关闭**：所有者确认列入本次范围。此前它只是清点六处引用时一并发现、所有者原始要求未提及的追加项，现已由所有者确认闭合（`Intent.md` 来源引用小节已补一条 `用户确认#` 记录）。
2. **`templates/.themis/AGENTS.md` "与 `core/` 的关系"一节（该文件行 47–51）如何处理**——**已获表态，关闭**：所有者确认删除（`Intent.md` 期望结果节已同步更新）。
3. **`templates/.themis/workspace/context/catalog.md:33` 的相对路径引用**（`../../core/protocols/context/references/catalog.md`，删除前目标存在）**处理方式**——**仍未解决**。所有者未给出处理方案本身，而是明确要求延后：待前两条落地后，另行提出处理方案，由所有者单独审阅。本项作为开放决策点带入后续节点（抽象设计需知道它悬而未决），不在本文件内定下做法，不得记为已解决。

## 结论

**approved**

所有者原话（逐字，未转述、未润色）：

> approved。1. .gitignore列入本次范围。2. 删除。3. 完成以上任务后再描述第3点的处理方案由我审阅
