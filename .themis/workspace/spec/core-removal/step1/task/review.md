# task/review.md — core-removal / step1

> 本文件是 R3 详细方案评审的实例工件。本节点的前置闸门、产出、以及本次评审对两份任务文件的覆盖方式见 `flow.md`"R3 详细方案评审"节；分类核查的判据与判定者见 `rules.md` §4；任务是否越界的判定见 `rules.md` §6；实现依据的事实源判定在本节点发生，条款见 `rules.md` §1；投影的低负担要求见 `rules.md` §3。以上各处只指向位置，本文件不复述其文字。
>
> 以下「评审范围」「分类核查」是任务作者提出的投影与自查记录，不构成评审结论；结论由 R3 评审者给出（判定者条款见 `rules.md` §4）。

## 评审范围

本次评审一次呈现三份工件：

1. **`step1/design.md`**——本轮（任务 4）修订后的状态。修订分三类，**逐条内容与命令核验记在该文件内，此处只给结论**：（a）**按所有者 R2 批复而做的三项**——删节口径由窄读改为宽读、仓库根 `AGENTS.md:13` 由"范围外不处理"改为纳入并给出处理做法、订正两处事实错误；（b）**本轮自查重跑命令时发现的六处数字错误**——四处章节行号区间（其中一处把两个独立章节合并记为一节，致该节此前实际未被逐节判定，本轮补判为"不改"）与两处列表条数；**外加一处引用点计数与路径归属错误**（属归属类，不计入上述六处，与「分类核查」第 7 条同一口径）；（c）**一处权威归属订正**——三处曾把本节点的推广标为所有者"明确点名"，已改为事实表述（见下「分类核查」第 8 条）。本轮另按同一口径新增了三处此前被保留、但同源理由要求删除的内容（`README.md:93`、`:109` 与 `CLAUDE.themis.md:96`）。「结构决策」第 5、6 条本轮未改动。
2. **`step1/task/basic.md`**——本段为空，无 `### T-B<n>` 条目；空段的判定过程、所采用的"外部可观测"口径及其边界写在该文件内。
3. **`step1/task/detail.md`**——六个任务 T-D1–T-D6，逐条给出行为目标、对应的 `specify.md` 条目与依赖的基础任务。

`design.md` 的修订与两份任务文件同属本节点的产出，一并进入本次判定，不是背景材料。

## 分类核查

以下是任务作者对自己提出的分类所做的自查，供 R3 判定时对照：

1. **basic 段为空是判出来的**：`task/basic.md` 先厘清候选清单（`design.md`「结构决策」七条中第 5、6 条为"不动"的决定不进候选，其余五条加删除动作本身共六项），再对六项逐项核对 `rules.md` §4 的两条判据，两条都不成立，故无任务归入 basic。判定依据写在该文件「基础任务」小节内，即 §4 为其指定的落点。
2. **每个 basic 任务是否写明判定依据与被谁依赖**：本段无条目，本项空集满足。空段本身的依据已写入 `task/basic.md`，不以"没有条目"代替说明。
3. **是否存在单独产生外部可观测结果的 basic 任务**：本段无条目，空集满足。
4. **是否存在无消费者的 basic 任务**（孤儿，条款见 `rules.md` §8）：本段无条目，空集满足；`task/detail.md` 六个任务的"依赖的基础任务"一项均为"无"，与空段一致，两份文件之间无悬空引用。
5. **任务是否引入 `design.md` 未定的结构**（判定条款见 `rules.md` §6）：T-D1–T-D5 逐条指向 `design.md`「结构决策」第 1、2、3、4、7 条，T-D6 指向「架构与边界」锚定的删除动作；行为目标只重述已定改动的范围与行号，未出现 `design.md` 里没写过的**结构**改法。`design.md`「结构决策」第 5、6 条不对应任何任务，因为二者是"不动"的决定，没有可落地的动作——这不是遗漏。**一处需要收窄措辞的自查**：`task/detail.md` T-D3 末句给了一条落地顺序建议（先于 T-D5 执行，以免中途出现指向已删章节的描述），`design.md` 未写过这条顺序。它不是字段、类型、签名、迁移方向或配置项，因此不落在 §6 所约束的结构决策范围内，但它确实是任务文件自行添加、`design.md` 未定的内容，不应被"未出现没写过的改法"一句笼统盖过。若 R3 认为任务文件不得携带任何 `design.md` 未写的内容，删去该句即可，不影响六个任务本身。
6. **trace 覆盖**：`specify.md` 四条行为条目均有对应任务——`SPEC-COREREMOVAL-001` ← T-D1、T-D2、T-D3（三者合取才可能通过）；`SPEC-COREREMOVAL-002` ← T-D4；`SPEC-COREREMOVAL-003` ← T-D6；`SPEC-COREREMOVAL-004` ← T-D5。六个任务也都各自挂到了一条判据上，无既不对应判据、又不对应结构决策的任务。
7. **实现依据的事实源**（该项判定在本节点发生，条款见 `rules.md` §1）：`design.md`「结构决策」的每一条都锚定在「事实依据」的命令输出上（命令 1–11）。本轮把命令 1–7 逐条重跑并与所载输出逐字比对，一致；命令 8–11 为本轮新增，分别核验章节行号区间、列表条数与 failure budget 残留位置、命令 2 实际覆盖的文件枚举、以及所有者是否点过章节名。所依据的是命令的真实输出，不是上一版工件里写下的数字。**这一条本轮特别核对，因为被订正的错误全部属于同一形态**：数字（无论沿用还是新写）未跑命令核对。本轮订正八处——两处事实错误（命中行数、`docs/core/` 存在性前提）、四处章节行号区间、两处列表条数——外加一处引用点计数与路径归属错误。其中两处列表条数是上一轮宽读改写时**新写**的数字，写下时未核对，且发生在执行者已把这一盲区写进漂移清单之后；这次复发本身已另记入 `docs/plan/spec-replay/drift-log.md`。
8. **提交 R3 判定的两处口径选择**（任务作者做了判断，但判断依据不在控制面内，请评审者确认或纠正）：
   - **"外部可观测"在本需求下指什么**。本需求没有可运行产物，`rules.md` §4 也没有为这类需求界定该词。`task/basic.md` 采用 `specify.md`（已获 R2 批准）的判据形态作为界定来源。若改用"产品运行时行为"这一更紧的读法，分类结论会翻转成"T-D3 归入 basic、T-D5 为其消费者"——两种读法的分岔点与选择理由写在 `task/basic.md` 末段。
   - **宽读逐节判定全部是本节点对 R2 口径的推广**。所有者 R2 给的是口径（宽读），**没有点过任何一个章节名**（四份实例工件全文检索零命中，核验见 `design.md`「事实依据」命令 11）。因此两份文件里每一节的删或留——包括本轮新增的几处部分删除——都是本节点应用该口径做出的判定，无一经所有者逐节确认。上一版 `design.md` 曾把其中三处标为"R2 批复明确点名"，那是无据的权威归属，本轮已订正（见 `design.md`「架构与边界」权威归属一条）。**已知残留清单是非穷尽的**，其构成、边界与本轮改为删除的几处见 `design.md`「取舍」，此处不重复枚举。推广是否得当、残留边界是否可接受，请一并判定。
9. **未出现"为填满两段而凑任务"**：判出空段后没有再构造 basic 任务去填充；六个 detail 任务全部对应 `specify.md` 判据或「事实依据」已核验的删除动作，没有为让 detail 段"看起来完整"而拆出边界模糊的子任务。自查时特意回看了这个动作有没有发生——`task/basic.md` 末段如实记下了唯一可能的凑法（把单个文件的编辑拆成伪阶段）以及不采用的理由。

## 未解决反馈

**一项，待所有者答复：`catalog.md:33` 的开放状态在本节点该如何计入。**

事实：`catalog.md:33` 是所有者在 R1、R2 两轮都主动要求延后的开放决策点——待其余各项落地后另行提出处理方案由所有者单独审阅（原话见 `intent-review.md`、`design-review.md` 结论）。`design.md`「结构决策」第 6 条据此不为它定做法，只记录延后状态本身；`specify.md` 的 `SPEC-COREREMOVAL-001` 判据也已把它作为"明确延后"的已知例外处理，不判 failed。本轮没有改变它的状态。

问题：`flow.md`"R3 详细方案评审"节对本小节设了一条硬性前置（文字见该节，此处不复述）。若把这一开放项计入本小节，本次评审将无法在不违反所有者自己的延后指示的前提下满足那条前置——延后要求其余各项先落地，而落地由本次评审解锁，构成闭环。若不计入，则需要一条依据说明"所有者已批准延后的开放项"与"本轮提出、尚待回应的反馈"是两回事，而 `flow.md` 与 `rules.md` 都没有给出这条依据。

任务作者的判断（不代所有者裁定）：倾向于**不计入**——延后本身已经是所有者给出的处置，性质上是已决而非待决。但这条依据是任务作者自己的解释，控制面里没有对应条款，因此不自行落定，作为本小节唯一一项列出，请所有者在 R3 批复时明确确认或纠正：

- 若确认，本项即告关闭，本小节清空，`catalog.md:33` 继续以「结构决策」第 6 条记录的延后状态存在，其处理方案按所有者原定安排在本次落地之后另行呈交；
- 若纠正（认为应当计入），本次评审停在本节点，退回"详细设计 + 任务"重新决定 `catalog.md:33` 该以何种形态呈现才不与那条前置冲突。

**本项不是在请所有者现在就裁定 `catalog.md:33` 的处理方案**——那一项所有者已明确安排在其余各项落地之后单独审阅，本轮不提前索取。本项只问它在本节点该如何计入。

除此一项外，本节点未产生其他待回应的确认请求；分类核查第 8 条的两处口径选择属于本次提交评审判定的材料本身，不另列为反馈。

## 结论

待所有者答复

（所有者答复后，本节按原话逐字记录结论；未获答复前不填、不代答。）
