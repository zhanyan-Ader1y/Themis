# intent-review.md — citation-overlap-check

> 本文件是 R1 意图评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R1 意图评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：给 CLI 加一项检查——找出"引用另一份文件时把内容抄过来"的地方。

**待裁定项**：批准需求本身与 step 分解（一个 step）。**追问零提问**，四项候选问题全部自行判定，依据写入 `QA.md` 末节。

**改变原定方向的发现**：实测推翻了"降噪很难"这个前提，也推翻了"实发 7 次"这个数字——**现在是 8 次，第 8 次由本会话执行者两个需求前所犯**，且过了四道关无人发现。

**未覆盖**：只查控制面四份文件间的重叠，不查工件；正当重合无法零误报，故标出而不拦截。

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影只有一个待裁定项（批准需求本身与 step 分解），「批准」一条即完整覆盖，无并列裁定项需要追问。

**本轮追问零提问，故无追问阶段的裁定需要在此对应。**

## 结论

**approved。**

确认需求本身，以及 step 分解为一个 step。

**本次批准确认的三项**：

1. **交付的是检测手段，不是修复**——已发现的三处违规不在本需求范围，修它们属控制面改动，另行处理。
2. **只查控制面四份文件之间的重叠**——工件复述控制面是另一类问题，不在本次范围。
3. **标出而不自动拦截**——正当重合无法零误报，与清单第 2 项原文一致。

**本次批准未覆盖**：三处已发现违规的修复；工件层的复述检测。

## 第 2 轮 —— step2 分解项确认（2026-08-28）

### 投影

**这一步要确认什么**：新增的 step2 分解项——修掉工具报出的五处复述。

**为什么新增 step 而非扩大 step1**：扩大会改变一个已 accepted step 的目标语义，触发完整失效级联，其四道闸门全部作废重走；新增落在失效级联的唯一例外内，不使 step1 失效。

**一处推断如实标出**：所有者答「一起修了」是对"要不要修"的答复，**未直接就 step1 批准与否表态**；本节点判定其蕴含接受并已在 `acceptance.md` 标明这是推断。

### 用户原话

**所有者答复：「确认」。**

**依 §11 记录对应关系**：本轮投影只有一个待确认项（新增的 step2 分解项），「确认」一条即完整覆盖。

**该答复同时闭合了上一轮标出的推断**：投影明写"若这个推断不成立，step1 应退回验证节点"，所有者未提出退回而是确认继续，**故 step1 的 accepted 结论获得追认**，不再是单方推断。

### 结论

**approved（第 2 轮，仅就新增分解项）。**

确认 step2 分解项：修掉五处复述，依赖 step1。

**本轮确认的两项**：

1. **新增 step2 的处置方式成立**——不扩大 step1、不触发失效级联。
2. **step1 的 accepted 结论获追认**——此前是本节点的推断，本轮转为所有者确认。

**本轮未覆盖**：五处各自删哪边、留哪边——那属 step2 的抽象设计与详细设计，本轮不预判。
