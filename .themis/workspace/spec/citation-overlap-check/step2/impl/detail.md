# impl/detail.md — citation-overlap-check / step2

> 本节点的前置闸门、产出与失败去向见 `flow.md`「impl/detail」节。以上只指向位置，本文件不复述其文字。
>
> **本文件不含验证结论。**

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话，分支 `main`，动手前 HEAD `40983a8`。
- **授权来源**：**R3 记作授权代行、非 approved**（`task/review.md`「结论」）。所有者答"按你的判断继续"，未就方案内容表态。**本 step 的 R2、R3 两道闸门均为授权代行。**

## 实际改动

三条任务全部落地，改控制面两份文件的五处文字。

- **T-D1**：`flow.md:154` 删「只判三项……无回归。」并把"判定者与断言范围"改为"判据、判定者与断言范围"；`flow.md:176` 删「依据实际实现与证据判定 `passed`/`failed`，」并把"判定见"改为"判据与判定见"。**两处的 `flow.md` 本职内容全部保留。**
- **T-D2**：`flow.md:189` 删「必须由重规划显式处理（复用或删除）后才解除」，改为"解除条件见 `rules.md` §8"；`flow.md:33` 删「（工具错误、验证不过、依赖缺失）」并补一句指向 §10。
- **T-D3**：`rules.md` §12 删去编号前提原文，改为指向 `template.md`「编号引用写法」，**并写明那一节规定"怎么写"、本节规定"据此怎么展开"**（R2 裁定二的补偿处置）。`template.md` 侧不动。

## 与批准范围的偏差

**一处，落地后通读发现并当场订正。**

**T-D2 的第 33 行改动引入了重复指向**：初稿在句中补了"见 `rules.md` §10"，而该行句尾本就有"（`SPEC-FAIL-001`，判定见 `rules.md` §10）"——**同一行两次指向同一节**。

**这不是判据能抓到的**：判据 002b 只查那串并列文字是否消失，查不出新引入的冗余。**是按 `design.md` 结构决策二"每删一处通读所在小节"读出来的。**

**订正**：把补充说明并入句尾那处指向，全行只留一次。

**该偏差佐证了结构决策二的必要性**——它明写"判据 004 只查 `verify/basic` 一节的指向，查不了另外三处的可读性，这一条靠人读"。**本次正是靠人读抓到。**

## 命令记录

- 落地前 HEAD：`40983a8`。**历史点位**，不按可重跑断言书写。
- 六条判据落地后实跑：001a `0`、001b `0`、002a `0`、002b `0`、003 `1`、004 `1`。
- **`themis overlap .themis/spec` → 报出 `0` 处**（人工实跑，`themis` 不在白名单）。
- 全仓 `go test ./...` 的 `FAIL` 数：`0`。
- 控制面四份文件 `themis verify` 报告数：各 `0`。

## 尚未关闭、留给 verify/detail 与人工的

1. **R2、R3 均为授权代行**——方案未经所有者内容审阅。**其中 R2 裁定二（编号前提归 `template.md`）在无决定性依据下作出**，两个方向都讲得通。
2. **四处删除后的可读性靠人读**——判据只覆盖 `verify/basic` 一节的指向，另三处无判据保护。
3. **过滤规则仍是经验规则**——`overlap` 报 `0` 处不等于确实无复述，只等于"无未被规则排除的重合"。
