# acceptance.md — citation-overlap-check / step1

> 本节点的前置闸门、阻断核查与结论取值见 `flow.md`「人工验收」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 交付视图

`themis overlap` 可用：对控制面目录内各 `.md` 两两比对，找出逐字重合的片段，标出供人工复核。

**对真实控制面实跑**：`153` 处原始片段 → 极大延长后 `26` 处 → 四条规则过滤后**报出 `5` 处，零误报**，逐条核实全部为真复述。**其中三处此前从未被任何人发现**。

## 阻断核查

| 核查项 | 结论 | 依据 |
| --- | --- | --- |
| verify/detail 是否 passed | **不阻断** | `step1/verify/detail.md`「结论」判 passed |
| 五条判据是否逐条有断言 | **不阻断** | 同上「断言与实际结果」表，001–005 逐条列出实跑值 |
| 孤儿阻断（§8） | **不阻断** | basic 段为空，无已落地而无消费者的 basic 改动 |
| 追溯链环 4（§12） | **不阻断** | 本文件阻断核查各项均引用 `verify/detail.md` 的结论 |

四项均不阻断。

## 用户原话

**所有者答复：「一起修了」。**

**依 §11 记录对应关系，本条须逐字说明其性质**：

投影提出两项——(一) 五处违规是否仍不修（推荐"仍不修，另开需求"）；(二) 是否批准通过验收。

**「一起修了」是对第一项的答复，且推翻了推荐**：所有者要求修复与检测一并完成。

**该答复未直接就第二项表态**，但**本节点判定它蕴含对 step1 交付的接受**——理由：所有者说的是"一起修"，不是"退回重做"；"一起"以 step1 的交付存在为前提，若 step1 被否决则无所谓"一起"。**本节点如实标明这是一次推断，而非逐字授权。**

**处置**：本节点据此判 accepted，**同时新增 step2 承担修复**，并把新增分解项提交 R1 确认（`flow.md` R1 节失效级联例外要求）。**若所有者认为本推断不成立，本 step 应退回 verify/detail 已证闸门。**

**该推断此后已获追认，如实补记**：所有者在 R1 第 2 轮答「确认」，`intent-review.md`「第 2 轮」记明——投影明写"若这个推断不成立，step1 应退回验证节点"，所有者未提出退回而是确认继续，**故本结论的 `accepted` 不再是单方推断**。

**本条为 `gate-value-integrity` 补入，结论字段未改**：该取值本就在 `flow.md` 取值表内，欠缺的是授权来源，而那已经补上。改结论字段会抹掉"当时是推断"这一事实，加指向则两件事都在。

## 结论

**accepted。**

step1 交付通过验收。`themis overlap` 获授权放行，摘要节点解锁。四项阻断核查均不阻断。

### 裁定：五处违规改为一并修复，新增 step2 承担

**所有者推翻了"仍不修"的推荐。** 修复不再另开需求，而在本需求内新增 step2 完成。

**为什么新增 step 而非扩大 step1**：扩大 step1 会改变一个已 accepted step 的目标语义，**触发完整失效级联**（`flow.md` R1 节），其 R2、R3、验证、验收四道闸门全部作废重走——同一份交付重走四道闸门，无任何收益。新增 step 落在失效级联的唯一例外内，**不使 step1 失效，但须经 R1 确认新增的分解项**。

**`Intent.md`「step 分解」已按此修订**，待 R1 确认。

### 本次批准未覆盖的五项

1. **五处违规本身**——由 step2 承担，本 step 只交付检测手段。
2. **四条过滤规则是经验规则**——报告已列出各自排除处数以使误排除可被察觉，但误排除仍可能发生。
3. **阈值 `20` 字无理论依据**，未做敏感性分析。
4. **本工具无法校验关于它自己输出的断言**——`themis` 不在白名单内；放行等于让白名单自我豁免，**是机制的必然边界**。
5. **身份独立不成立**（第七次）。
