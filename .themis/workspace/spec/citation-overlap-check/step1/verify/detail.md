# verify/detail.md — citation-overlap-check / step1

> 本文件的断言范围、身份独立与拒绝条件见 `rules.md` §7，孤儿判定见 §8，追溯链环 2/3 检出见 §12。以上只指向位置，本文件不复述其文字。
>
> **本文件里的每个数字与存在性断言均由本节点当场跑命令得出。** `impl/detail.md` 的自述在本文件中只是**被核验的对象**。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程会话，分支 `main`。
- **与 `impl/detail.md`「执行身份」比对：相同——身份独立不成立**（第七次）。soft 执行器下无派发层。
- **结构性偏袒如实标出**：本结论由与实现者同一会话的角色作出，证据强度低于独立验证。

## 断言与实际结果

| 条目 | 任务 | 判据与实跑 | 结论 |
| --- | --- | --- | --- |
| 001 `[basic]` | T-D3 | `go test … -run TestOverlapCommand -v \| grep -c '^--- PASS'` → `1`（判据 `≥1`） | **满足** |
| 002 | T-D1 | `go test … -run TestMaximalExtension -v \| grep -c '^--- PASS'` → `1`（判据 `≥1`） | **满足** |
| 003 | T-D2 | `go test … -run TestLegitimateOverlap -v \| grep -c '^--- PASS'` → `1`（判据 `≥1`） | **满足** |
| 004 | T-D3 | `go test … -run TestOverlapExitCode -v \| grep -c '^--- PASS'` → `1`（判据 `≥1`） | **满足** |
| 005 `[横切]` | 无 | `go test ./internal/themis/... 2>&1 \| grep -c '^FAIL'` → `0` | **满足** |

**四条 `go` 判据由人工判**，依 R2 裁定（`go` 不在白名单，与 `claim-command-evidence` step2 同处境）。本节点逐条实跑并记录输出。

### 判据 003 的语义部分另作核查

判据只证明测试通过，**证不出"排除规则真的可被调用者看见"**。本节点另跑一条实际报告：

`themis overlap .themis/spec` 的输出**含四条规则名与各自排除处数**（清单式并列 `4`、节点名并列 `4`、路径与编号 `5`、指向另一文件 `2`），并含"**经验规则，非完备判据**"字样。**语义成立。**

### 判据 002 的效果核查

`impl/detail.md` 称降噪由 `153` → `26` → `5`。本节点独立复核末段：**实跑 `themis overlap .themis/spec` 报出 `5` 处**，与自述一致。

## 命令证据

- 四条测试各 `1` 处 PASS（逐条见上表）
- `go test ./internal/themis/... 2>&1 | grep -c '^FAIL'` → `0`
- `internal/themis` 测试数 `26`、`internal/themis/cli` 测试数 `10`
- `themis overlap .themis/spec` → 报出 `5` 处待复核、四条规则各列排除数

## 五处报出全部核实为真违规

本节点逐条打开两端文件比对原文，**五处全部成立**：

| # | 落点 | 性质 |
| --- | --- | --- |
| 1 | `flow.md` ↔ `rules.md` | `passed`/`failed` 判定描述同形 |
| 2 | `flow.md:154` ↔ `rules.md:74` | basic 段三项判据被复述 |
| 3 | `flow.md:189` ↔ `rules.md:89` | §8 孤儿规则被复述 |
| 4 | `flow.md:33` ↔ `rules.md:107` | §10 fail-closed 判据被复述 |
| 5 | `rules.md:143` ↔ `template.md:79` | 编号写法前提被复述 |

**零误报。** 报出的五处无一是正当重合。

**第 3、4 两处是 impl 节点新发现的**，Intake 未找到——Intake 的粗过滤把含顿号的句子一并排掉了。**这佐证了 T-D2 那处修正的必要性**：拟定的顿号规则若不加句读限定，会漏掉这两处。

## 追溯链检出（§12 环 2、环 3）

- **环 2**：上游排除 `[横切]` 后为 `001 002 003 004`，下游展开后含全部四条，**差集为空 → 通**。
- **环 3**：上游 `T-D1 T-D2 T-D3`，下游为本文件任务栏，三者均出现，**差集为空 → 通**。

## 结论

**passed。**

五条判据全部满足，003 的语义部分另经实际报告核查。追溯链两环均通。**工具对真实控制面实跑零误报，报出的五处全部核实成立。**

**五项如实带出，不掩饰**：

1. **身份独立不成立**（第七次）。
2. **五处违规均未修**——本 step 只交付检测手段，R1 已确认。**其中三处此前从未被发现**（第 1、3、4 处），说明控制面此前从未被真正检查过。
3. **落地中三处偏离批准范围**，全部是实跑后据证据修正，逐条记入 `impl/detail.md`。**最要紧的一处**：拟定的顿号规则会排除掉本需求开立时要找的那处违规——**工具差点把自己要找的东西过滤掉**，由测试当场抓到。
4. **工具最初惩罚了合规行为**——「指向另一文件」那两处是控制面规则所要求的正确写法，拟定的三条规则没覆盖，实跑才发现。
5. **阈值 `20` 字无理论依据**，本 step 未做敏感性分析。

## 本节点自身的两条断言不受本工具校验，如实记明

`themis verify` 跑本文件报出两条，**均非错误**：

1. `go test … 2>&1 | grep -c '^FAIL'` —— 含 `2>&1` 重定向，本包不解释该 shell 语法，拒绝执行。**判据 005 的原文即如此书写**，R2 已批准。
2. `themis overlap .themis/spec` —— **`themis` 自身不在只读白名单内**。

**第 2 条是本 step 新暴露的一处**：**本工具无法校验关于它自己输出的断言。** `themis` 会执行外来命令（`verify` 子命令的本职），把它放进白名单等于让白名单自我豁免——**这不是疏漏，是白名单机制的必然边界**。

**处置**：不放行，本节点的该条断言由人工实跑判定（与四条 `go` 判据同处置）。**如实记入，交人工验收知悉。**
