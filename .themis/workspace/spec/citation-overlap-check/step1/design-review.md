# design-review.md — citation-overlap-check / step1

> 本文件是 R2 抽象设计评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R2 抽象设计评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：给 CLI 加子命令，找出控制面文件之间"引用时把内容抄过来"的地方。

**两个待裁定项**：(一) 判据用 `go test` 而 `go` 不在白名单，照旧人工判是否成立；(二) 是否批准抽象设计。

**值得知道的一件事**：这是 `question-eligibility` 补入的交叉核查**第一次真正生效**——step 定界主动列出本 step 触及的既有裁定，`go` 冲突在动手前就摆出来了。**上次正是因为没有这条，该冲突拖到落地才发现。**

**未覆盖**：排除正当重合的规则做不到零误报，故要求规则对调用者可见；只查四份控制面文件之间；三处已发现违规不修。

## 判据与边界的交叉核查（`flow.md` R2 节）

**本节点依该条要求核对，结论记于此。**

| 本 step 已确认的边界 | 判据是否冲突 | 处置 |
| --- | --- | --- |
| 白名单只放行只读查询命令 | **不冲突**——新子命令只读文件、不执行外来命令，不经过白名单机制 | 无需处置 |
| `go` 不放行、判据改人工判 | **冲突**——判据 001–004 均为 `go test …` | **照旧人工判**（本次裁定） |
| 「不改证据去迎合判据」 | **不冲突**——本 step 不改任何已闭合工件 | 无需处置 |

**该核查由 `scope.md`「会触及或放宽的既有裁定」先行列出，本节点复核并作出裁定。**

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影提出两项待裁定——(一) `go` 冲突照旧人工判是否成立；(二) 是否批准抽象设计。

**「批准」一条覆盖两项。** 投影对第一项给出推荐（接受照旧）与理由（同一情形已裁定过，重开会让同一件事裁两次），并写明判错后果。**答复未提出异议，故取推荐项为裁定内容。**

## 结论

**approved。**

五条行为条目获授权，进入详细设计与任务分解。

### 裁定：`go` 判据照旧人工判

本 step 判据 001–004 均以 `go test` 开头，与既有白名单裁定冲突。**处置沿用 `claim-command-evidence` step2 的裁定：不放行 `go`，这四条判据由人工判。**

**代价照旧记明**：本 step 的四条判据不受 `themis verify` 保护，与其它判据不同待遇。**这是第二次出现同一处境**，两次均因判据必须验证"代码是否工作"而无只读替代。

**本次批准同时确认**：`flow.md` 的交叉核查条款按预期生效——冲突在抽象设计阶段即被摆出，而非拖到落地。**该条款的第一次实际检验通过。**

**本次批准未覆盖**：排除规则的零误报（做不到，故要求规则可见）；工件层的复述检测；三处已发现违规的修复。
