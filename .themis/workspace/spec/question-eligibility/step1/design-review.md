# design-review.md — question-eligibility / step1

> 本文件是 R2 抽象设计评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R2 抽象设计评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：给"什么问题才该问"补一条判据，写进 `rules.md` §2。

**两个待裁定项**：(一) 执行者修正了 R1 已批准的一条表述，该修正是否成立；(二) 是否批准本抽象设计。

**改变原定方向的发现**：判据必须双向——`drift-log.md:51` 记有相反方向的失效（只问执行细节、没问目标本身），单向收紧会放大它。另加第三条：判之前须先读全依据，否则"多问一次"退化为"自己判错"。

**未覆盖**：判据无机器兜底；"能否推出答案"本身是判断，可能被用作不问的借口；本节点写判据时又犯一次同型错误（`grep -c` 计行数不计短语数），第 24 次实发。

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影提出两项待裁定——(一) R1 表述的修正是否成立；(二) 是否批准抽象设计。

**「批准」一条覆盖两项。** 投影对第一项给出推荐 (a)（接受修正）与理由（照原表述落地会写下一条已知无效的条款——那次边界确实按时暴露了，漏的是判据与边界的交叉核对），并写明判错后果。**答复未提出异议，故取推荐项为裁定内容。**

## 结论

**approved。**

五条行为条目获授权，进入详细设计与任务分解。

### 裁定：R1 表述的修正成立

**原表述**（R1 批准）：边界类问题须在 step 定界阶段暴露。
**修正后**：既有边界暴露做法保留并写进契约；**另补一条一致性要求**——`specify.md` 的判据须与本 step 已确认的边界不冲突，冲突由 R2 判定。

**修正依据经确认**：

1. `claim-command-evidence/step2/scope.md` 第 `38` 行证明边界暴露做法**本就存在且成功**——它在 step 定界阶段提出安全边界并明写"不得默默实现"，所有者随即裁定白名单。
2. `go` 那次的真实失效是**同一 step 内两处已批准内容互相矛盾而无节点交叉核对**：判据在抽象设计写下、白名单在 R2 裁定，两者都在落地前。
3. 照原表述落地**对该次失效零作用**。

**效力边界**：本裁定认可的是"执行者依核实结果修正 R1 表述、并主动标出该修正交 R2 判定"这一做法，**不是对"执行者可自行改动已批准范围"的一般性授权**。修正须满足两条：有实跑核实为据、在工件中显式标出并交下一个人工闸门判定。

**本次批准未覆盖**：判据无机器兜底；"能否推出答案"本身是判断，可能被用作不问的借口（所有者已就松紧作过裁定）。
