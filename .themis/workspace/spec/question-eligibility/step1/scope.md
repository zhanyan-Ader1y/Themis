# scope.md — question-eligibility / step1

> 本文件是 step 定界节点的实例工件。本节点的前置闸门与产出见 `flow.md`「step 定界」节。以上只指向位置，本文件不复述其文字。
>
> **本文件的全部数字与存在性断言按 `template.md`「断言形态」记法书写，由本节点当场跑出。**

## 承担的上层分解项

`Intent.md`「step 分解」的唯一一项：补 §2 的提问资格判据，并在 `flow.md` step 定界节点补边界暴露要求。

## 本 step 边界

改控制面两处：`rules.md` §2、`flow.md` step 定界节。**不写代码，不改任何已完成实例的工件。**

## 与其他 step 的关系

**本需求只有一个 step**，本 step 完成即需求达成。

## 核实到的、影响做法的三处事实

### （1）边界暴露的做法已经存在且有效，第二项配套须重新定性

**R1 批准的配套项之一是"边界类问题须在 step 定界阶段暴露"。本节点核实后发现该表述基于错误的诊断。**

`claim-command-evidence/step2/scope.md` 第 `38` 行**已经自发做了这件事**，且做对了——它在 step 定界阶段提出"本 CLI 会执行工件里写下的命令，这是一处必须正面处理的安全边界"，明写"**不在 R1/R2 已批准的任何内容里**"、"**须由本 step 的 R2 明确处置，不得默默实现**"，并附核实证据。所有者随即裁定取白名单。

**那次成功的边界暴露与失败的 `go` 那次，是同一个 step 内的两件事。**

**因此失败原因不是"暴露太晚"**。核实 `go` 那次的时间线：

- `step2/specify.md` 第 `15`、`27` 行等处写下以 `go build`、`go test` 为主的判据——在**抽象设计**节点。
- 白名单裁定（只允许只读命令）——在 **R2**。
- 两者**都在落地之前**，冲突在那一刻就已可推出：**本 step 自己的判据用了本 step 自己的白名单禁止的命令。**
- 但直到 **impl/detail** 跑起来才发现。

**真正的失效是：同一个 step 内两处已批准内容互相矛盾，而没有任何节点负责交叉核对。** 不是时机问题，是缺一次一致性检查。

**这一条改变了本 step 的做法**：若照 R1 批准的字面写"边界须在 step 定界暴露"，**它拦不住 `go` 那次**——那次的边界确实在 step 定界暴露了（第 38 行），漏的是后续判据与它的冲突。

### （2）§2 现有三条拒绝条件全部单向

`awk '/^## §2/,/^## §3/' .themis/spec/rules.md | grep -c '拒绝条件'` → `1`

该行三条拒绝条件——三要素任一不明确、存在未回答的问题、以"轮次已够"代替三要素判定——**全部指向"问得不够"**，无一条指向"问得多余"。补一条反向拒绝条件不与既有三条冲突，是补上缺失的另一半。

### （3）三类失效的实发处数，逐条可查

- **类一（有依据却不判）**：`grep -c '与 Agent 推荐一致' .themis/workspace/spec/trace-number-scan/QA.md` → `3`
- **类二（依据在手边却没读）**：`claim-command-evidence/QA.md` 问 3——答复推翻 Agent 建议，正确答案早在 `drift-log.md`；另加本需求 Intake 初稿一次（第 23 次实发）
- **类三（该问的没问）**：`grep -c '追问第 1 轮只问机制' docs/plan/spec-replay/drift-log.md` → `1`

**三类都有实发，判据须同时覆盖三类。**

## 本节点新暴露、不在 R1 已批准内容里的一处

**有一处**：事实（1）——R1 批准的第二项配套（"边界类问题须在 step 定界阶段暴露"）**基于错误诊断，照字面落地拦不住它要拦的那次失败**。

**本节点不自行改写 R1 已批准的范围**，而是按核实结果提出修正主张，交 R2 判定：

- **原表述**：边界类问题须在 step 定界阶段暴露。
- **修正主张**：step 定界已有的边界暴露做法保留并写进契约（它有效，且已自发使用）；**另补一条一致性要求**——`specify.md` 的判据须与本 step 已确认的边界不冲突，冲突在 R2 判定。

**为什么不直接按原表述落地**：那会写下一条**已知拦不住目标失效**的条款。`scope.md` 第 38 行的存在证明现有做法本就有效，再规定一遍只是把有效做法写进契约，**对 `go` 那次零作用**。

## 命令证据

- `claim-command-evidence/step2/scope.md` 中安全边界小节的起始行：`grep -n '本 CLI 会执行工件里写下的命令' .themis/workspace/spec/claim-command-evidence/step2/scope.md | awk -F: '{print $1}'` → `38`
- `awk '/^## §2/,/^## §3/' .themis/spec/rules.md | grep -c '拒绝条件'` → `1`
- `grep -c '与 Agent 推荐一致' .themis/workspace/spec/trace-number-scan/QA.md` → `3`
- `grep -c '追问第 1 轮只问机制' docs/plan/spec-replay/drift-log.md` → `1`
