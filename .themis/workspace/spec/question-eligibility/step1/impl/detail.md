# impl/detail.md — question-eligibility / step1

> 本节点的前置闸门、产出与失败去向见 `flow.md`「impl/detail」节。以上只指向位置，本文件不复述其文字。
>
> **本文件不含验证结论。** 五条判据是否满足属 verify/detail。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话，分支 `main`，动手前 HEAD `1b44507`。
- **授权来源**：**R3 记作"授权代行"而非 approved**（`task/review.md`「结论」）。所有者答复"接下来由你自己判断，直到完成这个任务"，未就方案内容表态。**本节点据授权推进，但如实记明方案未经所有者内容审阅。**
- **本节点不承担 verify/detail**：soft 执行器下无派发层，verify/detail 很可能仍由本会话承担。

## 实际改动

`task/detail.md` 两条任务全部落地，只改控制面两份文件，未写代码。

- **T-D1 — `rules.md` §2 补提问资格。** 判据项补提问资格一句（能推出的一律自己判）；拒绝条件项补三条并列条件；判定者项补 Agent 自检 + R1 复核，并附一句可操作的自检（"若不问，我会判错什么"）。另补三段说明：三条方向不同须一并读、第三条与第二条各自的实发依据、以及松紧的裁定依据（按可恢复性排序）。**现有三条拒绝条件逐字保留。**
- **T-D2 — `flow.md` R2 节补交叉核查。** 写明 R2 评审者须核对判据与本 step 已确认边界是否冲突，冲突即驳回或当场裁定；附 `go` 那次的实发依据（判据与白名单都在落地前确定、冲突当时可推出、无节点交叉核对）；并明写这不是重复 step 定界的职责——**边界暴露属该节点且实践中已有效，本条补的是"没人把两者放在一起看"。**

**顺带一处**：`flow.md` 抽象设计节原文只指 `[basic]` 见 §5，补上 `[横切]` 见 §12——`trace-number-scan` 新增该标识时未回改此处。**属该 step 的遗漏，本节点顺手补齐，不单列为任务。**

## 与批准范围的偏差

**一处，且性质须说清。**

**R3 不是 approved，是授权代行。** 所有者未审阅本方案内容，因此"落地是否在批准范围内"这一判断**没有可对照的批准表态**。本节点据 R2 已批准的 `specify.md` 五条判据推进，落地内容与 `task/detail.md` 两条任务逐条对应，**未引入任务之外的改动**（除上述 `[横切]` 索引一处，已单独标明）。

**除此之外无偏差**：两条任务的做法项逐条落地，无删减、无扩张。

## 命令记录

- 落地前 HEAD：`1b44507`。**历史点位**，不按可重跑断言书写。
- 五条判据落地后实跑：001 → `2`、002 → `1`、003 → `2`、004 → `2`、005 → `3`。
- 三条新增拒绝条件各自成句：`awk '/^## §2/,/^## §3/' .themis/spec/rules.md | grep -o '\*\*有依据却不判\*\*\|\*\*依据没读全就提建议\*\*\|\*\*目标层 claim 缺合法来源却不问\*\*' | wc -l` → `3`
- 控制面两份文件自校验：`themis verify` 对 `rules.md`、`flow.md` 均 `0` 条报告。

## 尚未关闭、留给 verify/detail 与人工的

1. **R3 未经内容审阅**——最要紧的一项。本 step 的方案与落地都只有执行者一人看过，**若所有者事后审阅认为方案有问题，本 step 应退回详细设计+任务节点重做**。
2. **判据无机器兜底**——三条拒绝条件全靠 R1 评审者判，`themis` CLI 不校验提问资格。
3. **"能否推出答案"本身是判断**——可能被用作不问的借口。所有者已就松紧作过裁定（宁可判错，不可多问），但该风险不因裁定而消失。
4. **第三条拒绝条件的抵消作用未经实测**——它是为防止前两条放大类三而写的，**但本 step 无法验证它是否真能抵消**；要等后续实例的追问行为才知道。
