# state.md — question-eligibility

> 各闸门行内格式：`- <节点名>：<结论> — <证据路径>`。结论取值由 `flow.md` 通用状态条款按节点类型固定，本文件不自建约定。

## 当前节点

**step1 的 R3 详细方案评审（等待所有者答复）。** 四条结构决策、两条 detail 任务已产出，basic 段为空。**本次投影无待裁定项。**

**本实例的特殊处境须记明**：本需求要治的是"该不该提问"，而**本 spec 自身每提一个问题都是一次实测机会**。第 1 轮追问只提一问、另三项候选问题自行判定并写入 `QA.md` 末节——**这是本需求所主张的做法在本需求自身的第一次应用**。

## 各闸门

### spec 级

- Intake：已走完 — `Intent.md`（**两次核实都推翻了前一版归纳**：初稿把三个追问一并归因为过度提问，逐条读 18 问后发现过宽；随后按自己刚提出的主张回头读 `drift-log.md`，发现第 `51` 行记有方向相反的第三类失效。**第二次核实本身即本需求要治问题的第 23 次实发**——初稿正是在没读该文件的情况下写下的）
- 追问：已走完 — `QA.md` 第 1 轮（**只提一问**：判据判错的代价该由谁承担，属产品判断、仓库推不出。所有者答「宁可判错，不可多问」。另三项候选问题有依据，自行判定并记入末节）
- R1 意图评审：approved — `intent-review.md`「结论」（所有者原话"批准"；依 §11 记录对应关系——本次投影只有一个待裁定项，一条答复语义上完整）

### step 级

`Intent.md`「step 分解」的一个步骤已由 R1 确认。

### step1 — 补提问资格判据（进行中）

- step 定界：已走完 — `step1/scope.md`（核实三处事实。**最要紧的一条**：R1 批准的第二项配套基于错误诊断——`claim-command-evidence/step2/scope.md:38` 证明边界暴露做法本就有效，`go` 那次漏的是判据与边界的交叉核对。已提出修正主张并标出交 R2 判定，未自行改动已批准范围）
- 抽象设计：已走完 — `step1/specify.md`（五条行为条目，001–004 落地前实跑均为 `0`。**判据 005 数错并当场订正**——`grep -c` 计行数不计短语数，输出 `2` 而非 `3`，第 24 次同型实发，与第 20 次同型）
- R2 抽象设计评审：approved — `step1/design-review.md`「结论」（所有者原话"批准"；两项待裁定由一条答复覆盖。**裁定：R1 表述的修正成立**，并划定效力边界——认可的是"依核实结果修正并主动交下一闸门判定"这一做法，非"可自行改动已批准范围"的一般性授权）
- 详细设计+任务：已走完 — `step1/design.md` 与两份任务文件（四条结构决策；basic 段为空，本 step 是该形态第六个使用者；**判据 005 带 `[横切]` 无任务承担**——本 step 是 `trace-number-scan` 定义该标识后的第一个外部使用者）
- R3 详细方案评审：进行中 — 等待所有者答复
- 其余节点：未开始

## 当前性

- `Intent.md`：current（触发、现状、收敛主张齐备；三类失效及其实发逐条记入）
- `QA.md`：current（第 1 轮一问，答栏逐字录入原话；自行判定的三项附依据）
- `intent-review.md`：current（R1 approved，含依 §11 的对应关系记录）
- `step1/scope.md`：current（三处事实均由本节点实跑；R1 表述的修正主张已标出）
- `step1/specify.md`：current（001–004 可证伪，005 为横切防回归判据）
- `step1/design-review.md` 与 `step1/design.md`、两份任务文件：current
