# task/review.md — 2026-08-19-core-removal / step1

> 本文件是 R3 详细方案评审的实例工件。本节点的前置闸门、产出、以及本次评审对两份任务文件的覆盖方式见 `flow.md`"R3 详细方案评审"节；分类核查的判据与判定者见 `rules.md` §4；任务是否越界的判定见 `rules.md` §6；实现依据的事实源判定在本节点发生，条款见 `rules.md` §1；投影的低负担要求见 `rules.md` §3。以上各处只指向位置，本文件不复述其文字。
>
> 以下「评审范围」「分类核查」是任务作者提出的投影与自查记录，不构成评审结论；结论由 R3 评审者给出（判定者条款见 `rules.md` §4）。

## 评审范围

本次评审一次呈现三份工件：

1. **`step1/design.md`**——本轮（任务 4）修订后的状态。修订分三类，**逐条内容与命令核验记在该文件内，此处只给结论**：（a）**按所有者 R2 批复而做的三项**——删节口径由窄读改为宽读、仓库根 `AGENTS.md:13` 由"范围外不处理"改为纳入并给出处理做法、订正两处事实错误；（b）**本轮自查重跑命令时发现的六处数字错误**——四处章节行号区间（其中一处把两个独立章节合并记为一节，致该节此前实际未被逐节判定，本轮补判为"不改"）与两处列表条数；**外加一处引用点计数与路径归属错误**（属归属类，不计入上述六处，与「分类核查」第 7 条同一口径）；（c）**一处权威归属订正**——三处曾把本节点的推广标为所有者"明确点名"，已改为事实表述（见下「分类核查」第 8 条）。本轮另按同一口径新增了三处此前被保留、但同源理由要求删除的内容（`README.md:93`、`:109` 与 `CLAUDE.themis.md:96`）。「结构决策」第 5、6 条本轮未改动。
2. **`step1/task/basic.md`**——本段为空，无 `### T-B<n>` 条目；空段的判定过程、所采用的"外部可观测"口径及其边界写在该文件内。
3. **`step1/task/detail.md`**——六个任务 T-D1–T-D6，逐条给出行为目标、对应的 `specify.md` 条目与依赖的基础任务。

`design.md` 的修订与两份任务文件同属本节点的产出，一并进入本次判定，不是背景材料。

## 分类核查

以下是任务作者对自己提出的分类所做的自查，供 R3 判定时对照：

1. **basic 段为空是判出来的**：`task/basic.md` 先厘清候选清单（`design.md`「结构决策」七条中第 5、6 条为"不动"的决定不进候选，其余五条加删除动作本身共六项），再对六项逐项核对 `rules.md` §4 的两条判据，两条都不成立，故无任务归入 basic。判定依据写在该文件「基础任务」小节内，即 §4 为其指定的落点。
2. **每个 basic 任务是否写明判定依据与被谁依赖**：本段无条目，本项空集满足。空段本身的依据已写入 `task/basic.md`，不以"没有条目"代替说明。
3. **是否存在单独产生外部可观测结果的 basic 任务**：本段无条目，空集满足。
4. **是否存在无消费者的 basic 任务**（孤儿，条款见 `rules.md` §8）：本段无条目，空集满足；`task/detail.md` 六个任务的"依赖的基础任务"一项均为"无"，与空段一致，两份文件之间无悬空引用。
5. **任务是否引入 `design.md` 未定的结构**（判定条款见 `rules.md` §6）：T-D1–T-D5 逐条指向 `design.md`「结构决策」第 1、2、3、4、7 条，T-D6 指向「架构与边界」锚定的删除动作；行为目标只重述已定改动的范围与行号，未出现 `design.md` 里没写过的**结构**改法。`design.md`「结构决策」第 5、6 条不对应任何任务，因为二者是"不动"的决定，没有可落地的动作——这不是遗漏。**一处曾添加、经所有者裁定删除的内容（如实保留这一经过）**：呈评审时 `task/detail.md` T-D3 末句带了一条落地顺序建议（先于 T-D5 执行，以免中途出现指向已删章节的描述），`design.md` 未写过这条顺序。本节点当时判断它不是字段、类型、签名、迁移方向或配置项，因而不落在 §6 所约束的结构决策范围内，但仍如实标出它是任务文件自行添加的内容，交 R3 裁定。所有者在 R3 批复中明确回应"删掉这句"（原话见「结论」），该句已从 `task/detail.md` 删除，六个任务本身不受影响。此处保留这一经过，不抹掉它发生过。（**本条在任务 4 记录 R3 处置的同一次编辑里也被未声明地改写过**——八处改动的清单、依据的比对命令与不恢复原文的理由，见下方第 8 条第二项末尾的补记。）
6. **trace 覆盖**：`specify.md` 四条行为条目均有对应任务——`SPEC-COREREMOVAL-001` ← T-D1、T-D2、T-D3（三者合取才可能通过）；`SPEC-COREREMOVAL-002` ← T-D4；`SPEC-COREREMOVAL-003` ← T-D6；`SPEC-COREREMOVAL-004` ← T-D5。六个任务也都各自挂到了一条判据上，无既不对应判据、又不对应结构决策的任务。
7. **实现依据的事实源**（该项判定在本节点发生，条款见 `rules.md` §1）：`design.md`「结构决策」的每一条都锚定在「事实依据」的命令输出上（命令 1–11）。本轮把命令 1–7 逐条重跑并与所载输出逐字比对，一致；命令 8–11 为本轮新增，分别核验章节行号区间、列表条数与 failure budget 残留位置、命令 2 实际覆盖的文件枚举、以及所有者是否点过章节名。所依据的是命令的真实输出，不是上一版工件里写下的数字。**这一条本轮特别核对，因为被订正的错误全部属于同一形态**：数字（无论沿用还是新写）未跑命令核对。本轮订正八处——两处事实错误（命中行数、`docs/core/` 存在性前提）、四处章节行号区间、两处列表条数——外加一处引用点计数与路径归属错误。其中两处列表条数是上一轮宽读改写时**新写**的数字，写下时未核对，且发生在执行者已把这一盲区写进漂移清单之后；这次复发本身已另记入 `docs/plan/spec-replay/drift-log.md`。
8. **提交 R3 判定的两处口径选择**（任务作者做了判断，但判断依据不在控制面内，请评审者确认或纠正）：
   - **"外部可观测"在本需求下指什么**。本需求没有可运行产物，`rules.md` §4 也没有为这类需求界定该词。`task/basic.md` 采用 `specify.md`（已获 R2 批准）的判据形态作为界定来源。若改用"产品运行时行为"这一更紧的读法，分类结论会翻转成"T-D3 归入 basic、T-D5 为其消费者"——两种读法的分岔点与选择理由写在 `task/basic.md` 末段。**R3 结论对本条的处置**：维持现状，问题延后——所有者选择优先闭合流程，该词的界定留到 MVP 之后再处理（原话见「结论」第 2 点）。因此不切换读法，basic 段仍为空，T-D3 不翻转。控制面缺这条界定是**已知且被有意延后**的缺口，不是本次遗漏，已记入 `docs/plan/spec-replay/drift-log.md` R3 条目。
   - **宽读逐节判定全部是本节点对 R2 口径的推广**。所有者 R2 给的是口径（宽读），**没有点过任何一个章节名**（四份实例工件全文检索零命中，核验见 `design.md`「事实依据」命令 11）。因此两份文件里每一节的删或留都是本节点应用该口径做出的判定，无一经所有者逐节确认。上一版 `design.md` 曾把其中三处标为"R2 批复明确点名"，那是无据的权威归属，已订正（见 `design.md`「架构与边界」权威归属一条）。**已知残留清单是非穷尽的**，其构成与边界见 `design.md`「取舍」。**R3 结论对本条的处置**：所有者未逐节裁定推广是否得当，而是就残留边界给出安排——删节与重写之间的边界由控制器提出定义、经所有者审阅后决定（原话见「结论」第 3 点）。在该边界确定之前，按 `design.md`「取舍」现载的非穷尽清单执行；边界一旦定下，可能要求回头处理现被列为"保留"的短语级残留。本项不阻塞本次批准。**补记（任务 5）：任务 4 把上述 R3 处置写进本项的同一次编辑里，还对本项与「分类核查」第 5 条做了未声明的删改。** 以下清单据 `git diff f04d721 c675c07 -- .themis/workspace/spec/2026-08-19-core-removal/step1/task/review.md` 的真实输出逐段核出。（**这份补记至今写了三版，每一版都以为自己已经全了**：第一版只凭眼看、仅披露其中两段；第二版跑了这条 diff，把本项补到五处、并首次点出第 5 条也被改写，但第 5 条只列了四处；第三版由任务 6 把两版原行按标点切段后逐段机械比对，才补齐第 5 条余下四处字词级改动并更正其中一处定性。三次复发都记在 `docs/plan/spec-replay/drift-log.md`。）**本项失去四段文字、外加一处词级改动**：（1）「——包括本轮新增的几处部分删除——」；（2）「、边界与本轮改为删除的几处」缩为「与边界」；（3）「，此处不重复枚举」；（4）**面向评审者的判定请求句「推广是否得当、残留边界是否可接受，请一并判定。」**——这一段不是编辑性零碎，它是呈评审版本向所有者提出的请求，删掉它等于抹掉本项当时在问什么；（5）「本轮已订正」缩为「已订正」。**「分类核查」第 5 条在同一次编辑里同型改写，同样未声明；本项清单经任务 6 第三轮补披露后为八处**（补披露的经过与依据见本段末尾）：（1）小标题由「一处需要收窄措辞的自查」改为现文；（2）新增「呈评审时 」，并把原句的主语补为该任务文件路径；（3）「末句给了一条落地顺序建议」改为「末句带了一条落地顺序建议」；（4）新增「本节点当时判断」作为整句的主语从句；（5）「因此不落在」改为「因而不落在」；（6）「但它确实是任务文件自行添加、`design.md` 未定的内容」改写为「但仍如实标出它是任务文件自行添加的内容」并续接新增分句「交 R3 裁定。」——**这一处此前被记为"删去「`design.md` 未定的」这一限定"，定性不准**：主语、谓语与句尾都变了，不止少一个限定成分；（7）删去自我批评句「不应被"未出现没写过的改法"一句笼统盖过。」；（8）删去面向评审者的条件句「若 R3 认为任务文件不得携带任何 `design.md` 未写的内容，删去该句即可，不影响六个任务本身。」，其位置改写成了已发生的结果。所有者在 R3 只下达了一处删除指令（`task/detail.md` T-D3 末句），不包含以上任何一处。授权范围未受损——那一轮新增的三处删除仍记在本文件「评审范围」第 1 项，残留清单的构成与边界仍在 `design.md`「取舍」，第 5 条那处内容的经过仍完整保留在该条现文里。选择补记而不是把原文放回去，理由有二：被删各段多用"本轮"指代任务 4 那一轮，原样恢复到一份现已跨轮阅读的文件里会造出指代错误；且第（4）段与第 5 条那条条件句都是**已获答复的请求**，放回去会让已关闭的请求看起来仍待回应。删改发生过这件事需要留痕，不该靠恢复文字把它抹平。本次补记只增加声明，未改动「结论」原话，也未改动任何分类判定。**任务 6 的第三轮补披露沿用同一边界**：只重写本段的第 5 条清单（四处扩为八处、更正一处定性）、只改第 5 条末尾那句指向里的条数，同样不恢复被删原文、不动「结论」原话与任何分类判定；所依据的逐段比对命令与真实输出见 `step1/impl/detail.md`「命令记录」命令 D7。第 8 条第二项所列五处经同法比对，与真实 diff 一致，无遗漏，本轮未改。
9. **未出现"为填满两段而凑任务"**：判出空段后没有再构造 basic 任务去填充；六个 detail 任务全部对应 `specify.md` 判据或「事实依据」已核验的删除动作，没有为让 detail 段"看起来完整"而拆出边界模糊的子任务。自查时特意回看了这个动作有没有发生——`task/basic.md` 末段如实记下了唯一可能的凑法（把单个文件的编辑拆成伪阶段）以及不采用的理由。

## 未解决反馈

**空——本轮唯一一项已由所有者在 R3 批复中关闭。**

该项是：`catalog.md:33` 的开放状态在本节点该如何计入。呈评审时它作为本小节唯一一项列出，写明任务作者倾向"不计入"、依据是所有者已批准的延后本身即是一种处置，同时言明控制面无对应条款、不自行落定，请所有者确认或纠正。

**所有者批复"确认不计入"**（原话见「结论」第 1 点）。据此本项关闭，本小节清空。`catalog.md:33` 不因本次批准而被视为已决：它继续以 `design.md`「结构决策」第 6 条记录的延后状态存在，处理方案按所有者原定安排在本次落地之后另行呈交、由所有者单独审阅。本次批准解锁的是已定做法的那六处，不包含它。

「分类核查」第 8 条提交判定的两处口径选择，所有者均已在批复中给出处置（维持现状延后、边界另行定义呈审），不构成遗留的待回应项。除此之外本节点未产生其他确认请求。

## 结论

**approved**

所有者原话（逐字，未转述、未润色、未改错字）：

> appreved。1. 确认不计入。2. 优先解决流程闭环，可观测在MVP后再处理。3. 由你定义边界给我审阅后决定。 删掉这句。

理解旁注（不代替原话，仅供阅读辅助）："appreved" 为 "approved" 笔误。四点分别落在：1. 对应「未解决反馈」唯一一项——`catalog.md:33` 确认不计入本小节，该项关闭；2. 对应「分类核查」第 8 条口径一——"外部可观测"的界定维持现状，问题延后到 MVP 之后再处理，因此不切换读法、basic 段仍为空；3. 对应「分类核查」第 8 条口径二——删节与重写之间的残留边界由控制器提出定义、经所有者审阅后决定，在此之前按 `design.md`「取舍」的非穷尽清单执行；4."删掉这句"对应「分类核查」第 5 条标出的那处内容——`task/detail.md` T-D3 末句的落地顺序建议，已删除。

所有者同一条消息末尾另有一句面向控制器的会话指令，与本 spec 实例无关，按其性质不录入实例工件。

**本结论解锁实现节点。** 删除动作本身仍未执行——它属 impl 节点，由后续任务落地；本节点到此为止不删任何文件。basic 段为空对后续节点的影响见 `state.md`。
