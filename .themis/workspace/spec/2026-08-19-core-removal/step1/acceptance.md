# acceptance.md — 2026-08-19-core-removal / step1

> 本文件是人工验收节点的实例工件。本节点的前置闸门、产出、失效波及与失败去向见 `flow.md`「人工验收」节；本节点适用的判定条款见该节所指的 `rules.md` §7（复核验证身份独立）与 §8（据 verify/detail 的孤儿结论阻断，不重做代码层判定）；四个小节的固定划分见 `template.md`（`acceptance.md` 一行）。以上各处只指向位置，本文件不复述其文字。
>
> **本节点是本流程唯一的授权点。** verify/detail 的 `passed` 是证据，不是授权（`flow.md`「人工验收」节）。「用户原话」与「结论」两节在项目所有者答复前保持空缺,由所有者的答复填入,Agent 不得代答、不得预填。

## 交付视图

本节写给项目所有者,目的是让验收判断不必翻下游工件即可作出。以下每个数字都取自 `verify/detail.md`「命令证据」中由验证角色当场跑出的输出,本文件不新跑命令、不引入新事实。

**这个 step 要做什么。** 删除 `templates/.themis/core/`,并处理因此断链的活跃引用。授权范围由 R3 批准的 `task/detail.md` 六个任务界定(T-D1 至 T-D6)。

**实际删了什么。** `templates/.themis/core/` 整体删除,**98 个文件**。

**实际改了什么。** 5 个文件,合计 `+7 −105` 行:

| 文件 | 改动 | 对应任务 |
| --- | --- | --- |
| `templates/.themis/CLAUDE.themis.md` | 130 → 79 行,七处(三节整节删除、两处半句删除、一处整行替换、一处表格四行删除) | T-D1 |
| `templates/.themis/README.md` | 112 → 72 行,十处 | T-D2 |
| `templates/.themis/AGENTS.md` | 51 → 45 行,末节「与 `core/` 的关系」整节删除 | T-D3 |
| `.gitignore` | 11 → 10 行,第 8 行 `/.themis/core/` 删除 | T-D4 |
| 仓库根 `AGENTS.md` | R2 结论纳入的第七处引用 | T-D5 |

**验证跑了什么、结果如何。** verify/detail 对 `specify.md` 的四条行为条目逐条断言,**四条全部满足**:

- `SPEC-COREREMOVAL-001`:剩余命中 2 条,逐条在 `design.md`「结构决策」第 5、6 条找到明确保留的决定(分别是"不改"与"延后"),断链 **0** 条。
- `SPEC-COREREMOVAL-002`:命中 **0**。
- `SPEC-COREREMOVAL-003`:输出 `GONE`,文件数 **0**。
- `SPEC-COREREMOVAL-004`:命中 **0**。

**有没有删过头。** 实际删除区间与 `design.md` 所载区间相减:**非空行差额 0**,空行差额 7 且七行长度均为 0。即没有任何非空内容被多删,未越出 R3 批准范围。该减法由验证角色独立做出,不采信实现者关于"多删的只是空行"的自述。

**六处半句改动**另做整行精确匹配检验,六处全中。

**所有者在 R3 划定的六项边界**逐项跑命令核验,全部满足;其中一项(控制面四份文件未被改动)的证据强度限制见下文「阻断核查」第 4 项。Go 侧构建通过、测试通过包数 10、无失败行——core 是 Markdown 合同,删它未触发任何隐藏的代码依赖。

**这个 step 没有产生 basic 段落地。** `task/basic.md` 为空段(`### T-B` 条目数 0),impl/basic 零落地调用。

## 阻断核查

本节两项为 `flow.md`「人工验收」节规定的进入前置,第三项为 §7 要求的复核。三项均引用下游工件已有的结论,不在本节点重做代码层判定(§8 判定者条款)。

**1. 孤儿阻断:不存在孤儿——不阻断。** 引用 `verify/detail.md`「结论」第四条:本 step 的 basic 段为空(`### T-B` 条目数 0),没有已落地的 basic 改动,因而没有可判的孤儿。该判定由 detail 段验证角色在有代码证据处作出,本节点据其结论放行,未重新做代码层核实。

**需要所有者知道这个"不存在"是哪一种。** 它不是"逐个 basic 任务都找到了消费者",而是"没有 basic 任务可判"。验证角色为此专门自己数过——`task/basic.md` 三级标题为 0 条、六条详细任务的依赖项逐条为无——把"空段"从上游工件的一句自述变成了本节点可引用的事实,而不是直接采信。但空段下这条结论的信息量本就低,文本上"判过了"与"没东西可判"极易混同,故在此写明前提。

**2. verify/detail 结论为通过:满足——不阻断。** 引用 `verify/detail.md`「结论」:`passed`。该结论明写依据的是验证角色自己运行的命令与真实输出,不依据 `impl/detail.md` 的自述、也不依据工件文件存在与否(`SPEC-VERIFY-002`)。

**3. 验证身份独立(§7)复核:成立。** 本节点比对两份工件的「执行身份」小节,三项不同:

| | `impl/detail.md` | `verify/detail.md` |
| --- | --- | --- |
| 派发任务 | 任务 6 前半程(impl/detail)执行者 | 任务 6b(verify/detail)独立验证者 |
| 会话 | 一次会话 | 另一次会话 |
| 起始 HEAD | `3b2dde0`(落地提交之前) | `2daa931`(落地提交 `c6417e9` 之后) |

两栏各自独立填写,`impl/detail.md` 明写不预写、不猜测对方,`verify/detail.md` 亦不替对方背书。

**这条成立的原因,所有者应当知道。** 它靠的是派发把两个节点拆给两次会话,不是节点自身的自觉——同一 step 的 basic 段两栏明写相同、该条未成立,两段之间执行者的自觉程度没有变化,变的只有派发方式。**这道闸门当前的开合完全取决于派发层。** 另,两栏的模型标识相同,当前没有任何机器签名把"两次会话"固定下来,独立性成立于控制面指定的比对方式,不是密码学证明。

**4. 一处证据强度限制,如实转达。** 控制面四份文件"一字未改"这一项,由于 `.themis/spec/` 被 `.gitignore` 排除、不入库,git 给不出历史。验证角色能拿到的最强证据是"现内容与入库包源逐字相同,而包源在区间内零改动"加 mtime 早于落地提交。该链能排除"被改了且没改回去",排除不了"被改后又精确改回原样"。本 replay 没有落地前的独立哈希留档。

**5. 一处范围外残留,需所有者决定是否另开处理。** `templates/.themis/workspace/README.md:5` 有一句以 Core 为主语、陈述 Core 与 Workspace 读写关系的话。它**不构成任何一条判据的不满足**——该句不含带斜杠的 `core/` 字串,不进 `SPEC-COREREMOVAL-001` 的命中集;该文件也不在 `design.md`「架构与边界」清点的 5 个文件内。验证角色如实标出而未擅自扩大判据范围(扩大等于替所有者改验收标准),依 §8 的分工把处置交给本节点。

**这一项不阻断验收**(它不落在任何判据上),但它与两件已知事实同源:`design.md`「取舍」明写残留清单非穷尽;R3 结论把"删节与重写的边界"留待所有者另行表态。处置选项:(a) 本 step 内另开处理;(b) 记为已知残留、留待后续 step;(c) 视为无需处理。**请所有者在验收答复中一并表态。**

**6. 一处开放决策点仍未关闭,随本 step 一同交付。** `templates/.themis/workspace/context/catalog.md:33` 的相对路径引用是 R1 以来唯一被所有者点名延后的决策点,`design-review.md`「未解决反馈」第三条未随 R2 通过而关闭,`design.md`「结构决策」第 6 条明写"延后、本轮不定做法"。它是 `SPEC-COREREMOVAL-001` 那 2 条剩余命中之一,判据层面已由"找到对应的明确决定"满足,**但决定的内容本身是"延后",不是"已处理"**。验收放行不等于关闭该决策点。

## 用户原话

项目所有者(zhanyan)的答复逐字如下,分两条。

**第一条**(对交付视图与阻断核查的整体答复,自由输入):

> a

**第二条与第三条**(前一条只回了残留项的选项字母,未给出验收结论,因此就两项分别追问,所有者从给定选项中选定):

> accepted

> 验收后作为新 step 处理

**录入说明**:所有者第一条答复是单字母 `a`,对应「阻断核查」第 5 项列出的选项 (a)「本 step 内另开处理」;该条**未包含验收结论**。验收结论与残留项的具体走法由随后两次追问取得,所有者的选定即上引第二、三条。三条均为所有者本人答复,未经转述、未经改写。所有者未对「阻断核查」第 3、4 项(身份独立靠派发、控制面证据强度上限)与第 6 项(`catalog.md:33` 仍未关闭)另行表态。

## 结论

**accepted。**

- 本 step 交付通过验收。`templates/.themis/core/` 98 个文件删除与 5 处引用改动获得授权放行,摘要节点解锁。
- 三项阻断核查均不阻断:孤儿不存在(空段,无可判对象)、`verify/detail.md` 结论 `passed`、§7 身份独立成立。
- **`templates/.themis/workspace/README.md:5` 的范围外残留:不并入本 step,由所有者裁定「验收后作为新 step 处理」。** 本 step 不因该项回退、不重走 R3;该残留另起新 step,自设计节点起完整走流程。本 step 的已批准工件不受影响、不标 stale。
- **`templates/.themis/workspace/context/catalog.md:33` 的开放决策点未因本次验收关闭。** 判据由"找到'延后'这一明确决定"满足,决定内容仍是延后;放行不等于关闭,该决策点随本 step 交付一并带出。
