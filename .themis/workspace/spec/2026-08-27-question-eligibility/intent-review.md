# intent-review.md — 2026-08-27-question-eligibility

> 本文件是 R1 意图评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R1 意图评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：给"什么问题才该问你"补一条判据。现在的规则只管"提了的问题答没答"，不管"这个问题本来该不该提"。

**待裁定项**：批准需求本身与 step 分解（一个 step）。追问阶段的那一问已闭合，裁定为「宁可判错，不可多问」。

**改变原定方向的发现**：Intake 两次核实都推翻了前一版归纳，第二次尤其关键——按新主张回头读 `drift-log.md`，发现第三类失效方向相反（该问的没问），一条只说"能自己判的别问"的规则会放大它。**判据因此必须双向。**

**未覆盖**：判据仍靠人执行，无机器兜底；"能否推出答案"本身是判断，可能被用作不问的借口。

## 未解决反馈

无。

## 用户原话

**追问阶段所有者答复：「宁可判错，不可多问」。**

**依 §11 记录对应关系**：该答复对应 `QA.md` 第 1 轮问 1（判据判错的代价该由谁承担），取选项 (a)。**裁定内容**：判据按"可恢复性"排序——能推出答案的一律自己判，判错由下一个人工闸门复核依据时发现。

**R1 结论：所有者答复「批准」。**

**依 §11 记录对应关系**：本次投影只有一个待裁定项（批准需求本身与 step 分解），「批准」一条即完整覆盖，无并列裁定项需要追问。追问阶段的那一问已于此前单独闭合，本轮未重复呈现。

## 结论

**approved。**

确认需求本身，以及 step 分解为一个 step。

**本次批准确认的核心主张**：判据须**双向**——既拒绝"有依据却不判"（把判断成本转嫁给评审者），也拒绝"目标层 claim 缺合法来源却不问"（`drift-log.md:51` 记载的第三类失效）。**单向收紧会放大第三类。**

**追问阶段已闭合的裁定随本次批准生效**：判据按"可恢复性"排序——多问一次的成本不可恢复，判错的成本可恢复。

**本次批准未覆盖**：判据无机器兜底；"能否推出答案"本身是判断，可能被用作不问的借口（收紧判据的固有代价，所有者已就此裁定）。
