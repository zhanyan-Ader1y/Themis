# design-review.md — 2026-08-29-product-judge / step1

> 本文件是 R2 抽象设计评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R2 抽象设计评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。
>
> **本文件是补写的**：R2 的答复发生在对话中，当时未落成工件。**这是同型遗漏的第七次**，也是本需求自身第二次犯它要修的错（另一处是 `state.md`）。**判据没有抓到**——本 step 的六条判据全部指向 `flow.md` 与 `rules.md` 的内容，不验实例工件是否齐备。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**提交 R2 判定的只有一项**：追问第 2 轮所有者裁定"补全六条产出项的小节名"，而抽象设计节点实测后**只补四条**。

**收窄的依据是实测**：`Intent.md` 与 `QA.md` 没有稳定实践可写进契约——`Intent.md` 有两套并行写法、逐字共有的只有「来源引用」；`QA.md` 只有「第 N 轮」共有。

**凭空规定一套的后果**：既有实例会批量不合规，而它们当时都通过了验收。那等于用新规则追溯判既往为错。

**执行者不自行认定该收窄成立**——所有者裁定的是"六条"，实测支持"四条 + 两条只规定不变部分"。

## 未解决反馈

无。

## 用户原话

**所有者答复：「成立。没有机器兜底是指不确定何时执行还是流程不是规范、固定的」。**

**依 §11 记录对应关系**：

- **前半句「成立」**构成对上述收窄的裁定——所提交判定的正是"该收窄是否成立"这一问，答复与问一一对应。
- **后半句是另一个问题**，问的是 `intent-review.md`「本次批准未覆盖」第 1 项里"无机器兜底"的含义，**不构成对本节点任何内容的表态**，不读进本结论。

## 结论

**approved。** 覆盖 `specify.md` 全部六条。

**该结论经一次澄清补齐，依 §11「答复不完整而经追问补齐的，须一并记明追问经过」记录如下。**

**补齐前的判定**：执行者在引用本结论作前置闸门时，依 §11「判定者是下一次引用该结论的角色」当场判定——**判不过**。理由：提交 R2 的只有收窄一项，其余五条呈现了但所有者未单独表态；把"未就内容表态"写成对全文的 `approved`，正是 §11 拒绝条件「把非授权答复写成授权取值」要拦的做法。**据此不得引用，回到评审者处澄清**，流程按 fail-closed 停在 R2。

**追问经过**：

- **问**：「成立」是否同时构成对整份抽象设计的批准？两条路——(是) 覆盖全部六条，直接进详细设计；(否) 只覆盖那处收窄，另四条重新投影判一次。
- **所有者答复原话**：**「是」。**

**依 §11 记录对应关系**：该答复直接回答"是否构成对整份抽象设计的批准"一问，取"是"，故本结论的效力范围为 `specify.md` 全部六条。**这不是执行者的推断，是评审者就该问题的直接表态。**

**本节点未覆盖**：`specify.md` 六条判据本身是否足以证明本 step 的交付——**其中"给十四条产出项配判定者"这一头号交付物无任何判据断言**，见 `design.md`「取舍」。该缺口在 R2 呈现时未被指出，**由详细设计节点新暴露**，交 R3 判定。
