# verify/detail.md — 2026-08-29-product-judge / step1

> 本文件是 verify/detail 节点的实例工件。本节点的前置闸门与产出见 `flow.md`「verify/detail」节；身份独立与断言范围见 `rules.md` §7，追溯链检出见 §12。以上只指向位置，本文件不复述其文字。

## 执行身份

**独立验证子代理，与 impl/detail 的执行者（主会话）不是同一身份**（`rules.md` §7 要求）。

**冷启动**：未获得主会话的任何推理过程，全部结论只来自读仓库与实跑命令。`impl/detail.md` 的自述**一律作为待核对象，不作为证据**——下列每个数字都由本节点重跑得出。

**工作面**：仓库根 `C:\Coding\Themis`，分支 `main`，无 worktree。**本节点只写本文件一份**，未改动任何被测文件、判据或既有工件。

**已知 CLI 缺陷的规避**：`themis verify` 对 BRE 分组 `\(` `\)` 给静默错值，**本节点全部判据直接以 shell 跑**，未经由该 CLI。

## 断言与实际结果

| 判据 | 断言 | 实际值 | 结论 |
| --- | --- | --- | --- |
| `SPEC-PJUDGE-001` | `rules.md` 新增一节定义 `product-judge` → `1` | `1` | **满足** |
| `SPEC-PJUDGE-002` | §13 内含「人工闸门前」→ `≥1` | `2` | **满足** |
| `SPEC-PJUDGE-003` | 十四条「产出」项各指名 `.md` → `14`；「产出」项总数 → `14` | `14`；`14` | **满足**（**但证不出本 step，见「说明」一**） |
| `SPEC-PJUDGE-004` | 带 ` / ` 的「产出」项 → `≥11` | `11` | **满足** |
| `SPEC-PJUDGE-005` | `无固定小节名` 处数 → `≥2` | `2` | **满足** |
| `SPEC-PJUDGE-006` [横切] | `go test ./...` 的 `^FAIL` 计数 → `0` | `0` | **满足** |

**六条全部满足。**

### 逐条的补充核对（不止于数字对上）

- **`001`**：`grep` 得 `1` 只证明标题行存在。**本节点另行读了 §13 全文**（`rules.md` 第 `186`–`210` 行），确认「适用节点 / 判据 / 拒绝条件 / 判定者」四项齐备，与 §1–§12 同构。**§13 是文件末节**（`rules.md` 共 `210` 行），故 `002` 的 `awk '/^## §13/,0'` 区间不越界到其他节。
- **`002`**：两处命中分别在第 `188`、`208` 行，**均在 §13 内**。第 `188` 行写明频次为「R2、R3、人工验收前各一次（每 step `3` 次），R1 前一次（每 spec `1` 次，因 R1 是 spec 级）」，**与 `flow.md` 第 `29` 行一致**（该行现读作「R1 在 spec 级只发生一次；R2、R3、验收每个 step 各一次」）。**频次写准这一项独立核过。**
- **`004`**：`11` 这个数字本身可被无关行虚增，故本节点**逐行列出了 `11` 个命中及其行号**（见「命令证据」），并与改动前的 `7` 个逐行比对：新增的恰是第 `135`、`179`、`188`、`212` 行，即 T-D3 表列的 `design.md` / `impl/detail` / `verify/detail` / `摘要` 四条，**无一条来自无关行**。另核 `Intent.md` 与 `QA.md` 两行含 ` / ` 的计数为 `0`，**未虚增**。
- **`005`**：两处命中在 `flow.md` 第 `71`、`80` 行，分别落在 Intake 与追问两节的「产出」项内，**是本 step 要写的那两处标注**，非同名巧合。
- **`006`**：`go test ./...` 全部包 `ok` 或 `[no test files]`，无 `FAIL`。另核既有十二节判据未变——`rules.md` 的 `git diff` **只有一个 hunk `@@ -184,0 +185,26 @@`**，即纯追加 `26` 行，§1–§12 所在的第 `1`–`184` 行一字未动。

### 追溯链检出（`rules.md` §12，环 2）

**上游**：`specify.md` 的 `### SPEC-` 条目共 `6` 条；`SPEC-PJUDGE-006` 带 `[横切]` 标识，**按 §12「带 `[横切]` 的条目不计入上游集合」剔除**，上游集合为 `001`–`005` 共 `5` 条。

**下游**：`task/detail.md` 中出现的编号，展开后为 `001`–`006` 共 `6` 条。

**差集（上游减下游）为空，行数 `0` → 环 2 通过。**

**环 3**（`task/detail.md` 的 `T-D` 编号被本文件引用）：上游为 `T-D1`、`T-D2`、`T-D3` 共 `3` 条，本文件三条均有引用，**差集为空 → 环 3 通过**。

## 命令证据

**本节点的每个数字与存在性断言，逐一记明其来源命令。** 全部在仓库根以 shell 实跑。

**六条判据（改动后，当前工作树）**：

- `grep -c '^## §13' .themis/spec/rules.md` → `1`
- `awk '/^## §13/,0' .themis/spec/rules.md | grep -c '人工闸门前'` → `2`
- `grep '^- \*\*产出\*\*' .themis/spec/flow.md | grep -c '\.md'` → `14`
- `grep -c '^- \*\*产出\*\*' .themis/spec/flow.md` → `14`
- `grep '^- \*\*产出\*\*' .themis/spec/flow.md | grep -c ' / '` → `11`
- `grep -c '无固定小节名' .themis/spec/flow.md` → `2`
- `go test ./... 2>&1 | grep -c '^FAIL'` → `0`

**改动前基线（`git show HEAD:` 取出原文另存后跑同一命令，非依赖任何自述）**：

- `git show HEAD:.themis/spec/flow.md > /tmp/flow_head.md`
- `grep '^- \*\*产出\*\*' /tmp/flow_head.md | grep -c '\.md'` → `14`
- `grep -c '^- \*\*产出\*\*' /tmp/flow_head.md` → `14`
- `grep '^- \*\*产出\*\*' /tmp/flow_head.md | grep -c ' / '` → `7`
- `grep -c '无固定小节名' /tmp/flow_head.md` → `0`
- `git show HEAD:.themis/spec/rules.md > /tmp/rules_head.md`
- `grep -c '^## §13' /tmp/rules_head.md` → `0`
- `wc -l < /tmp/rules_head.md` → `184`
- `grep -c '^## §' /tmp/rules_head.md` → `12`

**结构与位置核对**：

- `grep -n '^## §' .themis/spec/rules.md` → 十三个标题，§13 在第 `186` 行，为末节
- `wc -l < .themis/spec/rules.md` → `210`
- `grep -c '^## §' .themis/spec/rules.md` → `13`
- `grep -n '人工闸门前' .themis/spec/rules.md` → 第 `188`、`208` 行
- `grep -n '无固定小节名' .themis/spec/flow.md` → 第 `71`、`80` 行
- `grep -n '^- \*\*产出\*\*' .themis/spec/flow.md | grep ' / '` → 第 `89`、`102`、`120`、`135`、`144`、`153`、`162`、`179`、`188`、`203`、`212` 行（`11` 行）
- `grep -n '^- \*\*产出\*\*' /tmp/flow_head.md | grep ' / '` → 第 `85`、`98`、`116`、`140`、`149`、`158`、`199` 行（`7` 行）
- `grep -n '^- \*\*产出\*\*' .themis/spec/flow.md | grep -E 'Intent\.md|QA\.md' | grep -c ' / '` → `0`
- `wc -l < .themis/spec/flow.md` → `216`
- `sed -n '29p' .themis/spec/flow.md` → 频次原文所在行，现仍为第 `29` 行

**改动面（核实未超出批准范围）**：

- `git status --short` → 仅 `.themis/spec/flow.md`、`.themis/spec/rules.md` 两份 ` M`，其余为本实例新增工件
- `git diff --stat` → `2 files changed, 36 insertions(+), 6 deletions(-)`
- `git status --short -- '*.go' | wc -l` → `0`
- `git diff -U0 .themis/spec/rules.md | grep '^@@'` → 单一 hunk `@@ -184,0 +185,26 @@`
- `git diff -U0 .themis/spec/flow.md | grep '^@@'` → `7` 个 hunk：`-63,0 +64,4`（T-D2 通则）、`-67 +71`、`-76 +80`、`-131 +135`、`-175 +179`、`-184 +188`、`-208 +212`（T-D3 六条产出项）

**T-D2 实际文本（无判据可跑，本节点直读，见「说明」二）**：

- `grep -n '通用失败去向' .themis/spec/flow.md` → 第 `31` 行
- `sed -n '64p;66p' .themis/spec/flow.md` → 两段通则原文

**第 `133` 行 lint 告警归属（见「说明」三）**：

- `sed -n '133p' .themis/spec/rules.md` → §11 的「判定者」项
- `git diff -U0 .themis/spec/rules.md | grep '^@@'` → 唯一 hunk 起于第 `185` 行，**第 `133` 行不在任何 hunk 内**

**追溯链**：

- `grep '^### SPEC-' specify.md | grep -v '\[横切\]' | sed 's/^### //' | sed 's/ .*//' | sort -u` → `5` 条（`001`–`005`）
- `grep -o 'SPEC-PJUDGE-[0-9]\{3\}' task/detail.md | sort -u` → `6` 条（`001`–`006`）
- `comm -23 /tmp/up2.txt /tmp/down2.txt | wc -l` → `0`
- `grep -n '^### T-D' task/detail.md` → `3` 条（`T-D1`、`T-D2`、`T-D3`）

**`product-judge` 改动前在仓库的分布（见「说明」四）**：

- `git grep -c 'product-judge' HEAD -- '.themis/workspace/spec/2026-08-29-template-strip-prescriptions/*'` → `5` 个文件、合计 `9` 处

**`rules.md` §13 列表连续性（见「说明」五）**：

- `sed -n '186,192p' .themis/spec/rules.md | cat -A` → 第 `190` 行为空行

## 结论

**passed。**

`specify.md` 的六条判据 `SPEC-PJUDGE-001` 至 `006` **全部满足**，每条均由本节点独立实跑核实，未依赖 `impl/detail.md` 的任何自述。`rules.md` §12 环 2、环 3 的差集均为空。改动面未超出 R3 批准范围，`go test ./...` 无失败，既有十二节一字未动。

**该结论的覆盖范围，如实写明**：它证明的是**六条判据所断言的东西成立**，不等于「本 step 的目标已达成」。二者的差距见「说明」一与二——**本 step 的头号交付物 T-D2 不在这六条的覆盖内**，其是否做到由人工验收直接读文本判定（R3 已取 (a)）。

## 说明

### 一、`SPEC-PJUDGE-003` 无证明力——实测确认，非照抄自述

**本节点用 `git show HEAD:` 取出改动前的 `flow.md` 另存，跑同一对命令**，结果：

| 命令 | 改动前 | 改动后 |
| --- | --- | --- |
| `grep '^- \*\*产出\*\*' … \| grep -c '\.md'` | `14` | `14` |
| `grep -c '^- \*\*产出\*\*' …` | `14` | `14` |

**两条恒为 `14`，改动前即已满足。** 该判据**证不出本 step 做了任何事**——它是一条防回归断言（`task/detail.md`「判据到任务的映射」亦如此标注），却被 `specify.md` 列在**不带 `[横切]` 标识**的条目中，因而计入 §12 的上游集合并要求被任务承担。

**本节点如实报出，不作任何改动**：不改判据、不改被测文件。**这与 `SPEC-PJUDGE-006` 的处理不一致**——`006` 同为防回归而带了 `[横切]`，`003` 同为防回归而未带。**是否给 `003` 补 `[横切]` 属抽象设计的判断，超出本节点权限**，交人工验收裁定。

### 二、T-D2 的实际文本——本节点直读并原样报告

**本 step 的头号交付物 T-D2（`flow.md` 加判定者通则）没有任何一条判据断言它**（`design.md` 取舍一、`task/detail.md`「反向」均已标出，R3 取 (a) 并裁定「下一份 spec 补齐」）。**本节点无判据可跑，改为直接读文件并报告所见文本。**

`flow.md`「通用失败去向」节（第 `31` 行起）内，第 `64` 行与第 `66` 行确有两段新增文字，`git diff` 证其为本次新增（hunk `@@ -63,0 +64,4 @@`）。**实际文本如下，逐字照录**：

> **产出项的判定者**：以下十四个节点各自的「产出」项，**一律由 `product-judge` 判定其是否真的产出**——文件在不在、固定小节齐不齐。判什么、何时判、交接什么、报告写进哪里，全部见 `rules.md` §13，**本处不复述**。

> **本条与上一条的分工**：上一条查的是**闸门有没有结论**（`state.md` 读得出），本条查的是**该产出的工件有没有产出**。两者可以互不相干地失效——闸门结论齐备而工件缺失，上一条一次也拦不住。**本条的设立即源于此**：实测同型遗漏 `8` 次、分布在五个实例，**判据一次都没抓到过**，判据只验条目内容，不验工件本身在不在。

**本节点对这两段的核对结论**：位置在「通用失败去向」节内、紧接「前置的可查形态」条之后，与 `design.md` 决策一所定落点一致；**未逐节点重复十四行**（`grep -A1 '^- \*\*产出\*\*' … | grep -c '判定者'` 改动前后皆为 `0`，与决策一预告的后果一致）；规则全部指向 `rules.md` §13，`flow.md` 内未写判定规则本身。

**本节点只报告所见，不代人工闸门判定 T-D2 是否合格**——R3 已裁定该项由所有者在验收时直读判定。

### 三、第 `133` 行 lint 告警的归属——`git diff` 已验，实现者的说法属实

`rules.md` 第 `133` 行是 §11 的「判定者」项。**该文件的 `git diff` 只有一个 hunk，`@@ -184,0 +185,26 @@`**，即在文件末尾纯追加 `26` 行；第 `133` 行远在其前，**不在任何 hunk 内，本次一字未改**。

**结论：「非本次引入」属实。**

**须同时说明本节点核不到的部分**：仓库内**没有** markdownlint 配置文件，Go 代码中也搜不到 `MD032`（`grep -rn 'MD032' --include=*.go .` 无命中）。**因此本节点能证的是「该行非本次改动」，不能独立复现该告警本身**——告警来自编辑器侧，不在仓库可跑的检查里。

### 四、`design.md`「事实依据」的一处数字不实

`design.md` 第 `109` 行写「`product-judge` 在仓库的现存处数：`0`」。**实测不成立**：`git grep -c 'product-judge' HEAD -- '.themis/workspace/spec/2026-08-29-template-strip-prescriptions/*'` 在**改动前的 HEAD** 即命中 `5` 个文件、合计 `9` 处（`intent-review.md` `4` 处、`state.md` `1` 处、`step1/design-review.md` `2` 处、`step1/design.md` `1` 处、`step1/scope.md` `1` 处）。

**该行同时是 §7 的一处形式违反**：`design.md`「事实依据」其余各条均附了产出该数字的命令，**唯独这一条只给数字不给命令**——§7 要求每个数字都记明它是哪条命令跑出来的。

**影响评估**：该数字**不是任何一条判据**，也未被 T-D1/T-D2/T-D3 依赖，**不影响本次 passed 结论**。如实报出，处置交人工验收。

### 五、§13 的列表被一个空行截断，与「四项形态与 §1–§12 同构」有出入

`sed -n '186,192p' .themis/spec/rules.md | cat -A` 显示 **第 `190` 行是空行**，落在「拒绝条件」项与「判定者」项之间，**把 §13 的四项列表截成 `3` 项 + `1` 项两个列表**。§12（第 `141`–`147` 行）等既有各节的四项均连续无空行。

**这一处是本次新增引入的**（在 hunk `@@ -184,0 +185,26 @@` 内），**与「说明」三所述第 `133` 行的既有告警不是同一处**，且恰是同一类（列表被空行截断）问题。

**影响评估**：`design.md` 决策二要求 §13「与 §1–§12 同构」，本处严格说未完全同构；但**它不被任何一条判据断言**，且为纯排版层面、不改变任何一项的文字与效力。**不构成 failed 的理由**，如实记录，处置交人工验收。

### 六、本节点未做与不得做的事

- **未改动任何被测文件**（`.themis/spec/rules.md`、`.themis/spec/flow.md`）、**未改动任何判据**（`specify.md`）、**未改动任何上游工件**。`git status --short` 中两份控制面文件的改动全部来自 impl/detail 节点，本节点零贡献。
- **未创建分支、未提交、未 push。**
- **未使用 `themis verify`**——已知其对 BRE 分组给静默错值，全部判据以 shell 实跑。
- **未以文档、Agent 自述或文件存在判定通过**：`impl/detail.md` 报出的每个数字都被本节点用同一命令重跑，结果一致；不一致或无法复现的（「说明」三的 lint 告警、「说明」四的 `0`）已单独列出。
