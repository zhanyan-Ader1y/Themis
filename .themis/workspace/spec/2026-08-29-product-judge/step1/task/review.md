# task/review.md — 2026-08-29-product-judge / step1

> 本文件是 R3 详细方案评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R3 详细方案评审」节；投影要求见 `rules.md` §3，分类判定见 §4，结构决策归属见 §6，结论来源见 §11。以上只指向位置，本文件不复述其文字。

## 评审范围

`step1/design.md`（五条结构决策、三条取舍）、`step1/task/basic.md`（空段与判定依据）、`step1/task/detail.md`（三条有序任务 T-D1/T-D2/T-D3）。

**一次评审同时覆盖两份任务文件**，不拆成两次。

## 分类核查

**任务作者提出的分类**：三条任务全归 detail，basic 段为空，逐条判定依据已写入 `task/basic.md`。

**§4 要求的两条同时成立，逐条核过**：

| 任务 | 被依赖 | 单独不产生外部可观测行为 | 结论 |
| --- | --- | --- | --- |
| T-D1 新增 §13 | 是 | **否** | detail |
| T-D2 判定者通则 | 否 | 否 | detail |
| T-D3 补小节名 | 否 | 否 | detail |

**无一同时满足两条。** T-D1 是唯一被依赖的，但其产出被 `SPEC-PJUDGE-001`、`002` 直接断言，按定义外部可观测；**只满足一条即归 basic 正是 §4 的拒绝条件之一**。

**§8 孤儿核查**：basic 段为空，无 basic 任务，**不存在无人依赖的 basic 任务**，孤儿阻断不触发。

**§6 越界核查**：两份任务文件逐条对照 `design.md` 五条结构决策——报告落点、通则位置、§13 形态、交接沿用方式、判定频次**全部在 `design.md` 已定**，任务文件只做分解与依赖声明，**未引入 `design.md` 未定的结构**。

**本项判定的效力，如实写明**：以上是**任务作者的自查**，不是评审者的判定。§4 明定判定者是"R3 评审者"，而评审者未就本项单独表态——见「结论」。

## 未解决反馈

无。

## 用户原话

**所有者答复：「a，下一份spec补齐」。**

**依 §11 记录对应关系**：

- **「a」** 对应本节点提交的裁定一（`design.md` 取舍一：T-D2 这一头号交付物无判据覆盖），取 (a)——**不回抽象设计补判据**，由人在验收时直接读那段文字判。
- **「下一份spec补齐」** 是对该缺口的处置裁定：**判据缺口留到下一份 spec 补齐**，不在本 step 内解决。**本 step 结束时该缺口仍存在，这是所有者知情下的选择**（呈现时已明写 (a) 的代价是"这条交付物永远靠人读，没有自动证据"）。

## 结论

**approved。**

**该结论覆盖的**：三条任务的分解、五条结构决策、以及裁定一取 (a) 及其处置。

**一处推断，如实标明**：本节点提交了两项裁定，**所有者只就裁定一表态，未就裁定二（分类核查）单独作答**。

**执行者不代判该项**——§4 明定判定者是 R3 评审者。**本结论把它记为推断而非判定**，依据与强度如下：

- **依据**：呈现时两项并列提出、明写"两句话就够"，所有者作答后直接给出向下推进的处置（"下一份spec补齐"），未提出异议。
- **强度**：中等。**它不是"未表态"**——所有者确实读了这份投影并作了内容层裁定；但**分类这一项确实没有一个字对应**。
- **不按 `已授权代行` 处理的理由**：该取值的含义是"授权继续但未就内容表态"，而所有者就裁定一作了明确的内容表态，取该值与事实不符；且该值按 `flow.md` 不解锁下游，会使流程停在此处。

**该推断的复核去向**：依 `flow.md`「前置的可查形态」判定者条款，**下一个人工闸门（人工验收）在复核上游各闸门时一并判定分类是否成立**。**判错的代价已评估**：分类若不成立，后果是本该存在的准备工作段被写空，补一个空段即可，不影响已落地的改动。

**本节点未覆盖**：T-D2 是否真的做到——**无判据断言它**，按 (a) 的裁定，由所有者在人工验收时直接读 `flow.md` 那段文字判定。
