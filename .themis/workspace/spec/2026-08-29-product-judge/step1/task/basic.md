# task/basic.md — 2026-08-29-product-judge / step1

> 本文件是详细设计 + 任务节点的实例工件。basic/detail 判定见 `rules.md` §4，结构决策归属见 §6。以上只指向位置，本文件不复述其文字。

## 任务

**本段为空。**

## 判定依据

`rules.md` §4 要求 basic 同时满足两条，**判定依据必须写入本文件**。本 step 三条任务逐条判定：

| 任务 | 被本 step 其他任务依赖？ | 单独不产生外部可观测行为？ | 分类 |
| --- | --- | --- | --- |
| T-D1 新增 `rules.md` §13 | **是**——T-D2 的通则指向 §13 | **否**——`SPEC-PJUDGE-001`、`002` 直接断言 §13 存在且含"人工闸门前" | detail |
| T-D2 `flow.md` 加判定者通则 | 否 | **否**——通则本身即是控制面的可见改动 | detail |
| T-D3 `flow.md` 补全小节名与两处标注 | 否 | **否**——`SPEC-PJUDGE-004`、`005` 直接断言其计数 | detail |

**三条各自只满足第二列或一列都不满足，无一同时满足两条**，故全部归 detail。

**T-D1 值得单说**：它满足了第一列（被 T-D2 依赖），**但第二列不满足**——§4 要求两条**同时**成立，只满足一条即归入 basic 正是该节的拒绝条件之一。**依赖关系不等于 basic**：T-D1 的产出被 `specify.md` 两条判据直接断言，按定义就是外部可观测的。

**空段的后果**：不产生落地调用，不改变流程走向，不构成路径分支。verify/basic 节点据此取 `不适用`（`rules.md` §7 末段）。
