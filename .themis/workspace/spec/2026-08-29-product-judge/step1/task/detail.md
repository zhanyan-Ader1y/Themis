# task/detail.md — 2026-08-29-product-judge / step1

> 本文件是详细设计 + 任务节点的实例工件。任务只做分解与依赖声明，**不得引入 `design.md` 未定的结构**（`rules.md` §6）。以上只指向位置，本文件不复述其文字。

## 有序任务

### T-D1 — `rules.md` 新增 §13，定义 `product-judge`

**依赖**：无（本 step 第一条）。

**落点**：`.themis/spec/rules.md`，接在 §12 之后，**取既有十二节的四项形态**（`design.md` 决策二）。

**须写入的内容**：

1. **四项**：适用节点 / 判据 / 拒绝条件 / 判定者。
2. **判什么**——两项：该节点「产出」项所列的文件是否存在；该文件是否含「产出」项所列的固定小节。**两项都不需要读懂内容。**
3. **判定时机与频次**：每个人工闸门前各一次；每 step `3` 次（R2、R3、验收前），每 spec 另 `1` 次（R1 前）（`design.md` 决策五）。
4. **交接**：**沿用 §7 的四项最小集，不重述其文字**，只写第 1 项在本节填什么（`design.md` 决策三）。
5. **报告落点**：`judge.md` / `step<N>/judge.md`，追加写入不覆盖，每次一节「第 N 次 — <闸门名>」（`design.md` 决策四）。
6. **能力边界如实写明**：判不了报告是否敷衍；`Intent.md` 与 `QA.md` 只判存在性（`design.md` 取舍二、取舍三）。

**对应判据**：`SPEC-PJUDGE-001`（`grep -c '^## §13'` → `1`）、`SPEC-PJUDGE-002`（§13 内含"人工闸门前"）。

**不得做**：不得在 §13 内重述 §7 交接四项的文字；不得援引 `template.md` 作为任何小节名的事实来源（ADR）。

### T-D2 — `flow.md` 加一条判定者通则

**依赖**：T-D1（通则指向 §13，§13 须先存在）。

**落点**：`.themis/spec/flow.md`「通用失败去向」节内，与 `state.md` 更新要求、闸门强制通则同处（`design.md` 决策一）。

**须写入的内容**：十四个节点的「产出」项由 `product-judge` 判定；判什么、何时判、交接什么一律指向 `rules.md` §13，**本处不复述**。

**对应判据**：**无**。这是 `design.md` 取舍一标出的缺口——**本 step 的头号交付物没有任何一条判据断言它**，交 R3 判定是否需要回抽象设计补一条。

**不得做**：不得逐节点重复十四行判定者（`.themis/AGENTS.md` 禁止同句存两份）；不得在 `flow.md` 内写判定规则（那属 `rules.md`）。

### T-D3 — `flow.md` 补全四条小节名、标注两条无固定小节名

**依赖**：无（与 T-D1、T-D2 互不依赖，排在末位只为便于一次核计数）。

**落点与逐条改法**：

| 节点 | 现状 | 改为 |
| --- | --- | --- |
| 详细设计 + 任务 | `design.md` 无小节名 | 补 `架构与边界 / 结构决策 / 取舍 / 事实依据` 四节 |
| impl/detail | 只写"四节" | 补 `执行身份 / 实际改动 / 与批准范围的偏差 / 命令记录` |
| verify/detail | 只写"五节" | 补 `执行身份 / 断言与实际结果 / 命令证据 / 结论 / 说明` |
| 摘要 | `summary.md` 无小节名 | 补 `交付摘要 / 绑定的验收结论 / 中性工件说明` 三节 |
| Intake | `Intent.md` 无小节信息 | 标明**无固定小节名**，只判存在性 |
| 追问 | `QA.md` 无小节信息 | 标明**无固定小节名**，只判存在性 |

**四条小节名的来源**：`design.md`「事实依据」的核对表——**核自三个已闭合实例的实际工件，非 `template.md`**。

**对应判据**：`SPEC-PJUDGE-004`（带 ` / ` 的产出项 `≥11`，落地前实测 `7`）、`SPEC-PJUDGE-005`（`无固定小节名` 处数 `≥2`，落地前实测 `0`）、`SPEC-PJUDGE-003`（十四条仍各指名 `.md`，**改动前后恒为 `14`**）。

**不得做**：改写产出项时不得删掉原有的"以及实际代码改动""绑定实际交付"等既有语义；不得让 `Intent.md`、`QA.md` 两行出现 ` / `（会虚增 `004` 的计数）。

## 判据到任务的映射

| 判据 | 承担任务 |
| --- | --- |
| `SPEC-PJUDGE-001` 定义 `product-judge` | T-D1 |
| `SPEC-PJUDGE-002` 判定时机与频次 | T-D1 |
| `SPEC-PJUDGE-003` 十四条可判 | T-D3（防回归性质，改动前后恒 `14`） |
| `SPEC-PJUDGE-004` 补全四条小节名 | T-D3 |
| `SPEC-PJUDGE-005` 两条只规定不变部分 | T-D3 |
| `SPEC-PJUDGE-006` [横切] 防回归 | 全部三条共同不得违反 |

**反向**：**T-D2 无判据承担**——见 `design.md` 取舍一。
