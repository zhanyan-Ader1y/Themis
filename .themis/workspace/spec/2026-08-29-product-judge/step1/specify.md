# specify.md — 2026-08-29-product-judge / step1

> 本文件是抽象设计（specify）节点的实例工件。本节点的前置闸门与产出见 `flow.md`「抽象设计（specify）」节；`[basic]` 见 `rules.md` §5，`[横切]` 见 §12。以上只指向位置，本文件不复述其文字。
>
> **本文件的判据一律按记法书写，并由本节点当场实跑。**

## 行为条目

### SPEC-PJUDGE-001

`rules.md` 必须新增一节，定义 `product-judge` 的职责、判据、拒绝条件与判定者。

**验收判据**：

`grep -c '^## §13' .themis/spec/rules.md` → `1`

**依据**：所有者裁定 `product-judge` 是一个新的子代理角色；`rules.md` 十二节的四项形态是既有先例。

### SPEC-PJUDGE-002

该节必须写明**判定时机为每个人工闸门前**，并写明频次。

**验收判据**：

`awk '/^## §13/,0' .themis/spec/rules.md | grep -c '人工闸门前'` → `≥1`

**依据**：追问第 1 轮取 (b)。**频次须写准**——每 step `3` 次（R2、R3、验收前）、每 spec 另有 `1` 次（R1 前），**因 `flow.md` 第 `29` 行写明 R1 是 spec 级**。

### SPEC-PJUDGE-003

`flow.md` 的十四条「产出」项必须都能被判——**文件存在性全覆盖，小节齐备按各节点已定的小节名判**。

**验收判据**：

`grep '^- \*\*产出\*\*' .themis/spec/flow.md | grep -c '\.md'` → `14`

`grep -c '^- \*\*产出\*\*' .themis/spec/flow.md` → `14`

**依据**：`scope.md` 事实（1）——十四条无一例外指名了 `.md` 文件。

### SPEC-PJUDGE-004

原先只给数量或未给小节名的六条「产出」项中，**有稳定实践可依的四条必须补全小节名**。

**验收判据**：

`grep '^- \*\*产出\*\*' .themis/spec/flow.md | grep -c ' / '` → `≥11`

**依据**：本节点从**已闭合实例的实际工件**核出（**非从 `template.md`**，依 R1 第 2 轮的效力边界）：

| 工件 | 三个实例的共有小节 | 稳定？ |
| --- | --- | --- |
| `design.md` | 架构与边界 / 结构决策 / 取舍 / 事实依据 | **是** |
| `impl/detail.md` | 执行身份 / 实际改动 / 与批准范围的偏差 / 命令记录 | **是**（与 `flow.md` 已写的"四节"吻合） |
| `verify/detail.md` | 执行身份 / 断言与实际结果 / 命令证据 / 结论 / 说明 | **是**（与"五节"吻合） |
| `summary.md` | 交付摘要 / 绑定的验收结论 / 中性工件说明 | **是** |

**四条各自在三个实例中完全一致**，补全即是把既有实践写进契约。

### SPEC-PJUDGE-005

`Intent.md` 与 `QA.md` **无稳定实践**，其「产出」项只规定**实际不变的那部分**，不得凭空规定。

**验收判据**：

`grep -c '无固定小节名' .themis/spec/flow.md` → `≥2`

**本条判据初稿恒真，落地前实跑当场订正。** 初稿写作 `awk '/^## Intake/,…' | grep -c '来源引用'` → `≥1`，**实跑得 `2`**——Intake 节的「产出」项本就写着"`Intent.md` 的来源引用小节获得初始条目"，**与本 step 要写的东西无关**。判据因此在改动前即已满足。

改为断言本 step 将新增的确切措辞（两处「无固定小节名」标注），落地前实跑 `0`。**第 `35` 次同型实发。**

**依据**：本节点实测三个实例——

- **`Intent.md` 有两套并行写法**：其一为「问题 / 期望结果 / 核心链路 / 范围与非做 / 约束 / step 分解 / 来源引用」，其二为「触发 / 现状 / 实测 / 收敛主张 / 拟定的 step 分解 / 来源引用」。**逐字共有的只有「来源引用」**。
- **`QA.md` 只有「第 N 轮」是共有的**，其余随当轮内容而变。

**这两条不补全小节名，是本节点据实测作出的收窄**：**凭空规定一套会使既有实例批量不合规**，而它们当时都通过了验收。**只规定实际不变的那部分。**

### SPEC-PJUDGE-006 [横切]

改动不得使 `rules.md` 既有十二节的判据改变，也不得使既有测试失败。

**验收判据**：

`go test ./... 2>&1 | grep -c '^FAIL'` → `0`

**依据**：防回归。**本条带 `[横切]` 标识**——验证对象是既有十二节与既有测试，不是本 step 的产出。

## 来源覆盖

| 条目 | 覆盖的达成状态 | 事实来源 |
| --- | --- | --- |
| 001 | 定义 `product-judge` | 用户确认#"一个新的子代理角色" |
| 002 | 判定时机 | 用户确认#追问第 1 轮取 (b) |
| 003 | 十四条可判 | 代码#`scope.md` 事实（1） |
| 004 | 补全四条 | 用户确认#追问第 2 轮取 (a)；代码#本节点从三个实例核出的小节表 |
| 005 | 两条只规定不变部分 | 代码#本节点实测的两套并行写法 |
| 006 | 防回归（横切保护，非达成状态之一） | 代码#既有十二节与既有测试 |

**`[basic]` 标识：六条均不带。**

## 本节点标出、须 R2 判定的一处

**追问第 2 轮裁定"补全六条"，本节点实测后只补四条。**

**收窄的依据是实测**：`Intent.md` 与 `QA.md` **没有稳定实践可写进契约**——`Intent.md` 有两套并行写法、逐字共有的只有「来源引用」；`QA.md` 只有「第 N 轮」共有。

**凭空规定一套的后果**：**既有实例会批量不合规**，而它们当时都通过了验收。**那等于用新规则追溯判既往为错**，与「不改证据」三次裁定的方向相悖。

**本节点不自行认定该收窄成立**——所有者裁定的是"六条"，实测支持"四条 + 两条只规定不变部分"。**交 R2 判定。**
