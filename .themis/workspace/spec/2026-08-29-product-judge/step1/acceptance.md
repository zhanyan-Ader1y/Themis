# acceptance.md — 2026-08-29-product-judge / step1

> 本文件是人工验收节点的实例工件。本节点的前置闸门与产出见 `flow.md`「人工验收」节；孤儿阻断见 `rules.md` §8，结论来源见 §11，追溯链环 4 在本节点检出见 §12。以上只指向位置，本文件不复述其文字。

## 交付视图

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**做完了什么**：给流程里十四份"该产出的文件"配了检查者，新增一节规定它判什么、何时判、判不了什么；六份产出项补全小节名或标明本来就没有固定小节。改两份控制面文件，零代码改动。

**呈交所有者直读判定的**：`flow.md` 新增的判定者通则原文，逐字照录于投影中——**该项无任何判据覆盖，R3 已裁定取 (a) 由所有者直读**。

**呈交裁定的第二项**：验证给出 `passed` 之后实现者又改了两处（均为验证角色报出的问题），**是否需要重跑验证**。

**落地期改变原定方向的一处**：`flow.md` 的 `impl/basic` 节「产出」项与同节正文相互矛盾——十一个实例中 `7` 个没有 `impl/basic.md`，那是正确状态。不处理则新规则上线即对该七例全部误报。已加拒绝条件并回填 `design.md`。

## 阻断核查

**依 §8，本节点引用 `verify/detail.md` 的结论据以阻断，不重新做代码层判定。**

**孤儿阻断：不触发。** `task/basic.md` 的「任务」小节为空（`verify/basic.md` 已做空段代码层断言，条目数实跑 `0`），**不存在 basic 改动，因而不存在无消费者的 basic 改动**。

**追溯链环 4（§12，本节点检出）**：本文件引用了 `verify/detail.md` 的结论（见上一段与「结论」节），**环 4 只检出"是否引用"，不检出"引用是否覆盖全部断言"**——按 §12 该环不取集合或计数形态。**引用成立。**

**verify/detail 的上游三环**：环 2（`specify.md` → `task/detail.md`）与环 3（`task/detail.md` → `verify/detail.md`）差集均为 `0`，由验证角色实跑，本节点引用不复跑。

**前置闸门核查**：`verify/detail.md` 结论为 `passed`。**该结论有一处已披露的 stale**——见「结论」。

## 用户原话

**所有者答复：「接受」。**

**依 §11 记录对应关系**：该答复对应本节点投影末句"**接受交付吗？裁定二（要不要重跑验证）请一并给一句**"，取"接受"。

## 结论

**accepted。**

**该结论确定覆盖的**：本 step 的全部交付——`rules.md` 新增 §13、`flow.md` 新增判定者通则与六条产出项改写，以及投影中逐字呈交所有者直读的 T-D2 原文。

### 一处推断，如实标明

**投影同时提交了两项，所有者只就"接受交付"作答，未就裁定二（是否重跑验证）单独表态。**

**执行者不代判**——但把它读作"接受覆盖裁定二"，依据与强度如下：

- **依据**：投影**显式把两项捆在同一问**（"请一并给一句"），且 stale 的确切范围就写在同一屏——所有者是在该披露在场的情况下作出接受的。
- **强度**：**高于 R3 那次推断**。R3 的两项各自独立，本次投影把两项写成同一个问句。
- **不按 `已授权代行` 处理的理由**：所有者作出的是内容层的接受表态，取该值与事实不符，且该值不解锁摘要节点。

**该 stale 的实质，复述以备复核**：`passed` 写出后实现者改了两处，**均为验证角色自己报出的问题**（`design.md` 一处数字不实、§13 四项被空行截断）。**六条判据由实现者复跑，值与验证角色所得完全一致**；受影响的只是验证工件内的行号引用（`rules.md` `210`→`209` 行、diff hunk `+185,26`→`+185,25`）。**该复跑出自实现者本人，正是身份独立要求所不采信的那一类**——故上面把它记为推断而非判定。

### 本次验收顺带完成的一项上游复核

**R3 的分类核查曾记为中等强度推断**（所有者当时未就分类单独作答）。依 `flow.md`「前置的可查形态」，由本节点复核：**basic 段为空成立**——`task/basic.md` 条目数实跑 `0`，三条任务无一同时满足 §4 的两个条件，`verify/basic.md` 已据此取 `不适用`。**该推断经复核成立，转为已判定。**

### 本次验收未覆盖

1. **`SPEC-PJUDGE-003` 的判据缺陷**——改动前后恒为 `14`，证不出本 step 做了什么；且它与 `006` 同为防回归却未带 `[横切]`。**按所有者「下一份 spec 补齐」的裁定留到下一份 spec。**
2. **T-D2 无判据这一结构性缺口本身**——本次由所有者直读放行，**下一份 spec 补判据**。
3. **`product-judge` 无机器兜底**——它是子代理，一次派发是否真的发起，当前强制水平下无拦截点。`intent-review.md` 已列为未覆盖项。
4. **既有实例是否符合补全后的小节要求**——R1 第 2 轮已裁定不追溯核查。
5. **`rules.md` 第 `133` 行的既有 lint 告警**——非本次引入，`SPEC-IMPL-001` 禁无关重构。
