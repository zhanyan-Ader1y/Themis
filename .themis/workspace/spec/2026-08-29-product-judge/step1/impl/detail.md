# impl/detail.md — 2026-08-29-product-judge / step1

> 本文件是 impl/detail 节点的实例工件。本节点的前置闸门与产出见 `flow.md`「impl/detail」节；实现须在 R3 批准范围内，结构决策归属见 `rules.md` §6。以上只指向位置，本文件不复述其文字。

## 执行身份

**主会话执行者。** 验证由独立子代理另行承担（`flow.md`「verify/detail」节要求）。

**前置闸门**：R3 approved（`step1/task/review.md`「结论」）；basic 段为空，故本节点前置按 `flow.md`「impl/detail」节取 R3 approved 而非 verify/basic 通过。

## 实际改动

**两个文件，三条任务全部落地。**

### T-D1 — `rules.md` 新增 §13

新增一节 `## §13 product-judge：产出项齐备判定`，取既有十二节的四项形态（适用节点 / 判据 / 拒绝条件 / 判定者），另附五段说明：

- **适用节点**写明频次——每 step `3` 次（R2、R3、验收前），每 spec `1` 次（R1 前）。
- **判据**为两项：「产出」项所列文件是否存在、是否含所列固定小节，两项都不读懂内容。
- **拒绝条件**五条，末条为落地期新增（见「与批准范围的偏差」）。
- **与验证角色的分界**一句：验证看内容对不对，`product-judge` 看产物齐不齐。
- **交接沿用 §7 四项最小集，未重述其文字**，只写第 1 项在本节填什么。
- **报告落点**：`judge.md` / `step<N>/judge.md`，追加不覆盖，每次一节。
- **能力边界三条**：判不了报告是否敷衍；`Intent.md`、`QA.md` 只判存在性；闸门之间的窗口仍会漏。

### T-D2 — `flow.md` 新增判定者通则

在「通用失败去向」节末尾、既有「前置的可查形态」条之后，新增两段：

- **产出项的判定者**——十四个节点的「产出」项一律由 `product-judge` 判定，规则全部指向 `rules.md` §13，本处不复述。
- **与上一条的分工**——上一条查闸门有没有结论，本条查工件有没有产出；两者可互不相干地失效。

**未逐节点重复十四行**（`design.md` 决策一）。

### T-D3 — `flow.md` 六条「产出」项改写

| 节点 | 改动 |
| --- | --- |
| Intake | 追加：`Intent.md` **无固定小节名**，只判存在性 |
| 追问 | 追加：`QA.md` **无固定小节名**，只判存在性 |
| 详细设计 + 任务 | 补入 `架构与边界 / 结构决策 / 取舍 / 事实依据` 四节 |
| impl/detail | "四节" 展开为 `执行身份 / 实际改动 / 与批准范围的偏差 / 命令记录` |
| verify/detail | "五节" 展开为 `执行身份 / 断言与实际结果 / 命令证据 / 结论 / 说明` |
| 摘要 | 补入 `交付摘要 / 绑定的验收结论 / 中性工件说明` 三节 |

**四条小节名核自三个已闭合实例的实际工件，非 `template.md`**（ADR 效力边界）。**既有语义一律保留**——"以及实际代码改动""绑定实际交付"原样留在句中。

## 与批准范围的偏差

### 偏差一：§13 增第五条拒绝条件（已回填 `design.md` 决策六）

**R3 批准的 T-D1 未包含此条。** 落地时发现 `flow.md` 的 `impl/basic` 节「产出」项列出 `impl/basic.md`，而**同节正文写明"basic 段为空时本节点不产生调用"**——只读「产出」项的 `product-judge` 会把这份不该存在的文件判为缺失。

**实测规模**：十一个已走到实现的实例中 `7` 个没有 `impl/basic.md`，全部因 basic 段为空。**不处理则 §13 上线即对这七个全部误报。**

**处置**：增拒绝条件一条并写明判定方法与一处例外（`verify/basic` 空段下仍须产出工件，缺失就是缺失）。**依 §6 已回填 `design.md`**，不作就地决定而不记。

**为什么判为"把 T-D1 写对"而非扩大范围**：T-D1 本就要求写「判据与拒绝条件」，缺这条则该项是错的。

### 偏差二：`rules.md` 第 `133` 行的既有 lint 告警未修

§11 存在与本次 §13 初稿相同的结构问题（说明段落夹在 `拒绝条件` 与 `判定者` 两项之间，触发 `MD032`）。**本 step 只修了自己新写的那处，未动 §11**——`SPEC-IMPL-001` 禁止无关重构，且它落在本 step 边界之外。`git diff` 无该区间 hunk，可证非本次引入。

### 偏差三：verify/detail 给出 `passed` **之后**，实现者又改了两处

**这两处改动都是对验证角色报出问题的处置**，但发生在结论写出之后，**依 `flow.md`「impl/detail」节的失效波及，严格说使 verify/detail 结论 stale**。如实记明，不掩饰。

| 改的什么 | 对应验证报出的 | 改动性质 |
| --- | --- | --- |
| `rules.md` §13 删去「拒绝条件」与「判定者」之间的一个空行 | 「说明」五——四项列表被截成 3+1，与 §1–§12 不同构 | 纯排版，不改任何一项的文字与效力 |
| `design.md`「事实依据」订正「`product-judge` 现存处数 `0`」 | 「说明」四——该数字不实且未记命令 | 订正**实现者自己工件**里的一处不实断言 |

**订正方式如实写明**：把断言范围缩到它真正成立的集合（控制面 `.themis/spec/`，实跑 `git grep -o 'product-judge' HEAD -- .themis/spec | wc -l` → `0`），并补上命令。**不是改数字去迎合原说法**——全仓实际值 `49` 处、`10` 个文件已一并写入 `design.md`，其中五份正是本实例自己的工件。

**stale 的确切范围，逐项列明**：

- **六条判据的结论不受影响**——两处改动后由实现者复跑，`001`→`1`、`002`→`2`、`003`→`14`/`14`、`004`→`11`、`005`→`2`、`006`→`0`，**与验证角色所得完全一致**。
- **受影响的只是行号引用**：`rules.md` 由 `210` 行变 `209` 行，其 `git diff` hunk 由 `+185,26` 变 `+185,25`，§13 内第 `190` 行之后的引用各前移一行。
- **`design.md` 第 `109` 行**的内容已变（原不实断言 + 订正说明）。

**是否需要重新验证，交人工验收裁定。** 实现者的判断是"两处均不触及六条判据所断言的东西"，**但这个判断出自实现者本人，正是身份独立要求所不采信的那一类**——故不自行结案。

### 无其他偏差

三条任务的落点、形态、内容均在 R3 批准范围内。未引入 `design.md` 未定的字段、类型、签名或配置项。**未改动任何既有实例工件**（R1 第 2 轮裁定不追溯核查）。

## 命令记录

**改动面**：

- `git status --short` → 两份控制面文件被修改（`.themis/spec/flow.md`、`.themis/spec/rules.md`），其余为本实例新增工件
- Go 代码改动：`git status --short -- '*.go'` → 空

**六条判据落地后实跑**：

- `SPEC-PJUDGE-001`：`grep -c '^## §13' .themis/spec/rules.md` → `1`
- `SPEC-PJUDGE-002`：`awk '/^## §13/,0' .themis/spec/rules.md | grep -c '人工闸门前'` → `2`
- `SPEC-PJUDGE-003`：`grep '^- \*\*产出\*\*' .themis/spec/flow.md | grep -c '\.md'` → `14`；`grep -c '^- \*\*产出\*\*' .themis/spec/flow.md` → `14`
- `SPEC-PJUDGE-004`：`grep '^- \*\*产出\*\*' .themis/spec/flow.md | grep -c ' / '` → `11`（落地前 `7`）
- `SPEC-PJUDGE-005`：`grep -c '无固定小节名' .themis/spec/flow.md` → `2`（落地前 `0`）
- `SPEC-PJUDGE-006` [横切]：`go test ./... 2>&1 | grep -c '^FAIL'` → `0`

**T-D2 无判据可跑**——`design.md` 取舍一所述缺口，R3 取 (a)，由所有者在验收时直接读 `flow.md` 那两段判定。

**决策一的已知后果实跑核对**：`grep -A1 '^- \*\*产出\*\*' .themis/spec/flow.md | grep -c '判定者'` → 落地前 `0`，落地后仍应为 `0`（通则不紧跟产出项下一行）。**该数字不动是设计后果，非未落地。**
