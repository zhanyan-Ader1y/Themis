# intent-review.md — 2026-08-29-product-judge

> 本文件是 R1 意图评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R1 意图评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：给 `flow.md` 十四个节点的「产出」项配判定者，由 `product-judge` 子代理承担。

**缺口的规模**：`14` 个节点写有「产出」项，**其中带判定者的为 `0`**。实发 `6` 次遗漏、分布在 `4` 个实例；**判据一次都没抓到过**——判据只验条目内容，不验工件本身在不在。

**追问一问已闭合**：取 (b)——每个人工闸门前跑一次。

**执行者呈现时的一处不准确已订正**：说的"`4` 次／step"实为**每 step `3` 次 + 每 spec `1` 次**（R1 是 spec 级）。**实际成本低于所有者作答时所设想的。**

**未覆盖**：`product-judge` 本身无机器兜底——它是子代理，与验证角色同处境。

## 未解决反馈

无。

## 用户原话

**所有者答复：「b」。**

**依 §11 记录对应关系**：该答复对应 `QA.md` 第 1 轮问 1，取选项 (b)——每个人工闸门前派发一次。

**该答复同时构成对需求本身的批准**：选择运行时机以需求成立为前提。**本节点如实标明这是一处推断**——但其强度较高：**所有者此前已单独指示"产出的判定者通过 `product-judge` 进行判定"**，本轮是在该指示之下选具体时机。

## 结论

**approved。**

确认需求本身，以及 step 分解为一个 step。

**本次批准同时确认的一项裁定**：`product-judge` **在每个人工闸门前派发一次**——每 step `3` 次（R2、R3、验收前），每 spec 另有 `1` 次（R1 前）。

**该裁定的依据记明**：六次遗漏中 `3` 次是 `state.md`，而它按 `flow.md` 第 `35` 行**每个节点后都要更新**——**持续处于风险中，只在链尾查一次会让它错很久**。而每节点跑一次（`14` 次／step）的成本与收益不成比例。

**本次批准未覆盖**：

1. **`product-judge` 无机器兜底**——它是子代理，判定仍靠一次派发是否真的发起，与验证角色同处境。
2. **闸门前跑仍会漏掉"闸门之间"的窗口**——遗漏可存活到下一个闸门，只是窗口从十来个节点缩短到几个。
