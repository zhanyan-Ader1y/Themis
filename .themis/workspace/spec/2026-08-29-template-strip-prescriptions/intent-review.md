# intent-review.md — 2026-08-29-template-strip-prescriptions

> 本文件是 R1 意图评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R1 意图评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：按 ADR 清掉 `template.md` 里不该住在那儿的规定性内容。

**范围比 ADR 写的小，因为核实推翻了一条**：三行规定里两行 §12 已完整写有，处置是删除而非搬家——**照 ADR 字面执行会制造重复，而重复正是上上个需求刚治过的问题**。

**执行者改了自己刚写的 ADR 两处数字**：规定性行数写的 `14` 实为 `5`；「实例目录树」那节实为 `0` 行、本就是纯示例。

**未覆盖**：`flow.md` 各节点「产出」项仍无判定者，属第 2 件事。

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准，产出的判定者通过 product-judge 进行判定」。**

**依 §11 记录对应关系**：该答复含两项——

- **「批准」**：对本需求与 step 分解的批准，构成本节点结论。
- **「产出的判定者通过 product-judge 进行判定」**：这是对**第 2 件事**（`flow.md` 各节点「产出」项配判定者）的指示，**不属本需求范围**——本需求只剥离 `template.md` 的规定性内容。

**本节点不把第二项读进本需求的范围**：它指向的是所有者此前排定的"先 1 后 2"中的第 2 件，**在本 spec 内落地会使范围越出 R1 所批准的分解项**。

**`product-judge` 在本仓库零处存在**（`grep -rl` 全仓无命中，`.themis/skills/` 与 `.claude/skills/` 下均无该名）。**执行者未自行定义它，而是当场向所有者澄清其形态**——所有者答：**一个新的子代理角色**，与现有验证子代理并列但职责不同（验证看内容对不对，它看产物齐不齐）。

**该澄清记录在此备查，其落地属第 2 件事，不在本需求范围内。**

## 结论

**approved。**

确认需求本身，以及 step 分解为一个 step。

**本次批准的范围**：`template.md` 剥离规定性内容——两行删除（§12 已有）、一行搬去 §7、两行改措辞、相应位置留指向。

**本次批准未覆盖**：

1. **`product-judge` 的引入**——属第 2 件事，且该名称在本仓库尚不存在，形态待澄清。
2. **`flow.md` 各节点「产出」项配判定者**——第 2 件事本身。
