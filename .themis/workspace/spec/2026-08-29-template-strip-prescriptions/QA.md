# QA.md — 2026-08-29-template-strip-prescriptions

> 追加写入，不覆盖、不回填。每轮记 问 / 答 / 来源三项。"答"栏在获得所有者答复前一律写"待所有者答复"，Agent 不代答（`flow.md`「追问」节，判据见 `rules.md` §2）。

## 第 1 轮

**本轮零提问。**

依 `rules.md` §2 的提问资格判据——**能从既有事实推出答案的一律自己判**。三项候选逐条核实后全部可判，**其中一项在核实中被推翻，收窄了本需求的范围**。

**同时说明为何不属第三条拒绝条件**（目标层 claim 缺合法来源却不问）：本需求的目标层 claim 是"`template.md` 的规定性内容须剥离"，其来源是 `docs/adr/2026-08-29-template-is-skeleton-not-fact-source.md`——**所有者裁定的 ADR，且本节点当场核实了它的数字（并订正了其中一处）**。目标层无悬问。

## 自行判定、未提出的三项

### 一、三行规定怎么处置——核实后发现其中两行不必搬，只需删

**`Intent.md` 原说"三行搬进消费者节"。本节点逐条核实后推翻其中两条**：

| 规定 | 消费者那里已有吗 | 处置 |
| --- | --- | --- |
| 编号只用三种写法 | **已有** —— §12 的「三种写法」表格完整列出三种 | **`template.md` 侧删除，只留指向** |
| 裸数字须紧跟完整编号 | **已有** —— §12 写着"其中裸数字与完整编号的位置关系是把编号与散文里的普通数字分开的唯一依据" | **同上，删除** |
| 数字断言须记明命令 | **没有** —— `rules.md` 全文零处 | **真需要搬**，去 §7（该规定的判定者已在 §7） |

**依据**：本节点实跑三条 `grep`，结果分别为 `4`、`1`、`0`。

**这一条核实改变了范围**：从"三行搬家"变成"两行删除 + 一行搬家"。**若不核实，会把 §12 已有的内容再搬一份进去，制造重复**——而重复正是 `2026-08-28-citation-overlap-check` 刚治过的问题。

### 二、两行"固定记几项"改措辞而非删除——自行判定

**依据**：ADR 明写 `template.md` 提供"文件命名与**结构**示例"。**"某小节固定记哪几项"正是结构**——它描述工件长什么样，不描述怎么判。

**改的是口吻不是内容**：从"固定记四项"改为示例式表述，去掉规定性措辞。

### 三、`template.md` 相应位置留指向——自行判定

**依据**：ADR「已知代价」第 1 项写明规定要"搬到已在消费它的那一节"，而 R1 对上一个（已作废的）需求曾裁定过同类配套——**取消规定后须留索引，否则读 `template.md` 的人会以为那些要求不存在了**。

**该配套在本需求同样成立**，且成本极低（每处一行）。
