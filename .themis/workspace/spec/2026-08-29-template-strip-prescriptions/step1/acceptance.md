# acceptance.md — 2026-08-29-template-strip-prescriptions / step1

> 本节点的前置闸门、阻断核查与结论取值见 `flow.md`「人工验收」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 交付视图

**`template.md` 不再规定任何事，只给命名与结构示例**——落实 `docs/adr/2026-08-29-template-is-skeleton-not-fact-source.md`。

五处改动：两行去掉规定性动词（内容一项未删）、两行删除并指向 §12、一行搬进 `rules.md` §7。**三种写法的表格、记法示例、CLI 比对规则全部保留**——那些是示例。

## 阻断核查

| 核查项 | 结论 | 依据 |
| --- | --- | --- |
| verify/detail 是否 passed | **不阻断** | `step1/verify/detail.md`「结论」判 `passed`，**由独立子代理给出** |
| 五条判据是否逐条有断言 | **不阻断** | 同上，`SPEC-TPLSTRIP-001` 至 `005` 逐条列出实跑值 |
| 孤儿阻断（§8） | **不阻断** | basic 段为空——`grep -c '^### T-B' step1/task/basic.md` → `0` |
| 追溯链环 4（§12） | **不阻断** | 本文件上列各项均**引用** `verify/detail.md` 的结论，未重做判定 |

四项均不阻断。

## 独立验证报出的四处问题及其处置

**本会话逐条核实，全部属实。**

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | §12 仍两处称 `template.md` 是写法的规定源，而本 step 已把那些规定删了——**两边互指，该要求在控制面无归属** | **已修** |
| 2 | 判据 002a 的 `awk` 终止模式处数为 `0`，区间取到文件末尾；**且改动前后恒为 `4`，证不出本 step 做了什么** | **已订正**，第 `34` 次同型实发 |
| 3 | 缺 `state.md`、缺 `verify/basic.md` | **已补齐** |
| 4 | `themis verify` 对 BRE 分组 `\(` `\)` **静默给出错值** | **未修**——CLI 缺陷，不在本 step 范围 |

**第 1 处须单独记明**：**它是本 step 亲手造成的**，而**没有任何一条判据问得到它**——002 只问表格在不在、005 只数节数。**是独立验证读出来的。**

## 须所有者裁定的一处

**子代理判"三行到位、两行没到"，是否接受这三行的现状？**

`specify.md` 已标明"措辞改得对不对"无判据覆盖，R2 批准时亦知悉该盲区。子代理的判断：

- **第 `75`、`83` 行到位**——讲写法的来历、描述表格长什么样。
- **第 `67`、`69` 行只到"去规定性"**——"每个 `T-B<n>` **的四项**"仍是关于文件内容的全称断定。**合乎 `design.md` 已被 R2、R3 批准的判准（去动词、留名词），但未达 ADR 字面标准**（"不作为任何文件内容的事实"）。
- **第 `87` 行最不像示例**——前半句把已搬去 §7 的要求又复述一遍，后半句才指向 §7。**`design.md` 自己写明"指向句只写一层"，这一行同时做了两件事。**

**两条路**：

- **(a) 接受现状**——三行合乎已批准的判准，判准本身经 R2、R3 两道闸门。**代价**：ADR 字面标准未完全达到，`template.md` 仍有三行在陈述"文件内容是什么"。
- **(b) 退回再改**——按 ADR 字面标准继续收紧。**代价**：判准是 R2、R3 批准的，据字面标准推翻它等于事后加严；且"到什么程度算示例口吻"没有可判的边界，可能反复。

**执行者倾向 (a)**，理由：**判准经两道闸门批准，而 ADR 的"字面标准"是本会话事后对它的解读**——以解读推翻已批准的判准，方向不对。**但这不构成推荐**——ADR 是所有者裁定的，其字面含义应由所有者判。

## 用户原话

**待所有者答复。**

**本节点不代行**：`flow.md` 明写人工验收是流程中唯一的授权点。

## 结论

**待所有者答复。**

**执行者的自判**：四项阻断核查均不阻断，`verify/detail.md` 由独立子代理判 `passed`，前置闸门满足，**交付在技术上可接受**。

**须所有者就以下两项表态后，本结论方可写入**：

1. **三行措辞是否接受**（上节 (a)／(b)）。
2. **是否批准本 step 通过验收**。

**本次未覆盖的四项**（无论此后取何结论均成立）：

1. **`themis verify` 对 BRE 分组静默给错值**——CLI 缺陷，另需处置。
2. **工件齐备性仍无判定者**——同型遗漏第三次，属所有者排定的第 2 件事。
3. **「实例目录树」列的 `15` 份文件仍无消费者**——ADR「已知代价」第 2 项。
4. **判据 001 按行号范围取节**——`sed -n '11,102p'` 而文件现为 `100` 行，两次都恰好覆盖到末尾；**文件变长则会静默漏检**。
