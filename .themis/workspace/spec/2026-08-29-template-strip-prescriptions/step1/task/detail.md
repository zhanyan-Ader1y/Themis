# task/detail.md — 2026-08-29-template-strip-prescriptions / step1

> 本文件是详细设计+任务节点的实例工件之一。本节点的前置闸门与产出见 `flow.md`「详细设计+任务」节；每个 `### T-D<n>` 的固定三项见 `template.md`。编号引用写法见 `template.md`「编号引用写法」。以上只指向位置，本文件不复述其文字。

### T-D1 — 两行改措辞

**行为目标**：`template.md` 第 `67`、`69` 行不再含"固定记"这类规定性动词，四项／三项的内容保留。

**做法**：

1. 按 `design.md` 结构决策一的判准——**去掉动词性强制措辞，保留名词性结构罗列**。
2. **内容一项不删**：读者照着写工件时需要知道有哪几项。

**对应 `specify.md` 条目**：判据 SPEC-TPLSTRIP-001、004。

**依赖的基础任务**：无。

### T-D2 — 两行删除，留指向 §12

**行为目标**：「编号引用写法」节不再含"只用下列三种写法"与"裸数字必须紧跟"两行规定，留一句指向。

**做法**：

1. 删两行规定。
2. 补一句"写法的判定与展开规则见 `rules.md` §12"，**只写一层**（`design.md` 结构决策二）。
3. **三种写法的表格保留**——那是示例，不是规定。

**对应 `specify.md` 条目**：判据 SPEC-TPLSTRIP-002。

**依赖的基础任务**：无。

### T-D3 — 一行搬进 §7，「断言形态」节留指向

**行为目标**："数字断言须记明命令"这条要求出现在 `rules.md` §7 判据项；`template.md`「断言形态」节改为指向。

**做法**：

1. **并入 §7 判据项**，与既有的 detail 段断言要求并列（`design.md` 结构决策三）。
2. `template.md` 该节删去首句规定，改为指向 §7。
3. **记法示例、占位路径说明全部保留**（`design.md` 结构决策四）。

**对应 `specify.md` 条目**：判据 SPEC-TPLSTRIP-003、004。

**依赖的基础任务**：无。**但须在 T-D2 之后**——两处都改 `template.md`，先后做便于一次通读。

## 判据 005 无任务承担，理由

`SPEC-TPLSTRIP-005` 带 `[横切]` 标识，验证对象是 `rules.md` 既有十二节与既有测试，**不是本 step 的产出**。

## 执行次序

**T-D1 → T-D2 → T-D3**，三者互不依赖；顺序只为便于一次通读 `template.md`。
