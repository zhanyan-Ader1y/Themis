# design-review.md — 2026-08-29-template-strip-prescriptions / step1

> 本文件是 R2 抽象设计评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R2 抽象设计评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：`template.md` 五行——两行改措辞、两行删除留指向、一行搬去 §7。

**待裁定项**：批准与否。

**单独标出一对相反约束**：判据 001 要规定性措辞归零、004 要四节结构仍在——**只取 001 的最省力做法是把三节删空**，004 是为堵这条捷径而设。**但判据够不到"措辞改得对不对"那一层**，只能由 R2 与验证角色读文字判断。

**一处判据落地前订正**：002b 初稿恒真（`grep '§12'` 实跑得 `2`，与本 step 无关），改为断言指向句的确切措辞后归 `0`。

**关于 `product-judge`**：所有者答复的后半段指向第 2 件事，已按 §11 拆解、不读进本需求范围；其形态已当场澄清并记入 R1 工件。

## 判据与边界的交叉核查（`flow.md` R2 节）

| 本 step 已确认的边界 | 判据是否冲突 | 处置 |
| --- | --- | --- |
| ADR：`template.md` 只提供命名与结构示例 | **不冲突**——本 step 即为落实它 | 无需处置 |
| 验证须由独立子代理承担 | **不冲突**，但本 step 的 verify 须派发 | **照办** |
| `themis` 不在只读白名单 | **冲突**——判据 005 要跑 `go test` | **照旧人工判** |

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影只有一个待裁定项，「批准」一条即完整结论。**一对相反约束与判据的覆盖盲区属主动披露**，所有者知悉后仍批准。

## 结论

**approved。**

五条行为条目获授权，进入详细设计与任务分解。

**本次批准确认的一项**：判据 001 与 004 并存，**且知悉"措辞改得对不对"无判据覆盖**——该层由 R2 与验证角色读文字判断。

**本次批准未覆盖**：`flow.md` 各节点「产出」项配判定者（第 2 件事）；`product-judge` 的引入。
