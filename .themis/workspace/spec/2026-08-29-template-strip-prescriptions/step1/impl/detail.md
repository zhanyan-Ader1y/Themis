# impl/detail.md — 2026-08-29-template-strip-prescriptions / step1

> 本节点的前置闸门、产出与失败去向见 `flow.md`「impl/detail」节。以上只指向位置，本文件不复述其文字。
>
> **本文件不含验证结论。** 五条判据是否满足属 verify/detail，**而该节点须由独立子代理承担**（`rules.md` §7）。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话，分支 `main`，动手前 HEAD `84c4b97`。
- **授权来源**：R3 approved（`task/review.md`「结论」）。basic 段为空，本节点前置闸门取 R3 approved。
- **本节点不承担 verify/detail**：依 `2026-08-28` 生效的条款，验证须派发独立子代理。

## 实际改动

三条任务全部落地，改控制面两份文件的五处。

- **T-D1 — 两行改措辞**（`template.md` 第 `67`、`69` 行）。"固定记四项"改为"的四项"、"固定记三项"改为"的三项"。**四项／三项的内容一项未删**——那是结构信息，读者照着写工件时需要。
- **T-D2 — 两行删除，留指向**（「编号引用写法」节）。删"只用下列三种写法"这句规定，改为"引用……时的三种写法，均来自已完成实例的实际书写"；删"裸数字必须紧跟"整段规定与其后的"为什么要规定"段，改为一句"上表第二、三行里的裸数字紧跟在完整编号之后——**写法的判定与展开规则见 `rules.md` §12**"。**三种写法的表格全部保留**。
- **T-D3 — 一行搬进 §7**。`template.md`「断言形态」首句规定改为"该要求与其判定者见 `rules.md` §7，本节只给记法示例"；`rules.md` §7 判据项并入该要求，与既有的 detail 段断言要求并列。**记法示例、占位路径说明、CLI 比对规则全部保留**。

## 与批准范围的偏差

**无。** 三条任务的做法项逐条落地，未删减、未扩张。

**一处判断如实记明**：T-D2 删"裸数字必须紧跟"那句时，**连同其后的"为什么要规定"整段一并删去**。`task/detail.md` 只写了"删两行规定"，未点名该段。

**判断依据**：该段的内容是"§12 的检出按这三种写法展开编号、写法没规定时扫描会误报"——**那是 §12 的理由，不是 `template.md` 的示例**，留着与 ADR 冲突。**且删后要求仍在 §12**（判据 002a 实测 `4`）。

## 命令记录

- 落地前 HEAD：`84c4b97`。**历史点位**，不按可重跑断言书写。
- 五条判据落地后实跑：001 `0`、002a `4`、002b `1`、003 `1`、004 `4`、005a `12`、005b 的 `FAIL` 数 `0`。
- 控制面四份文件 `themis verify` 报告数：各 `0`。
- `themis overlap .themis/spec` → **未发现需人工复核的重合片段**——**搬进 §7 的那条要求未与 `template.md` 构成重复**。

## 尚未关闭、留给 verify/detail 与人工的

1. **"措辞改得对不对"无判据覆盖**——判据 001 只验规定性词归零，**验不出改后是否真成了示例口吻**。R2 已知悉，该层由验证角色读文字判断。
2. **判据 001 按行号范围取节**——`sed -n '11,102p'`，而剥离后文件行数已变（现为 `100` 行）。**判据 004 是从另一角度的交叉验证**，但两者都不直接验"节的边界对不对"。
3. **「实例目录树」列的 `15` 份文件仍无消费者**——ADR「已知代价」第 2 项记明，本 step 未处置。
