# scope.md — 2026-08-29-template-strip-prescriptions / step1

> 本文件是 step 定界节点的实例工件。本节点的前置闸门与产出见 `flow.md`「step 定界」节。以上只指向位置，本文件不复述其文字。
>
> **本文件的全部数字与存在性断言按 `template.md`「断言形态」记法书写，由本节点当场跑出。**

## 承担的上层分解项

`Intent.md`「step 分解」的唯一一项：三行处置（两删一搬）、两行改措辞、相应位置留指向。

## 本 step 边界

改 `template.md` 五处、`rules.md` §7 一处。**不改 `flow.md`；不引入 `product-judge`**——后者属第 2 件事，R1 已明确不在本需求范围。

## 与其他 step 的关系

**本需求只有一个 step。**

## 会触及或放宽的既有裁定与安全边界

| 既有裁定 | 本 step 是否触及 |
| --- | --- |
| ADR：`template.md` 只提供命名与结构示例 | **本 step 即为落实它** |
| 「不改证据去迎合判据」 | **不触及**——只改控制面，不动任何工件 |
| 验证须由独立子代理承担 | **触及**——本 step 的 verify 须派发。**照办** |
| `themis` 不在只读白名单 | **触及**——判据要跑 `themis verify`。**照旧人工判** |

## 核实到的、影响做法的三处事实

### （1）五行的确切位置与处置，逐行核定

| 行号 | 内容摘要 | 处置 | 依据 |
| --- | --- | --- | --- |
| `67` | `T-B<n>` 固定记四项 | **改措辞** | 属结构描述，`template.md` 本职 |
| `69` | `T-D<n>` 固定记三项 | **改措辞** | 同上 |
| `75` | 编号只用三种写法 | **删，留指向** | §12 已有完整表格 |
| `83` | 裸数字须紧跟完整编号 | **删，留指向** | §12 已明写同义内容 |
| `89` | 数字断言须记明命令 | **搬去 §7** | `rules.md` 全文零处 |

### （2）第 `89` 行是唯一真需要搬的，且它的判定者已在 §7

`template.md`「断言形态」节末尾自带一句"判定者：验证角色在验证时判形态是否合规（`rules.md` §7）"。

**即规定在 `template.md`、判定者指向 §7——正是 ADR 要消除的倒置。** 搬去 §7 后两者合一。

### （3）删除两行不会丢失任何要求

`rules.md` §12 的「三种写法」表格完整列出三种写法及其展开规则；裸数字前提亦已明写。**`template.md` 那两行是重复，删后要求仍在 §12。**

**本节点实跑核对**：§12 内提及三种写法的行数 `4`、提及裸数字位置关系的行数 `1`。

## 本节点新暴露、不在 R1 已批准内容里的一处

**无。** 三处事实均为对 R1 已批准范围的补证据。

## 命令证据

- `template.md` 四节起始行：`grep -n '^## ' .themis/spec/template.md | awk -F: '{print $1}'` → `11`、`42`、`73`、`87`
- 规定性行数合计：`sed -n '11,102p' .themis/spec/template.md | grep -cE '必须|不得|只用|固定记|一律'` → `5`
- §12 内三种写法的行数：`awk '/^\*\*三种写法\*\*/,/^\*\*三种写法的共同前提/' .themis/spec/rules.md | grep -c '完整\|逗号并列\|区间'` → `4`
- `rules.md` 中"数字断言须记明命令"的处数：`grep -c '数字与存在性断言.*记明它是哪条命令' .themis/spec/rules.md` → `0`
