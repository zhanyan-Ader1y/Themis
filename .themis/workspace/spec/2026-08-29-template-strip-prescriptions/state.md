# state.md — 2026-08-29-template-strip-prescriptions

> 各闸门行内格式：`- <节点名>：<结论> — <证据路径>`。结论取值由 `flow.md` 通用状态条款按节点类型固定，本文件不自建约定。

## 当前节点

**step1 的人工验收（等待所有者授权）。** verify/detail 由**独立子代理**判 `passed`，其报出的两处问题已修、两处工件已补。

**本文件是补写的**：产出时机晚于 verify/detail，**由独立验证子代理在核验时报出缺失**。**这是同型遗漏的第三次**，三次都不是判据抓到的——判据只验条目内容，不验工件是否齐备。**该缺口的修复属第 2 件事。**

## 各闸门

### spec 级

- Intake：已走完 — `Intent.md`（**核实 ADR 的数字并订正其两处**：规定性行数写作 `14` 实为 `5`，错因是 `awk` 取节范围时四节输出层层重叠；「实例目录树」一节实为 `0` 行、本就是纯示例）
- 追问：已走完 — `QA.md` 第 1 轮（**零提问**。三项候选全部可判，**其中一项核实推翻了 `Intent.md` 的范围**——原说"三行搬进消费者节"，实测 §12 已完整写有前两行，处置改为删除而非搬家；**照原范围执行会制造重复**）
- R1 意图评审：approved — `intent-review.md`（所有者原话"批准，产出的判定者通过 product-judge 进行判定"。**依 §11 拆解**：前半是对本需求的批准，后半指向第 2 件事、不读进本范围。`product-judge` 全仓零处存在，**执行者未自行定义而是当场澄清**——所有者答：一个新的子代理角色）

### step 级

### step1 — 剥离规定性内容（进行中）

- step 定界：已走完 — `step1/scope.md`（逐行核定五行的处置：两行改措辞、两行删除留指向、一行搬去 §7）
- 抽象设计：已走完 — `step1/specify.md`（五条条目。**判据 002b 初稿恒真并当场订正**——`grep '§12'` 实跑得 `2`，是 `template.md` 本就有的两处引用，与本 step 无关，第 `33` 次同型实发）
- R2 抽象设计评审：approved — `step1/design-review.md`（所有者原话"批准"；确认判据 001 与 004 是一对相反约束并存，**且知悉"措辞改得对不对"无判据覆盖**）
- 详细设计+任务：已走完 — `step1/design.md` 与两份任务文件（四条结构决策；**改措辞的判准是"去掉动词性的强制措辞、保留名词性的结构罗列"**）
- R3 详细方案评审：approved — `step1/task/review.md`（所有者原话"批准"，无待裁定项）
- impl/basic：不适用 — basic 段为空
- verify/basic：不适用 — `step1/verify/basic.md`（**补写**；空段三项无断言对象。未派发子代理并写明理由：派人去验"没有东西可验"不产生证据）
- impl/detail：已走完 — `step1/impl/detail.md`（三条任务落地。**一处判断如实记明**：T-D2 连同其后的"为什么要规定"整段一并删去，而任务只写"删两行规定"——依据是该段属 §12 的理由而非 template 的示例）
- verify/detail：passed — `step1/verify/detail.md`（**由独立子代理写出**。五条判据全满足，两环差集为 `0`。**报出的问题见下**）
- 人工验收：**等待所有者授权**
- 摘要：未开始

## 独立验证报出的问题及其处置

**本会话逐条核实，全部属实。**

| # | 问题 | 处置 |
| --- | --- | --- |
| 1 | **本 step 引入的实质缺口**：§12 仍两处称 `template.md` 是写法的规定源，而 T-D2 已把那些规定删了——**两边互指，"裸数字须紧跟完整编号"在控制面已无规范性归属** | **已修**：书写要求改由 §12 自己规定，`template.md` 单向指向 §12 |
| 2 | **判据 002a 的命令测的不是它断言的东西**：`awk` 终止模式在 `rules.md` 处数为 `0`，区间一路取到文件末尾 `35` 行；且它改动前后恒为 `4`，**证不出本 step 做了什么** | **已订正**：改为直接取三种写法的表格，实跑 `3`。第 `34` 次同型实发 |
| 3 | 缺 `state.md`、缺 `verify/basic.md` | **已补齐**（即本文件与 `step1/verify/basic.md`） |
| 4 | `themis verify` 一处**静默错值**：`grep -c '\(SPEC\)'` shell 得 `4`、CLI 得 `0`，**只有 BRE 分组 `\(` `\)` 出错** | **未修**——属 CLI 缺陷，不在本 step 范围 |

**第 1 处最要紧**：它是本 step 亲手造成的，**且没有任何一条判据问得到它**——002 只问表格在不在、005 只数节数。

## 子代理对"措辞是否真成了示例口吻"的判断

**判据够不到的一层，R2 已知悉由验证角色读文字判断。** 子代理判：**三行到位、两行没到**。

- **第 `75`、`83` 行到位**——讲写法的来历、描述表格长什么样，是真正的示例口吻。
- **第 `67`、`69` 行只到"去规定性"**——"每个 `T-B<n>` **的四项**"仍是关于文件内容的全称断定。**合乎 `design.md` 已被 R2/R3 批准的判准（去动词、留名词），但未达 ADR 字面标准。**
- **第 `87` 行最不像示例**——前半句把已搬去 §7 的要求又复述一遍，后半句才指向 §7；**`design.md` 自己写明"指向句只写一层"，这一行同时做了两件事。**

**该判断不改变 `passed`**，但**是否接受这三行的现状属人工验收的裁定**，本会话不代判。

## 当前性

- `Intent.md`、`QA.md`、`intent-review.md`：current
- `step1` 各工件：current
