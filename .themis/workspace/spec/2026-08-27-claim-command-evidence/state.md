# state.md — 2026-08-27-claim-command-evidence

> 各闸门行内格式：`- <节点名>：<结论> — <证据路径>`。结论取值由 `flow.md` 通用状态条款按节点类型固定，本文件不自建约定。

## 当前节点

**本需求已完成。** 两个大步骤全部走完十四个节点并经人工验收：step1 定下断言形态，step2 落地 `themis` CLI 使该形态可被机器重跑核对。

**交付后仍开着的五项**见 `step2/summary.md` 末节，其中两项须另开需求：`rules.md` §12 的缩写写法缺口（区间与逗号两次实发）、以及本 step 四条判据因 `go` 不在白名单而不受本工具保护。

追问闭合结果：取真机器强制（引入 `themis` Go CLI）、逐条比对输出与断言值、全部数字一视同仁。**问 1 原本可能否决本需求**——该强制项原文断言"提醒对这个盲区无效，必须是机器强制"而本项目无 CLI；所有者裁定引入 CLI，正面回应该断言而非绕开。

**本实例的特殊处境须记明**：本需求要治的是"写数字时不跑命令"，而**本 spec 自身每写一个数字都是一次实测机会**。若期间再次发生，须如实记入本文件与相应工件，不得因"正在治它"而回避记录。

## 各闸门

### spec 级

- Intake：已走完 — `Intent.md`「来源引用」小节（三条命令来源均当场跑出：本轮五次实发落点、§1 零处覆盖转录准确性、现有条款只两处要求实跑）
- 追问：已走完 — `QA.md` 第 1 轮（三问全部获所有者答复并闭合；答栏逐字录入原话，未代答）
- R1 意图评审：approved — `intent-review.md`「结论」（所有者原话逐字："approved 选a"；裁定分两步走，与 Agent 推荐一致。确认需求本身与两个大步骤的分解）

### step 级

`Intent.md`「step 分解」的两个大步骤已由 R1 确认：

### step1 — 断言的结构化形态（已完成）

- step 定界：已走完 — `step1/scope.md`（核实到三处影响形态设计的事实，其中「该写法已自发出现、控制面零规定」直接决定了以既有写法为起点）
- 抽象设计：已走完 — `step1/specify.md`（五条行为条目；**本 step 内部第一次自证**——初稿判据 002 恒真，落地前实跑输出 3 而非 0，当场订正并如实记录）
- R2 抽象设计评审：approved — `step1/design-review.md`「结论」（所有者原话两条："批准" / "允许两段式（我的推荐）"。**首条未就并列裁定项表态，经追问取得第二条**——与 R1 那次一条答全形成对照：同一评审者相邻两轮，一次答全、一次答半，佐证该形态取决于当次情形而非执行者，判据层处置不因 R1 成功而弱化）
- 详细设计+任务：已走完 — `step1/design.md` 与两份任务文件（basic 段为空，两个候选逐条对照 §4 两条判据均不满足；detail 段 T-D1、T-D2）
- R3 详细方案评审：approved — `step1/task/review.md`「结论」（所有者原话"批准"；本次投影无并列裁定项，故一条答复即完整结论、无需追问。**本 spec 三次评审的对照已记入该文件**：R1 有裁定项一次答全、R2 有裁定项需追问、R3 无裁定项一次答全——需不需要追问取决于投影里有没有并列裁定项及当次答复是否覆盖它）
- impl/basic：已走完 — `step1/impl/basic.md`（零落地调用，空段形态）
- verify/basic：不适用 — `step1/verify/basic.md`（本 step 是该取值第三个使用者；三项中两项无断言对象。**三条命令证据是控制面新增「断言形态」后第一批按该形态书写的断言**）
- impl/detail：已走完 — `step1/impl/detail.md`（两条任务全部落地，无偏差；自证结果非平凡——四条命令改写后 D1 用三段式、其余两段式，佐证 R2 裁定「允许两段式」正确）
- verify/detail：passed — `step1/verify/detail.md`（五条判据全部满足；**报出一处追溯链环 2 断裂并判为判据形态问题而非落地缺陷**——`task/detail.md` 用"001 至 005"区间写法，§12 的 comm 只匹配字面编号；如实报出、不判 failed、不改证据凑判据，交人工验收裁定。另记本节点自身一处订正：初稿写"五个缺失"实为四个，靠当场重跑抓到）
- 人工验收：accepted — `step1/acceptance.md`「结论」（所有者原话两条："批准" / "判为扫描规则的缺口（我的推荐）"；首条未就并列裁定项表态，经追问取得第二条。裁定：区间写法不改，缺口判在 §12，留待另开需求——**确立"不改证据去迎合判据"的原则**）
- 摘要：已走完 — `step1/summary.md`（三节齐备，绑定 accepted 的交付；另列带出未关闭的四项，并如实带出本 step 内发生的两次同型错误——本需求要治的形态在本 step 自身发生了第 14、15 次）


### step2 — `themis` CLI 的校验能力（已完成）

- step 定界：已走完 — `step2/scope.md`（核实到三处影响做法的事实，其中**「本 CLI 会执行来自 Markdown 的字符串、而本项目产品代码零处这么做」是本节点新暴露的**，不在 R1/R2 已批准内容里；已单独提出并由所有者裁定取白名单）
- 抽象设计：已走完 — `step2/specify.md`（五条行为条目；判据以 `go test` 为主。**001 是四个实例中第一条真正符合 `[basic]` 形态的条目**——它建立的是"CLI 这个东西存在"的契约存在性。另如实标出 005 是弱判据：验的是"没做什么"，落地前后同值）
- R2 抽象设计评审：approved — `step2/design-review.md`「结论」（所有者原话："批准，三处都选 a"，**一条答复同时覆盖评审结论与三个并列裁定项、无需追问**。三处裁定：不经 shell 执行（每段过白名单）、`git` 整体放行（已知最宽处，所有者知情后仍取）、静默跳过不合形态的行）
- 详细设计+任务：已走完 — `step2/design.md` 与两份任务文件（basic 段为空；**本 step 是四个实例中第一个"看起来该有 basic 段"的**——T-D1 确实被后两条依赖，但判据 002、004 使它一落地即可观测，故属 detail）
- R3 详细方案评审：approved — `step2/task/review.md`「结论」（所有者原话"批准"；本次投影无并列裁定项，一条答复即完整结论）
- impl/basic：不适用 — basic 段为空，无落地对象
- verify/basic：不适用 — 同上（该取值第四个使用者）
- impl/detail：已走完 — `step2/impl/detail.md`（三条任务全部落地，提交 `8c6946d`；**偏离批准范围五处，全部是首次对 83 份真实工件实跑后修误报**，逐条附证据。其中两处的误报对象是本控制面的说明性表格、和本需求自己已批准的判据——**不修则本工具第一次跑就会否定它自己的依据**。另如实记本节点自身一次同型错误：测试条数初稿写 23、实为 25，由本工具跑本文件时抓到，本 spec 第 16 次实发）
- verify/detail：passed — `step2/verify/detail.md`（五条判据全部满足；001–004 依所有者裁定由人工判，005 另经两项实质核验。**报出 §12 缺口第二次实发**——逗号缩写 `SPEC-CLAIMCLI-002、004` 使字面扫法误报断裂，与 step1 区间写法同型，依既定原则不改证据、交人工。另记本节点自身两次同型错误：追溯链结论先写后验、`git diff --stat` 只抄摘要行，第 17、18 次实发）
- 人工验收：accepted — `step2/acceptance.md`「结论」（所有者原话"批准"；投影三个并列裁定项中两项已给出推荐与理由，一条答复语义上完整，判定无需追问。两条裁定：工具在自身落地记录中的三次检出认定为**有效性证据**、逗号缩写判为 §12 缺口仍另开需求）
- 摘要：已走完 — `step2/summary.md`（三节齐备，绑定 accepted 的交付；带出未关闭的五项，并如实记本 step 内第 16、17、18 次同型错误——**三次中两次由本工具抓到**）

## 当前性

- `Intent.md`：current（六节齐备并按追问结果重写；三要素齐备、无悬问，已提出收敛主张。**范围由"补条款"改为"引入 CLI + 定断言形态"**，step 分解写出的两个大步骤均已完成）
- `QA.md`：current（第 1 轮三问，追加写入不覆盖不回填；答栏为所有者原话逐字录入。问 3 的答复推翻了 Agent 此前的错误建议，与原始记录一致）
- `intent-review.md`：current（R1 approved，「用户原话」含依 §11 的对应关系记录；投影按 spec-review-presentation 写成，含推荐及其依据）
- step 级工件：current（step1、step2 各十四个节点的工件齐备，两份 `summary.md` 分别绑定各自 accepted 的交付）
