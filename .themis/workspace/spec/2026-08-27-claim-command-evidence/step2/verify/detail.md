# verify/detail.md — 2026-08-27-claim-command-evidence / step2

> 本文件的断言范围、身份独立与拒绝条件见 `rules.md` §7，孤儿判定见 §8，追溯链环 2/3 检出见 §12。以上各处只指向位置，本文件不复述其文字。
>
> **本文件里的每个数字与存在性断言均由本节点当场跑命令得出，并按 `template.md`「断言形态」记法书写。** `impl/detail.md` 的自述在本文件中只是**被核验的对象**，不作为证据使用。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程会话，分支 `main`。
- **与 `impl/detail.md`「执行身份」比对：相同——身份独立在本段不成立。** `impl/detail.md` 已预先指出并把判定留给本节点，本节点如实确认：**不成立**。
- **结构性偏袒如实标出**：本结论由与实现者同一会话的角色作出，**证据强度低于独立验证**。这是本 spec 第四次出现同一情形，soft 执行器下无派发层，非本 step 可解。

## 断言与实际结果

`specify.md` 五条行为条目逐条断言，不挑拣。**每条断言注明其验证的任务编号**（`template.md` 环 3 落点）。

| 条目 | 任务 | 判据与实跑 | 结论 |
| --- | --- | --- | --- |
| 001 | T-D1、T-D3 | `go build ./...` → 无输出、退出码 `0`；`test -f cmd/themis/main.go` → 存在 | **满足**（人工判，见下） |
| 002 | T-D1 | `go test ./internal/themis/... -run TestParse -v \| grep -c '^--- PASS'` → `5`（判据 `≥2`） | **满足**（人工判） |
| 003 | T-D2 | `go test ./internal/themis/... -run TestVerify -v \| grep -c '^--- PASS'` → `6`（判据 `≥2`） | **满足**（人工判） |
| 004 | T-D1 | `go test ./internal/themis/... -run TestAllowlist -v \| grep -c '^--- PASS'` → `5`（判据 `≥2`） | **满足**（人工判） |
| 005 | 全部 | `grep -c 'unavailable' .themis/spec/README.md` → `1` | **满足**（另作实质核验，见下） |

### 001–004 由人工判定，理由与授权

**这四条的判据命令以 `go` 开头，而 `go` 不在 CLI 白名单内**——`go build` 会写 build cache，`go test` 会编译并运行仓库里的测试代码，两者都不是只读查询。

**所有者已就此裁定：白名单不放行 `go`，这四条判据改由人工判。** 原话："不放行，判据改由人工判"。**故本节点逐条实跑并记录输出，结论由本节点作为人类流程中的验证角色作出，不经 CLI。**

**代价如实记明**：本 step 的四条判据不受本工具保护，与其它判据不同待遇——这正是本 step 造出来的工具尚够不到的地方。

### 005 的实质核验（不止于计数）

判据 005 只能证明 `README.md` 里 `unavailable` 的声明未被改动，**验的是"没做什么"**。本节点另作两项直接核验：

1. **控制面改动范围**：`git diff 7231e47..HEAD --stat -- .themis/spec/` → 仅 `template.md` 一份、`3 insertions(+), 3 deletions(-)`，即记法示例改占位路径那一处。
2. **控制面提及 CLI 的全部位置**：`grep -rn 'themis verify\|themis CLI\|须先通过.*校验' .themis/spec/` → 命中 `1` 行，为 `template.md:73` 的说明性括注，**不是节点前置条款**。

**因此 005 在实质上也成立**：未引入任何"须先通过 CLI 校验才能进入下一节点"的条款。

## 命令证据

- `go build ./...` → 无输出，退出码 `0`
- `test -f cmd/themis/main.go` → 存在
- `go test ./internal/themis/... -run TestParse -v | grep -c '^--- PASS'` → `5`
- `go test ./internal/themis/... -run TestVerify -v | grep -c '^--- PASS'` → `6`
- `go test ./internal/themis/... -run TestAllowlist -v | grep -c '^--- PASS'` → `5`
- `grep -c 'unavailable' .themis/spec/README.md` → `1`
- `git diff 7231e47..HEAD --stat -- .themis/spec/` → 两行输出：`.themis/spec/template.md | 6 +++---` 与 `1 file changed, 3 insertions(+), 3 deletions(-)`。**初稿只抄了摘要行、漏了文件行，被本工具抓到并订正**（本 spec 内第 18 次实发，同型）。
- 全仓测试：`go test ./...` → 12 个包全绿，`themico` 无回归

**以上前五条以 `go` 开头者由人工实跑，不经 CLI（授权见上）。**

## 追溯链检出（§12 环 2）

上游取 `specify.md` 的 `### SPEC-` 条目编号集合，下游取 `task/detail.md` 中出现的 `SPEC-` 编号集合，前者减去后者为空则通。

**结论：通。** 上游 `001 002 003 004 005`，下游同，差集为空。

### 但按 §12 字面写法扫描会误报，本节点如实记录

**§12 规定的扫法在本 step 上给出错误结果。** 按字面取 `SPEC-` 编号集合：

- 上游 → `001 002 003 004 005`
- 下游 → `001 002 003`
- 差集 → `004 005`，**报断裂**

**实为误报。** `task/detail.md` 用逗号缩写并列编号——第 18 行「判据 SPEC-CLAIMCLI-002、004」、第 38 行「SPEC-CLAIMCLI-001、005」——其中 `004`、`005` 是不带前缀的裸数字，字面扫描取不到。改用能识别 `、NNN` 缩写的扫法后差集为空。

**这是 §12 同一缺口的第二次实发。** 第一次是 step1 的「`001 至 005`」区间写法，所有者已裁定"缺口判在 §12，留待另开需求，不改证据去迎合判据"。**本次是逗号缩写写法，与区间写法同型**：§12 假定编号总以完整形式出现，而工件里两种缩写都自发在用。

**本节点依既定原则处置**：不改 `task/detail.md` 去迎合判据、不判 failed，如实报出并交人工验收。**同时如实记明本节点自身的一次失误**——本文件初稿直接写"`comm -23` → 空 → 通"而未实跑，是先写结论后补证据；实跑后才发现字面扫法报断裂。**这是本 spec 内第 17 次实发，与第 16 次同型（数字/结论未经实跑即写下）。**

## 本工具跑本 step 全部工件的结果

`themis verify` 对本 step 六份工件逐份跑：

- `scope.md`、`specify.md`、`design.md`、`impl/detail.md` 共报出若干条，**全部属已知的四类**（`go` 白名单外、历史点位、省略/转义写法、多行输出的散文摘要），无新增真错。
- **`impl/detail.md` 那次是真错且已订正**——初稿写"测试条数 23"，实为 `25`。**本需求要治的失效模式，发生在为治它而建的工具的落地记录里**，本 spec 内第 16 次实发，已记入 `impl/detail.md`。

## 结论

**passed。**

五条判据全部满足，其中 001–004 依所有者裁定由人工判、005 另经两项实质核验。追溯链环 2 通（**但 §12 字面扫法在本 step 上误报，见上节**）。

**六项如实带出，不掩饰**：

1. **身份独立不成立**（第四次），本结论证据强度低于独立验证。
2. **001–004 不受本工具保护**——本 step 造的工具够不到本 step 自己的判据。
3. **落地过程中偏离批准范围五处**，全部是实跑后修误报，已逐条记入 `impl/detail.md` 并附证据；**其中两处的误报对象是本控制面的说明性表格、和本需求自己已批准的判据。**
4. **`git` 整体放行**仍是白名单最宽处，本节点未收窄。
5. **§12 的扫描规则第二次被证明有缺口**——本次是逗号缩写（`SPEC-CLAIMCLI-002、004`），与 step1 的区间写法同型。按既定原则不改证据、交人工裁定。
6. **本节点自身犯了两次同型错误**——(a) 追溯链结论先写后验，实跑才发现字面扫法报断裂（第 17 次）；(b) `git diff --stat` 只抄摘要行、漏了文件行，被本工具抓到（第 18 次）。**两次都是本需求要治的那一型：数字/结论未经实跑即写下。**
