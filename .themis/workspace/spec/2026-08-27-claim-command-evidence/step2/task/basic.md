# task/basic.md — 2026-08-27-claim-command-evidence / step2

> basic/detail 判定见 `rules.md` §4，`[basic]` 标识见 §5，孤儿阻断见 §8。以上各处只指向位置，本文件不复述其文字。

## 基础任务

**本段为空——零条 basic 任务。**

## 判定依据

依 `rules.md` §4，basic 任务须**同时**满足两条：被本 step 其他任务依赖，且单独不产生外部可观测行为。

**本 step 是四个实例中第一个"看起来该有 basic 段"的**——它有代码，而代码通常有"先建结构、再实现行为"的分层。逐个候选核对如下：

| 候选 | 被其他任务依赖？ | 单独不产生外部可观测行为？ | 判定 |
| --- | --- | --- | --- |
| 建 `internal/themis/verify.go` 的解析与白名单 | 是——比对逻辑依赖它 | **否**——判据 002、004 直接观测其测试通过 | detail |
| 建 `cmd/themis/main.go` 入口 | 否 | **否**——判据 001 直接观测该文件存在 | detail |
| 实现重跑比对 | 否 | **否**——判据 003 直接观测 | detail |

**关键判断**：第一个候选**满足第一条**（比对确实依赖解析与白名单），但**不满足第二条**——`specify.md` 为它单设了判据 002 与 004，**它一落地就产生外部可观测行为**。

**§4 明列"只满足一条即归入 basic"为拒绝条件**，故它属 detail。

**这一处值得记**：本 step 的代码确有分层，**但分层不等于 basic/detail 分段**。判据怎么划，决定了什么是"外部可观测"——`specify.md` 给解析与白名单各设一条判据，就使它们成了可观测的交付物，而非内部准备。**若当初把 002、004 并入 003 一条判据，第一个候选就会成立为 basic。** 这是判据设计影响分段的一个真实例子。

## 孤儿阻断（§8）

本段为空，**无 basic 任务，因而无可判的孤儿**。`task/detail.md` 三条任务的依赖关系见该文件，无一指向 basic 任务。verify/detail 仍须自行数出本文件 `### T-B` 条目数为 0（§8 拒绝条件）。
