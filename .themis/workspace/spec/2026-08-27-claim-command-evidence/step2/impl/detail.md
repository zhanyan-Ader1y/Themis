# impl/detail.md — 2026-08-27-claim-command-evidence / step2

> 本节点的前置闸门、产出与失败去向见 `flow.md`「impl/detail」节。以上各处只指向位置，本文件不复述其文字。
>
> **本文件不含验证结论。** 五条判据是否满足属 verify/detail。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话，分支 `main`，动手前 HEAD `7231e47`，落地提交 `8c6946d`。
- **授权来源**：R3 approved（`task/review.md`「结论」）。basic 段为空，本节点前置闸门取 R3 approved。
- **本节点不承担 verify/detail**：soft 执行器下无派发层，verify/detail 很可能仍由本会话承担，届时身份独立不成立——由该节点自行如实记录。

## 实际改动

`task/detail.md` 三条任务全部落地。

- **T-D1 — 解析与白名单**（`internal/themis/verify.go`）。按行取反引号段、以 `→` 分隔；两段式与三段式；不合形态的行静默跳过。白名单按批准的初始名单实现，首词匹配、默认拒绝，管道逐段过滤，shell 元字符一律拒绝。
- **T-D2 — 重跑比对**（`internal/themis/run.go`）。每段独立 `exec.Command`，段间用 `io` 管道串联，**不经 shell**；两端去空白后比较。三段式比对的是原始输出段而非推导值——推导属人的判断，机器去校验会把正确断言判错。
- **T-D3 — 命令入口**（`cmd/themis/main.go`、`internal/themis/cli/cli.go`）。入口是薄的，与 `cmd/themico/main.go` 同形。**未写入任何控制面条款作为节点前置**，与批准范围一致。

## 与批准范围的偏差

**有五处，全部是"对真实工件实跑后发现缺陷再修"，不是范围扩张。** 每处都先写失败测试、看它红、再实现。落地顺序与证据见下。

**修这五处的共同理由**：批准的任务文本规定的是行为，不是实现细节；而首次对 83 份真实工件实跑暴露出，按字面最直接的实现会产生**误报**。误报比漏报更致命——一个会对散文和已批准判据乱报的检查器，会被关掉。

| # | 缺陷 | 证据 | 处置 |
| --- | --- | --- | --- |
| 1 | 分词不认引号：`grep -c 'two words'` 被拆成四个参数 | Windows 版 grep 自行重解析命令行，**掩盖了该缺陷**——本机通过、换台机器即错 | 在 Go 内做引号解析（`splitWords`） |
| 2 | 管道切分不认引号：`grep -cE 'a\|b'` 从引号内被切断 | `2026-08-26-authorization-traceability/step1/verify/detail.md:43` 实发 | 切分改为引号感知（`splitPipeline`），检查与执行共用 |
| 3 | 文档表格行被误判为断言 | `template.md:50-51`——「`task/basic.md` \| 基础任务 → `### T-B<n>`」符合形状 | 首词须形似命令（无路径分隔符、无扩展名）。**判据刻意不查白名单**：否则 `rm -rf /` → `0` 这类"形状合规但命令被拒"的行会被一并静默跳过，而 R2 裁定三只覆盖"不合形态的行" |
| 4 | 阈值记法按字面比对 | 4 条误报**全部落在本需求自己已批准的判据上**（`step1/specify.md` 的 `≥1`/`≥2`） | `≥ ≤ > <` 按数值比较，非数字输出不满足阈值 |
| 5 | 围栏代码块内的示例被当作断言重跑 | `template.md` 定义形态的那两行示例——数字被例子冻结，**重跑必然失败且无法通过修改消除** | 围栏内不校验；围栏闭合后恢复校验（两条测试分别护住） |

**第 3、4 两处值得单独看**：它们的误报对象分别是本控制面的说明性表格、和本需求自己已批准的判据。**若不修，这个工具第一次跑就会否定它自己的依据。**

## 命令记录

- **落地前 HEAD**：`7231e47`，落地提交 `8c6946d`。**这是历史点位，写下时为真、此后必然不符**，不按可重跑断言书写（理由见下方第 2 项）。
- **测试条数**：`internal/themis` 18 条、`internal/themis/cli` 7 条，合计 `25`。
- **全仓构建与测试**：`go test ./...` → 12 个包全绿，`themico` 无回归。
- **全树扫描报告条数变化**：`34` → `30`（阈值修复后）→ `26`（围栏修复后）。组成命令为对 `.themis` 下 83 份 `.md` 逐份跑 `themis verify` 并计报告行数，非单条命令输出。

### 本节点自身发生了一次同型错误，如实记录

**初稿此处写「测试条数 23」，实际 25。** 写下时是从更早一轮的输出里抄的数字，之后又加了两条围栏测试却没有重数。

**是本工具跑本文件时抓到的**——`themis verify` 对本文件报出四条，其中这一条是真错。**本需求要治的失效模式，发生在为治它而建的工具的落地记录里**，这是本 spec 内第 16 次实发。

另三条报告不是错：`git rev-parse` 那条是历史点位（已改为不按断言书写）、`go test` 两条是白名单与 `2>&1` 重定向所致（见下方第 1 项）。

## 尚未关闭、留给 verify/detail 与人工的

1. **`go` 不在白名单**——本 step 自己的判据用的正是 `go build` / `go test`（`step2/specify.md` 六处）。**因此本 step 的判据目前无法由本工具校验。** 这是白名单边界问题，且所有者已就"只允许只读查询命令"作过裁定，**故不自行放宽，提交人工裁定。**
2. **散文中的历史点位断言**——`git rev-parse --short HEAD` → 某历史值、已删目录的文件计数，写下时为真、此后必然不符。工具无法与"写错"区分。
3. **省略与转义写法**——`awk '…' … \| grep` 含 `…` 与 Markdown 表格转义的 `\|`，本就不可直接跑。
4. **多行输出的散文摘要**——`head -3 go.mod` 的三行输出被压成一行散文。
5. **`git` 整体放行**仍是白名单最宽处，R2 已知情裁定，本节点未收窄。
