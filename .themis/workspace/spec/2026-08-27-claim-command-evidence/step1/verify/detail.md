# verify/detail.md — 2026-08-27-claim-command-evidence / step1

> 本节点的断言范围、身份独立与拒绝条件见 `rules.md` §7，孤儿判定见 §8，追溯链环 2/3 检出见 §12。以上各处只指向位置，本文件不复述其文字。
>
> **本文件里的每个数字与存在性断言均由本节点当场跑命令得出，并按 `template.md`「断言形态」记法书写。** `impl/detail.md` 的自述在本文件中只是**被核验的对象**，不作为证据使用。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程会话，分支 `main`。
- **与 `impl/detail.md`「执行身份」比对：相同——身份独立在本段不成立。** `impl/detail.md` 已预先指出并把判定留给本节点，本节点如实确认：**不成立**。
- **结构性偏袒如实标出**：本结论由与实现者同一会话的角色作出，**证据强度低于独立验证**。

## 断言与实际结果

`specify.md` 五条行为条目逐条断言，不挑拣。**每条断言注明其验证的任务编号**（`template.md` 环 3 落点）。

| 条目 | 验证的任务 | 断言与实际结果 | 判定 |
| --- | --- | --- | --- |
| 001 | T-D1 | `grep -c '断言形态' .themis/spec/template.md` → `1`；`awk '/断言形态/,0' … \| grep -cE '命令\|断言值'` → `3` | **满足** |
| 002 | T-D1 | `awk '/断言形态/,0' … \| grep -c '→'` → `2` | **满足** |
| 003 | T-D1 | `awk '/断言形态/,0' … \| grep -cE '输出\|原样'` → `2` | **满足** |
| 004 | T-D1 | `awk '/断言形态/,0' … \| grep -c '一行'` → `1` | **满足** |
| 005 | T-D1 | `awk '/断言形态/,0' … \| grep -c '判定者'` → `1` | **满足** |

**语义部分的判定，本节点只做能做的那部分：**

- **001 的语义**（形态是否真让机器能分别取到命令与断言值）与 **002 的语义**（差异是否只在补机器可读所缺）——`specify.md` 明写由 R2 判。R2 已 approved 且投影中呈现了这两处做法，本节点确认其批准覆盖了它们，不重复判定。
- **005 的语义**（判定者是否在本 step 验收时真实存在）——本节点可确认：判定者取「验证角色在验证时判」，**而本节点正是该角色，此刻正在做这件事**。该判定者不仅存在，且已被使用一次。

## 命令证据

**V1** — 落地范围：

`git diff --name-only 2dcfe44 -- .themis/spec/` → `.themis/spec/template.md` → `恰一个文件`

**V2** — 是否声称机器校验（命令只列候选、由人读）：

`awk '/断言形态/,0' .themis/spec/template.md | grep -cE '机器|CLI|自动'` → `2`

**逐行读出**：两处均为否定式声明——"`themis` CLI **尚不存在**，本段写明供其实现时消费"、"**本段不由机器校验**"。**是应写的内容，非违规。**

**V3** — 孤儿判定：

`grep -c '^### T-B' .themis/workspace/spec/2026-08-27-claim-command-evidence/step1/task/basic.md` → `0`

**V4** — detail 任务的依赖项：

`grep -c '^- 依赖：无' .themis/workspace/spec/2026-08-27-claim-command-evidence/step1/task/detail.md` → `2`

**V5** — 追溯链环 2（`rules.md` §12，上游集合被下游集合包含）：

`comm -23 <(specify 的 ### SPEC- 编号) <(task/detail.md 中的 SPEC- 编号)` → `SPEC-CLAIMFORM-002 003 004 005` → `差集非空，报断裂`

**这是本节点最重要的一条发现，须单独说明**——见「说明」第 1 条。

## 结论

**passed。**

- `specify.md` 五条行为条目逐条断言，**五条全部满足**。
- **孤儿判定：不存在孤儿**——`### T-B` 条目数为 0（本节点自行数出），两条 detail 任务依赖项均为"无"。前提须随结论一并读：这是"没有可判对象"，不是"逐个 basic 任务都找到了消费者"。
- **落地范围无越界**：恰一个文件（命令 V1）。

**本结论不覆盖的两处：**

1. **追溯链环 2 断裂**（命令 V5）——见「说明」第 1 条。本节点如实报出，**但不因此判 failed**，理由见该条。
2. **身份独立不成立**——本结论由与实现者同一会话的角色作出，证据强度低于独立验证。

## 说明

**1. 追溯链环 2 报断裂，但本节点判定它是判据形态问题而非落地缺陷，理由如下。**

`rules.md` §12 环 2 要求 `specify.md` 的 `### SPEC-` 条目编号集合被 `task/detail.md` 中出现的编号集合包含。本 step 的 `task/detail.md` **以"判据 SPEC-CLAIMFORM-001 至 005"这一区间写法**表述，而非逐个列出五个编号——`comm -23` 因此只匹配到字面出现的 `001`，报其余四个缺失。

**核实**：`task/detail.md` 的 T-D1「出处」行原文为"判据 SPEC-CLAIMFORM-001 至 005"，**语义上覆盖了全部五条**，不是漏引。字面只出现 `001` 与 `005` 两个编号，而 `005` 未带 `SPEC-CLAIMFORM-` 前缀，故 `comm` 只匹配到 `001`。

**本节点自身的一处订正，如实记录**：本条初稿写作"报五个编号全部缺失"，实跑为四个（`001` 字面在场）。**这正是本 step 要治的形态——写数字时未逐字核对实际输出**；靠当场重跑抓到，已订正。

**本节点的处置**：判为 §12 的判据形态未考虑区间写法，**如实报出、不判 failed，也不擅自修改 `task/detail.md` 去迎合判据**（那是改证据凑判据）。**交人工验收裁定**——它是 §12 落地后第一次遇到区间写法，属真实的判据覆盖缺口，不是本 step 的落地错误。

**2. 本 step 的全部工件均按新落地的形态书写**，这是形态的第一次实际使用。**但这不构成对形态的验证**——执行者自愿遵守不等于形态可被机器解析；真正的验证要等 CLI 存在（step2）。

**3. 自证已完成且结果非平凡。** `impl/detail.md` 记载四条命令改写后 D1 用三段式、其余用两段式——**三段式有真实用武之地，不是为凑形态造的例子**。本节点复核该分布成立：D1 确实是"三个输出 → 一个结论"的推导形态。
