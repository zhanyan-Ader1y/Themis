# task/review.md — 2026-08-28-citation-overlap-check / step1

> 本文件是 R3 详细方案评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R3 详细方案评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 评审范围

`design.md` 四条结构决策、`task/basic.md`（空段）、`task/detail.md` 三条任务与判据 005 的无任务说明。

## 分类核查

| 核查项 | 结论 | 依据 |
| --- | --- | --- |
| basic/detail 分段是否成立 | **成立** | `design.md`「basic/detail 分段」——`[basic]` 标识属 §5 的条目分类，basic 段属 §4 的落地分段，两者不必对应；子命令入口与检出逻辑拆开会产生无法验证的空壳 |
| 每条任务是否指向 `specify.md` 条目 | **是** | 三条任务的「对应 `specify.md` 条目」项齐备 |
| 判据是否全部有任务承担 | **四条有，005 无且有理由** | 005 带 `[横切]`，验证对象是 `verify` 既有行为，非本 step 产出 |
| 任务是否越界（§6） | **未越界** | 三条任务的落点与 `design.md`「架构与边界」表一致 |
| 追溯链环 2 | **通** | 上游排除 `[横切]` 后为 `001 002 003 004`，下游展开后含全部四条，差集为空（本节点实跑） |

## 未解决反馈

无。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：写三样东西——极大公共片段算法、正当重合过滤、`overlap` 子命令。

**待裁定项**：无。

**两个由实测定下的取舍**：(一) 向右延长是必须的而非优化——子串合并实测无效（片段等长、互不包含，合并前后同为 `153`）；(二) `overlap` 退出码恒 `0`，与 `verify` 不同，因正当重合无法零误报、非零会变成硬阻塞。

**顺带报出既有缺陷一处**：`2026-08-27-claim-command-evidence/step2/specify.md:77` 声明「`[basic]` 标识：001 带」而条目标题实际没带。**不改它**，交人工验收裁定。

**未覆盖**：三条过滤规则是经验规则；只查控制面四份文件之间。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影**只有一个待裁定项**（批准与否），「批准」一条即完整覆盖，无并列裁定项需要追问。

**本 spec 三次评审均只含一个待裁定项**（R1、R2 各一，R3 无待裁定项），三次均一条答复即完整结论。**这与本会话前期形成对照**——彼时投影常含并列裁定项，需追问或多轮。**差异源于执行者在提问前先穷尽既有依据**，即 `rules.md` §2 新补的提问资格判据在本 spec 的实际效果。

## 结论

**approved。**

三条 detail 任务与空 basic 段获授权，进入落地。
