# task/detail.md — 2026-08-28-citation-overlap-check / step2

> 本文件是详细设计+任务节点的实例工件之一。本节点的前置闸门与产出见 `flow.md`「详细设计+任务」节；每个 `### T-D<n>` 的固定三项见 `template.md`。编号引用写法见 `template.md`「编号引用写法」。以上只指向位置，本文件不复述其文字。

### T-D1 — 删去 `flow.md` 对 `rules.md` §7 的两处复述

**行为目标**：`flow.md` 第 `154`、`176` 行不再含 §7 的判据文字，指向保留。

**做法**：

1. 第 `154` 行删「只判三项：结构存在、可构建、既有测试（若有）无回归。」——**保留**该行的"判定者与断言范围见 `rules.md` §7"与"结论必须写入验证角色所有的工件"（后者是 `flow.md` 本职，非复述）。
2. 第 `176` 行删「依据实际实现与证据判定 `passed`/`failed`，」——**保留**"不得依据文档、Agent 自述或文件存在判定通过（`SPEC-VERIFY-002`）"与指向 §7 的那句。
3. **两处删完后通读所在小节**，确认剩余文字成句、指向仍在。

**对应 `specify.md` 条目**：判据 SPEC-DEDUP-001、004。

**依赖的基础任务**：无。

### T-D2 — 删去 `flow.md` 对 §8、§10 的两处复述

**行为目标**：`flow.md` 第 `33`、`189` 行不再含 §8、§10 的判据文字。

**做法**：

1. 第 `189` 行删「必须由重规划显式处理（复用或删除）后才解除」——保留"存在已落地但无消费者的 basic 改动时不得进入本节点（`SPEC-ACCEPT-002`）"与该行其余的 `flow.md` 本职内容（人工验收只有一次、唯一授权点等）。
2. 第 `33` 行删「（工具错误、验证不过、依赖缺失）」这一并列——保留 fail-closed 的节点行为描述与指向 §10。**§10 反向指向 `flow.md` 的"失败去向"是正确写法，不动。**
3. **通读上下文**，同上。

**对应 `specify.md` 条目**：判据 SPEC-DEDUP-002、004。

**依赖的基础任务**：无。

### T-D3 — 编号前提归 `template.md`，§12 改指向

**行为目标**：该前提只在 `template.md` 成文，`rules.md` §12 改为指向且写明性质。

**做法**：

1. **`template.md` 侧不动**——它是所有者（R2 裁定二）。
2. **`rules.md` §12 侧**删去该句原文，改为指向 `template.md`「编号引用写法」，**并写明那边规定的是"怎么写"这一层**。
3. **指向句只写一层**——写长了会再次构成复述（`design.md` 结构决策三）。

**对应 `specify.md` 条目**：判据 SPEC-DEDUP-003。

**依赖的基础任务**：无。

## 判据 005 无任务承担，理由

`SPEC-DEDUP-005`（"不得报出新重合、不得使既有工件断言失效"）**带 `[横切]` 标识**。它验证的对象是既有工件与既有测试——**不是本 step 的产出**。

## 执行次序

**T-D1 → T-D2 → T-D3**，三者互不依赖；前两条同在 `flow.md`，先后完成便于一次通读该文件。
