# scope.md — 2026-08-28-citation-overlap-check / step2

> 本文件是 step 定界节点的实例工件。本节点的前置闸门与产出见 `flow.md`「step 定界」节。以上只指向位置，本文件不复述其文字。
>
> **本文件的全部数字与存在性断言按 `template.md`「断言形态」记法书写，由本节点当场跑出。**

## 承担的上层分解项

`Intent.md`「step 分解」的 step2：修掉 step1 的工具报出的五处复述。**该分解项经 R1 第 2 轮确认。**

## 本 step 边界

改控制面三份文件（`flow.md`、`rules.md`、`template.md`）的**五处文字**。**不改任何判定语义**——只删除被复述的那一份，保留其所有者处的原文。

**不动的**：`themis overlap` 的代码（step1 交付，已闭合）；任何工件。

## 与其他 step 的关系

**依赖 step1**：五处是 step1 的工具报出来的，且修完后要用同一工具验证是否归零。**反向不依赖。**

## 会触及或放宽的既有裁定与安全边界

**依 `flow.md` R2 节的交叉核查要求，本节先行列出。**

| 既有裁定 | 本 step 是否触及 |
| --- | --- |
| 「不改证据去迎合判据」（三次确认） | **须辨析，见下** |
| `go` 不放行、判据人工判 | **触及**——本 step 判据要跑 `themis overlap`，而 `themis` 亦不在白名单（step1 已记明该边界）。**处置照旧：人工判** |
| 白名单只放行只读命令 | **不触及** |

**「不改证据」的辨析（本节点认为不触及，理由须写明）**：

该原则禁止的是**为让判据通过而修改证据**。本 step 修的是**控制面条款本身**，不是任何工件里的证据记录：

- 五处复述是**控制面的缺陷**，不是记录某次事实的证据。
- 修它们**不会使任何已闭合工件的断言变假**——那些工件引用的是条款位置（`rules.md` §7 等），不是被删的复述文字。
- **所有者已明确指示修**（"一起修了"），并经 R1 第 2 轮确认。

**本节点仍将此列出并交 R2 判定**，不自行认定该辨析成立。

## 核实到的、影响做法的三处事实

### （1）前四处方向一致：`flow.md` 复述 `rules.md`，删 `flow.md` 那侧

本节点逐处打开两端原文核对：

| # | `flow.md` 侧 | `rules.md` 侧 | 被复述的内容 |
| --- | --- | --- | --- |
| 1 | 第 `176` 行 | §7（第 `74` 行） | 依据实际实现与证据判定的表述 |
| 2 | 第 `154` 行 | §7（第 `74` 行） | basic 段三项判据 |
| 3 | 第 `189` 行 | §8（第 `89` 行） | 孤儿阻断的处置方式 |
| 4 | 第 `33` 行 | §10（第 `107` 行） | fail-closed 的失败去向 |

**四处的 `flow.md` 侧都已带指向**（"判定见 `rules.md` §7"、"判定见 `rules.md` §10"等）——**复述是多余的，删掉后指向仍在**。

**第 4 处须单独记**：`rules.md` §10 反向也指向 `flow.md`（"各节点的具体停靠点见 `flow.md` 每个节点的失败去向"）。**两侧互指是正确写法**，被复述的只是判据文字本身。

### （2）第 5 处性质不同：不是复述，是**归属未定**

`rules.md:143` 与 `template.md:79` 的同句，**两侧都不是"引用另一份"**——它们各自把这条前提当作自己的内容在写。

- `rules.md` §12 侧：作为扫描展开规则的前提。
- `template.md` 侧：作为「编号引用写法」的书写要求。

**这不是"补引用时顺手复述"，是同一条规则被写进了两个所有者。** 处置方式与前四处不同：**须先判定它归谁**，再在另一侧改为指向。

**这一处是本会话执行者在 `2026-08-27-trace-number-scan` 写下的**，两处都是新写的，不存在"谁先谁后"的历史依据。

### （3）修完后可用同一工具验证归零

`themis overlap .themis/spec` 当前报出 `5` 处。**本 step 的判据可以直接是"报出 `0` 处"**——这是 step1 交付带来的直接好处：**修复有了机器可判的完成条件。**

**但须记明**：`themis` 不在白名单内（step1 已记明），故该判据仍由人工实跑判定。

## 本节点新暴露、不在 R1 已确认内容里的一处

**有一处**：事实（2）——第 5 处**不是复述而是归属未定**，其处置方式与前四处不同（须先判归属，非简单删除）。

R1 第 2 轮确认的是"修掉五处复述"，**而第 5 处严格说不是复述**。本节点如实标出，**交 R2 判定该处是否仍在本 step 范围内**——若 R2 认为归属判定属另一件事，本 step 应只修前四处。
