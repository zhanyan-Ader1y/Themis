# summary.md — 2026-08-28-citation-overlap-check / step2

> 本节点的前置闸门与产出见 `flow.md`「摘要」节。本文件绑定 accepted 的交付，不复述工件文字。

## 交付摘要

**控制面五处复述全部清除，`themis overlap` 报出 `0` 处。**

- **`flow.md` 四处**：删去抄自 `rules.md` §7、§8、§10 的判据文字，**各自的指向与 `flow.md` 本职内容全部保留**——被删的只是多余的那半句。
- **编号前提一处**：归 `template.md`（写法的所有者），`rules.md` §12 改为指向并写明"那节管怎么写、本节管据此怎么展开"。

## 绑定的验收结论

`step2/acceptance.md`「结论」判 **accepted**，所有者原话「批准」。

**本次验收同时闭合了 R2、R3 两处授权代行**——那两处的方案内容此前未经所有者审阅，**验收是所有者对本 step 全部产出的第一次表态**。三项代行裁定一并追认。

**一条效力边界随裁定生效**：编号前提归 `template.md` 这一判定**不构成"编号类规则一律归 `template.md`"的一般性先例**——它是在两个方向都讲得通时作出的，下一次同类归属问题仍须各自判定。

## 中性工件说明

本 step 只改控制面两份文件的五处文字，未写代码、未新增文件、未动任何工件。

## 本 step 带出、未随交付关闭的四项

1. **`overlap` 报 `0` 不等于确实无复述**——只等于"无未被四条经验规则排除的重合"。**这是本工具能力的上限**，不因本次归零而改变。
2. **另三处删除的可读性无判据保护**——判据只覆盖 `verify/basic` 一节的指向；其余三处由 verify 节点逐处通读核对，那是人判。
3. **阈值 `20` 字无理论依据**（step1 带出，本 step 未处置）。
4. **身份独立不成立**（第八次）。

## 一处落地偏差，如实带出

**第 33 行初稿引入了重复指向**——同一行两次"见 `rules.md` §10"，因为句中补了一个而句尾本就有一个。

**判据抓不到这类冗余**：判据 002b 只查那串并列文字是否消失。**是按 `design.md` 结构决策二"每删一处通读所在小节"读出来的，而该决策明写"这一条靠人读"。**

**这次是它第一次生效，也是本 step 唯一一次靠人读抓到的问题。**

## 两个 step 合起来的意义

**step1 造工具、step2 用它修，且用它验证修完。**

- step1 的工具报出 `5` 处，**其中 `3` 处此前从未被任何人发现**——`passed`/`failed` 判定描述、§8 孤儿规则、§10 fail-closed 判据，全部经过六个需求的评审、验证、验收而无人察觉。
- step2 修完后，**完成条件由同一工具判定**：报 `0` 处。

**这是本项目第一次出现"修复有机器可判的完成条件"**。此前的修复都只能靠人声称"改完了"。

**须同时记明代价**：本 step 是本项目至今独立性最弱的一个——R2、R3 两道闸门均为授权代行，**方案、落地、验证三者只有执行者一人看过**，直到人工验收才有所有者的第一次表态。
