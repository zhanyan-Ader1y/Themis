# acceptance.md — 2026-08-28-citation-overlap-check / step2

> 本节点的前置闸门、阻断核查与结论取值见 `flow.md`「人工验收」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 交付视图

控制面五处复述全部清除，`themis overlap .themis/spec` 报出 `0` 处。

四处的 `flow.md` 本职内容全部保留——被删的只是抄自 `rules.md` 的判据文字，各自的指向仍在。第五处的编号前提归 `template.md`，`rules.md` §12 改为指向并写明性质。

## 阻断核查

| 核查项 | 结论 | 依据 |
| --- | --- | --- |
| verify/detail 是否 passed | **不阻断** | `step2/verify/detail.md`「结论」判 passed |
| 五条判据是否逐条有断言 | **不阻断** | 同上「断言与实际结果」表，001–005 逐条列出实跑值 |
| 孤儿阻断（§8） | **不阻断** | basic 段为空 |
| 追溯链环 4（§12） | **不阻断** | 本文件阻断核查各项均引用 `verify/detail.md` 的结论 |

四项均不阻断。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影提出三项——(一) R2 裁定二在无决定性依据下作出；(二) 本 step 独立性是本项目至今最弱的（两道闸门均为授权代行）；(三) 是否批准通过验收。

**「批准」一条覆盖三项。** 前两项是执行者主动提请所有者知悉的风险披露，**投影明写"若你审阅后认为哪条裁定不对，这一步应退回相应节点"**；所有者未要求退回而是批准，**故两项披露均获知情确认**。

**本次批准的性质须记明**：它同时闭合了 R2、R3 两处授权代行——**那两处的方案内容此前未经所有者审阅，本次验收是所有者对本 step 全部产出的第一次表态**。

## 结论

**accepted。**

step2 交付通过验收。控制面五处删改获授权放行，摘要节点解锁。四项阻断核查均不阻断。

### 裁定：R2 的三项代行裁定全部追认

本 step 的 R2、R3 均为授权代行，其中三项裁定由执行者作出。**本次验收对三项一并追认**：

1. **第 5 处仍在本 step 范围内**——虽严格说不是复述而是归属未定。
2. **编号前提归 `template.md`，§12 改指向**——**执行者自认此项无决定性依据**，所有者知情后仍批准。
3. **「不改证据去迎合判据」不触及本 step**——改的是控制面条款，非工件里的证据记录。

**第 2 项须单独记明效力边界**：它在两个方向都讲得通的情况下作出，**本次批准使它生效，但不使它成为"编号类规则一律归 `template.md`"的一般性先例**。下一次遇到同类归属问题，仍须各自判定。

### 本次批准未覆盖的四项

1. **`overlap` 报 `0` 不等于确实无复述**——只等于"无未被四条经验规则排除的重合"。规则是经验性的。
2. **另三处删除的可读性无判据保护**——verify 节点逐处通读核对，但那是人判非机器判。
3. **阈值 `20` 字无理论依据**（step1 带出，本 step 未处置）。
4. **身份独立不成立**（第八次）。
