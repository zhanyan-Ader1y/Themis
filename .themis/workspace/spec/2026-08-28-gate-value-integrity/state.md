# state.md — 2026-08-28-gate-value-integrity

> 各闸门行内格式：`- <节点名>：<结论> — <证据路径>`。结论取值由 `flow.md` 通用状态条款按节点类型固定，本文件不自建约定。

## 当前节点

**step1 的人工验收取 `已授权代行`，停在本节点。** 五条判据全满足、verify/detail 判 passed、四项阻断核查均不阻断，**但所有者未就验收内容表态**，故不取 `accepted`。

**摘要节点不得开始**——`flow.md` 新条款明写取该值时下游不得据它开始。**这是该取值补入合同后的第一次实际使用，且用在它所修补的同一类情形上。**

**须所有者就两项表态后方可继续**：落地中那处未经批准的区分是否成立；是否批准通过验收。

## 各闸门

### spec 级

- Intake：已走完 — `Intent.md`（**未停留在清单记载的 2 次，当场统计 27 处闸门结论**，发现 3 处合同外自创取值与 1 处推断写入的 accepted）
- 追问：已走完 — `QA.md` 第 1 轮（一问：既有四处是否一并处理。**执行者未给推荐**，如实写明"倾向 (a) 但那是保守、不构成推荐"；所有者裁定与该倾向相反）
- R1 意图评审：approved — `intent-review.md`「结论」（所有者原话"一起处理"。**裁定划定「不改证据」的一处边界**：该原则保护记录的真实性，不保护合同未定义的无效状态）

### step 级

### step1 — 补取值与回填（人工验收停在本节点）

- step 定界：已走完 — `step1/scope.md`（**把范围从 R1 说的四处收窄为三处**——第四处已获追认、取值本就合法，照四处做会去改一个已合规的结论字段）
- 抽象设计：已走完 — `step1/specify.md`（五条条目；判据 004 初稿的 glob 够不到 `task/` 子目录，实跑得 1 而非 3，落地前订正，第 27 次同型实发）
- R2 抽象设计评审：approved — `step1/design-review.md`「结论」（所有者原话"批准"；裁定取值名为 `已授权代行`、范围收窄成立）
- R3 详细方案评审：approved — `step1/task/review.md`「结论」（所有者原话"批准"；投影中的张力属知情披露，非待裁定项）
- 详细设计+任务：已走完 — `step1/design.md` 与两份任务文件（四条结构决策；限制写表下不挤单元格、后续处置写死不留解释余地）
- impl/basic、verify/basic：不适用 — basic 段为空
- impl/detail：已走完 — `step1/impl/detail.md`（三条任务落地；**一处区分当场判定**——"描述取值的句子"改、"记录答复的句子"不改，未经批准，已标出）
- verify/detail：passed — `step1/verify/detail.md`（五条判据全满足，两环均通；**发现 `themis verify` 对 `\|` 的三义缺陷**，含首次触发的挂起形态，属 step1 工具缺陷、不在本 step 范围）
- 人工验收：**已授权代行** — `step1/acceptance.md`「结论」（所有者答"由你自己判断分析直到完成这个 spec"，**未就投影两项表态**。**执行者不把"完成这个 spec"解释为可跳过验收**——那正是本需求要防的推断补足）
- 摘要：**未开始** — 前置闸门未满足（取 `已授权代行` 时下游不得开始）

## 当前性

- `Intent.md`、`QA.md`、`intent-review.md`：current
- `step1` 各工件：current（验收结论待所有者表态后改写）
