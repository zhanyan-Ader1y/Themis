# task/detail.md — 2026-08-28-gate-value-integrity / step1

> 本文件是详细设计+任务节点的实例工件之一。本节点的前置闸门与产出见 `flow.md`「详细设计+任务」节；每个 `### T-D<n>` 的固定三项见 `template.md`。编号引用写法见 `template.md`「编号引用写法」。以上只指向位置，本文件不复述其文字。

### T-D1 — `flow.md` 补取值与限制

**行为目标**：闸门取值表的人工评审与人工验收两行各含 `已授权代行`，表下说明段写明其限制与后续处置。

**做法**：

1. 表格两行各补该取值。
2. **表下紧邻处**新增说明段，写明三件事：含义（授权继续但未就内容表态）、**它不等于通过**、**下游节点不得据它开始**。
3. **写明后续处置**：取该值时停在本节点，要往下走须评审者就内容另行表态。
4. **限制不得留解释余地**（`design.md` 结构决策二）——留了就会被读成"可以继续"。

**对应 `specify.md` 条目**：判据 SPEC-GATEVAL-001、002。

**依赖的基础任务**：无。

### T-D2 — `rules.md` §11 补拒绝条件

**行为目标**：§11 拒绝条件含"把非授权答复写成授权取值"。

**做法**：

1. 在现有拒绝条件末补该条。
2. **写明它与既有条件的分工**：既有条件管"记录是否齐备自洽"，本条管"取值本身选得对不对"。**三处自创取值与一处推断 `accepted` 都通过了现有条件**，正说明缺这一条。

**对应 `specify.md` 条目**：判据 SPEC-GATEVAL-003。

**依赖的基础任务**：无。

### T-D3 — 三处回填，第四处加指向

**行为目标**：三处合同外取值改为 `已授权代行。`；第四处加指向追认记录的说明。

**做法**：

1. 三份工件的「结论」小节首行改为 `**已授权代行。**`——`2026-08-27-question-eligibility/step1/task/review.md`、`2026-08-28-citation-overlap-check/step2/design-review.md`、`2026-08-28-citation-overlap-check/step2/task/review.md`。
2. **其后原有的说明段落一字不动**（`design.md` 结构决策三）——那是证据。
3. `2026-08-28-citation-overlap-check/step1/acceptance.md` 的「用户原话」小节补一句，指向 `intent-review.md`「第 2 轮」的追认记录；**结论字段不改**。
4. **三处改完后逐份通读**，确认取值与其后说明仍然一致——说明里写的是"非 approved"，而新取值名不含该字样。

**对应 `specify.md` 条目**：判据 SPEC-GATEVAL-004。

**依赖的基础任务**：无。

## 判据 005 无任务承担，理由

`SPEC-GATEVAL-005` 带 `[横切]` 标识，验证对象是既有工件与既有测试，**不是本 step 的产出**。

## 执行次序

**T-D1 → T-D2 → T-D3**。**有实质依赖**：T-D3 要把三处改成 T-D1 定义的取值，定义须先存在。
