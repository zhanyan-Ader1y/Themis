# acceptance.md — 2026-08-27-trace-number-scan / step1

> 本节点的前置闸门、阻断核查与结论取值见 `flow.md`「人工验收」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 交付视图

改好了追溯链的检查规则。**五个已完成步骤里，原本四个被误报成"有标准没人做"，现在三个不报了。**

规则现在认得三种编号写法（完整、逗号并列、区间），并新增 `[横切]` 标识，让"管全局、不属于任何单个任务"的标准能正当地不映射到任务。

## 阻断核查

| 核查项 | 结论 | 依据 |
| --- | --- | --- |
| verify/detail 是否 passed | **不阻断** | `step1/verify/detail.md`「结论」判 passed |
| 五条判据是否逐条有断言 | **不阻断** | 同上「断言与实际结果」表，001–005 逐条列出实跑值，无挑拣 |
| 追溯链环 2 是否通 | **不阻断** | 同上「追溯链检出」——上游排除 `[横切]` 后差集为空，本节点复核时独立实跑确认 |

三项均不阻断。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影提出两项——(一) `2026-08-26-workspace-cleanup/step1` 残留差集有意不修，该理由是否成立；(二) 是否批准通过验收。

**「批准」一条覆盖两项。** 投影对第一项给出了推荐 (a)（接受残留）与理由（选 (b) 会使新规则做的第一件事就违反它自己刚写下的边界，且一旦开此先例，以后每次改规则都可回头改老文件让自己好看，那些文件就不再是证据），并写明了判错后果。**答复未提出异议，故取推荐项为裁定内容。**

**判定无需追问**：第二项本身即批准动作，第一项已有推荐且答复未表异议。

## 结论

**accepted。**

本 step 交付通过验收。`rules.md` §12 与 `template.md` 的改动获授权放行，摘要节点解锁。三项阻断核查均不阻断。

### 裁定：残留差集接受，"不改证据"原则不开例外

`2026-08-26-workspace-cleanup/step1` 的 `SPEC-WSCLEAN-005` **不补标 `[横切]`**，其环 2 差集保留 `5`。

**认定该理由成立**，三条依据均获确认：

1. 该 step 已 accepted 闭合，回头改已验收工件使新规则扫描变好看，**正是所有者两次裁定确立的"不改证据去迎合判据"所禁止的**。
2. 按本 step 自己刚写下的横切边界，`2026-08-26-workspace-cleanup` 的工件属"本 step 不该扰动的既有东西"——**改它，本 step 第一件事就违反自己定的规则**。
3. 该实例改动前就报差集，不属"当前真通过的实例"，本就不在判据 005 的保护范围内。

**效力边界**：本裁定**第三次确认**"不改证据去迎合判据"这一原则（前两次在 `2026-08-27-claim-command-evidence` step1、step2），并把它扩展到一种新情形——**不仅不为让命令通过而改本次的证据，也不为让新规则好看而改既往已闭合的工件**。

**已知代价如实记明**：该实例会持续报一条差集，每次扫描都需人工分辨"这条是已知的"。**接受这个噪音，换取证据不被追溯修改。**

### 本次批准未覆盖的五处

1. **`2026-08-26-workspace-cleanup/step1` 的残留差集**——已裁定接受，但噪音持续存在。若将来该 step 因其他原因重开，可顺带补标。
2. **`[横切]` 标注错了无机器兜底**——判定者是 R2 评审者（人），与 `[basic]` 承担同样风险，未因新标识加重也未减轻。
3. **展开规则无机器实现**——§12 明写本节不由机器执行；`themis` CLI 只校验断言形态，不校验追溯链。**本 step 定的是规则，不是工具。**
4. **判据 005 的弱点**——只护住已存在的实例，护不住将来出现的新写法。
5. **身份独立不成立**（第五次）——验证与实现由同一会话承担，soft 执行器下无派发层。
