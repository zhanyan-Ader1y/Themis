# impl/detail.md — 2026-08-27-trace-number-scan / step1

> 本节点的前置闸门、产出与失败去向见 `flow.md`「impl/detail」节。以上只指向位置，本文件不复述其文字。
>
> **本文件不含验证结论。** 五条判据是否满足属 verify/detail。

## 执行身份

- **身份**：Claude Opus 5，本次 spec 流程执行者会话，分支 `main`，动手前 HEAD `78df779`。
- **授权来源**：R3 approved（`task/review.md`「结论」）。basic 段为空，本节点前置闸门取 R3 approved。
- **本节点不承担 verify/detail**：soft 执行器下无派发层，verify/detail 很可能仍由本会话承担，届时身份独立不成立——由该节点自行如实记录。

## 实际改动

`task/detail.md` 三条任务全部落地，只改控制面两份文件，未写代码。

- **T-D1 — 改写 §12 扫描形态。** 替换原第 125 行一句，改为"先展开、再取差集"两步；三种写法以表格列出；写明"裸数字必须紧跟完整编号"这一共同前提及其理由（缺了它会把散文数字当编号，把误报换成漏报）；明写"不采用放宽比对"及理由；环 3 在同一句里声明适用同一套规则。**保留了 §12 原有的能力边界段与环 1、环 4 说明，未连带删改。**
- **T-D2 — 新增横切保护判据规则。** 定义、判定方法（一问即可）、标注形态 `[横切]`、扫描处置（不计入上游集合）、判定者（作者标注 + R2 判定，与 §5 同构）、五条拒绝条件。**写明反例 `SPEC-CLAIMCLI-005`**——它落地前后同样满足却不属横切，因为它约束的是本 step 自己的产出。
- **T-D3 — `template.md` 补两处。** 第 47 行 `specify.md` 结构项补上 `[basic]`／`[横切]` 可选标识；新增「编号引用写法」小节，三种写法各一例，并写明裸数字前提与"为什么要规定"。

**本 step 自己的 `specify.md` 条目 005 已标注 `[横切]`**——本 step 是该标识的第一个使用者，须先用于自身。

## 与批准范围的偏差

### 一、三条判据的 `awk` 范围写错，落地后订正

**不是范围扩张，是判据命令的缺陷。** 初稿的 `awk '/起点/,/^$/'` 在起点后第一个空行即终止：001 只取到开头一行（输出 `1`，判据要 `≥3`）、004 只取到标题行（输出 `0`）。**内容完整，缺陷在命令。**

已改为不被空行截断的范围。**订正后的命令经 `git stash` 回到改动前复跑，三条全部输出 `0`**——证明它们仍可证伪，不是把错误命令换成恒真命令。

**这是同一缺陷在本项目的第三次实发**（前两次在 `2026-08-26-authorization-traceability/step1` 的判据 002 与 005）。**三次都靠实跑抓到，没有一次靠人读出来。**

### 二、`2026-08-26-workspace-cleanup/step1` 的 `SPEC-WSCLEAN-005` 未补标 `[横切]`，是有意为之

按新规则重跑六个 step 实例的环 2 差集，**五个为空，`2026-08-26-workspace-cleanup/step1` 仍报 `5`**——因为该条目没有 `[横切]` 标识，而标识是本 step 刚定义的。

**本节点不去补标那个条目**，理由：

1. **该 step 已 accepted 并闭合**（`acceptance.md`「结论」）。回头改一个已验收实例的工件，使新规则的扫描结果变好看，**正是所有者两次裁定确立的"不改证据去迎合判据"所禁止的**。
2. **它不是本 step 的产出**——按本 step 自己刚写下的横切边界，`2026-08-26-workspace-cleanup` 的工件是"本 step 不该扰动的既有东西"。**若为了让判据通过就去改它，本 step 第一件事就违反了自己定的规则。**
3. 判据 005 要求的是"当前真通过的实例不得转为报断裂"。`2026-08-26-workspace-cleanup` 在改动前就报 `5`（见 `scope.md` 事实（2）），**它本来就不是"当前真通过"的实例**，因此不在 005 的保护范围内。

**如实带出的后果**：新规则落地后，`2026-08-26-workspace-cleanup/step1` 仍会报一条差集。**这是已知的、有理由的残留**，须由 verify/detail 与人工验收判定是否接受，本节点不自行判定。

## 命令记录

- 落地前 HEAD：`78df779`。**历史点位，写下时为真、此后必然不符**，不按可重跑断言书写。
- 五条判据落地后实跑：001 → `5`、002a → `7`、002b → `1`、003 → `1`、004 → `5`，逐条见 `specify.md` 的验收判据行（本工具可复跑）。
- 六实例环 2 差集（新规则）：五个为空，`2026-08-26-workspace-cleanup/step1` 为 `5`。组成命令为按 `[横切]` 排除上游、按三种写法展开下游后取 `comm -23`。

## 尚未关闭、留给 verify/detail 与人工的

1. **`2026-08-26-workspace-cleanup/step1` 的残留差集**——有理由不修，须人工确认该理由成立。
2. **`[横切]` 标注错了无机器兜底**——判定者是 R2 评审者（人），与 `[basic]` 同风险。
3. **判据 005 的弱点**——只护住已存在的实例，护不住将来的写法。
4. **展开规则本身尚无机器实现**——§12 明写"本节不由机器执行"，`themis` CLI 当前只校验断言形态，不校验追溯链。**本 step 定的是规则，不是工具。**
