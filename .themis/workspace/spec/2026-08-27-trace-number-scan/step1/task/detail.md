# task/detail.md — 2026-08-27-trace-number-scan / step1

> 本文件是详细设计+任务节点的实例工件之一。本节点的前置闸门与产出见 `flow.md`「详细设计+任务」节；每个 `### T-D<n>` 的固定三项见 `template.md`。以上只指向位置，本文件不复述其文字。

**编号引用写法**：本文件按本 step 拟定的规则书写——完整、逗号并列、区间三种。**本 step 自身即新规则的第一个书写样本**，落地后须能被新扫描形态正确展开（这是判据 001 的实际检验之一）。

### T-D1 — 改写 §12 的扫描形态

**行为目标**：替换 `rules.md` §12 第 `125` 行那一句，使环 2 与环 3 都按"先展开、再取差集"判定，并写明三种写法的识别规则。

**做法**：

1. **展开规则**按 `design.md`「结构决策」第一条写明三种写法：完整（`SPEC-<主题>-<数字>`）、逗号并列（后紧跟 `、<数字>`，裸数字继承前缀）、区间（`<起> 至 <止>`，闭区间展开）。
2. **写明共同前提**——裸数字必须紧跟在一个完整编号之后，中间不得插入其他文字。**这一句不可省**：它是把"编号"与散文里的普通数字分开的唯一依据，漏了会把"第 1、2 条"当成编号。
3. **环 3 同改**，且须在文字上明确两环适用同一套识别规则（判据 003 要求「环 3」与识别规则关键词同现）。
4. **保留 §12 现有的能力边界段**（"集合包含只能发现漏引、发现不了引错"等）——那几段与本次改动无关，不得连带删改。

**对应 `specify.md` 条目**：判据 SPEC-TRACESCAN-001、003。

**依赖的基础任务**：无（basic 段为空）。

### T-D2 — 新增横切保护判据的规则

**行为目标**：在 `rules.md` §12 增写一段，规定横切保护判据的定义、边界、标注形态与判定者，使扫描排除带标识的条目。

**做法**：

1. **定义与边界**按 R2 已确认的判定：**验证对象不是本 step 的产出，而是本 step 不该扰动的既有东西**。写明一问即可的判定方法。
2. **写明反例**——`SPEC-CLAIMCLI-005`（"CLI 缺席时流程不得被阻塞"）**不属**横切，因为它约束的是本 step 自己能产出什么。**这个反例不可省**：它是边界最易被定歪的方向，只写正例会让下一个人把"落地前已满足"当作判据。
3. **标注形态**：`### SPEC-<主题>-<序号> [横切]`，与 `[basic]` 同形。
4. **判定者**：条目作者标注，**R2 评审者判定标注是否成立**（与 §5 同构）。
5. **拒绝条件**须写明：拿 `[横切]` 规避映射义务；标了 `[横切]` 却验证本 step 自己的产出。

**对应 `specify.md` 条目**：判据 SPEC-TRACESCAN-002。

**依赖的基础任务**：无。

### T-D3 — `template.md` 补标识位与编号写法

**行为目标**：`specify.md` 的结构项补「横切标识」；新增「编号引用写法」小节规定三种写法。

**做法**：

1. **结构项**：第 `47` 行 `specify.md` 那一行的结构描述补上横切标识，指向 `rules.md` §12 的规则，**不复述其判定内容**（避免两处写同一条即漂移）。
2. **新增小节**「编号引用写法」，写明三种允许的写法各一例，并写明裸数字须紧跟完整编号这一前提。
3. **以既有自发写法为起点**——三种写法全部来自已完成实例的实际书写，不另发明第四种。

**对应 `specify.md` 条目**：判据 SPEC-TRACESCAN-004。

**依赖的基础任务**：无。

## 判据 005 无任务承担，理由

`SPEC-TRACESCAN-005`（"改动不得使当前真通过的实例转为报断裂"）**是横切保护判据，按本 step 拟定的规则带 `[横切]` 标识，不映射到任何任务**。

它验证的对象是**五个已完成实例的既有通过状态**——那不是本 step 的产出，本 step 只要求别弄坏它。判定与依据见 `specify.md`「本节点标出的两处」第二点，R2 已确认。

**本 step 因此是该标识的第一个使用者**，其正确性由既有 `24` 条判据的分类结果背书，不由本 step 自证。

## 执行次序

**T-D1 → T-D2 → T-D3**，但三者互不依赖，顺序只为便于验证：T-D1、T-D2 同在 §12，先后写完便于一次通读该节；T-D3 在另一份文件，最后做。

**三条任务无一被其他任务依赖**，故 basic 段为空成立。
