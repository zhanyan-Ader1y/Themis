# task/review.md — 2026-08-27-trace-number-scan / step1

> 本文件是 R3 详细方案评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R3 详细方案评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 评审范围

`design.md` 四条结构决策、`task/basic.md`（空段）、`task/detail.md` 三条任务与判据 005 的无任务说明。

## 分类核查

| 核查项 | 结论 | 依据 |
| --- | --- | --- |
| basic/detail 分段是否成立 | **成立** | `design.md`「basic/detail 分段」逐条对照 §4 两条判据；三条任务互不依赖，各自一落地即可观测 |
| 每条任务是否指向 `specify.md` 条目 | **是** | `task/detail.md` 三条任务的「对应 `specify.md` 条目」项齐备 |
| 判据是否全部有任务承担 | **四条有，005 无且有理由** | 005 是横切保护判据，R2 已确认其不映射；理由写在 `task/detail.md`「判据 005 无任务承担，理由」 |
| 追溯链环 2 | **通（须用新展开规则）** | 字面扫法漏 `003`（在 `SPEC-TRACESCAN-001、003` 的逗号缩写里），展开后差集为空 |

**第四项须单独记明**：本 step 的 `task/detail.md` 自身即新写法的样本，**旧扫法在它身上再次误报**——这是本 step 要修的缺口在本 step 工件上的又一次演示。

## 未解决反馈

无。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：改两份控制面文件，不写代码。

**待裁定项：只有"批准与否"一项。**

**两个取舍**：扫描取"先展开、再比对"而非"放宽比对"——放宽会把任务文件里的普通数字当成编号，把误报换成漏报，而漏报不会被发现；横切标识与 `[basic]` 同形、判定者同为 R2——标错了要在动手前发现。

**未覆盖**：`[横切]` 标注错了无机器兜底（与 `[basic]` 同风险）；判据 005 只护现有五个实例。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影**只有一个待裁定项**（批准与否），「批准」一条即完整覆盖，**无并列裁定项需要追问**。

**本 spec 四次人工闸门的对照**：R1、R2、R3 三次投影均只含一个待裁定项，三次均一条答复即完整结论。**与 `2026-08-27-claim-command-evidence` 形成对照**——那个 spec 的 R2 曾因投影含三个并列裁定项而需追问。**本 spec 的差异源于执行者在 R2 前主动收回了两个本可自行判定的裁定项**，而非评审者答复方式的变化。

## 结论

**approved。**

三条 detail 任务与空 basic 段获授权，进入落地。
