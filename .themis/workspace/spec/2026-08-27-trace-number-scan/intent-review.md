# intent-review.md — 2026-08-27-trace-number-scan

> 本文件是 R1 意图评审节点的实例工件。本节点的前置闸门与产出见 `flow.md`「R1 意图评审」节，人工闸门结论的来源要求见 `rules.md` §11。以上只指向位置，本文件不复述其文字。

## 投影

（按 spec-review-presentation 写成，与对话中呈现的一致。）

**这一步要做什么**：修一条检查规则。它负责确认"每条验收标准都有对应的任务去实现"，但认编号的方式太死板，**五个已完成的步骤里有四个被它误报**。

**需要裁定的两项**：

1. **批准需求本身，以及它比原计划多修一处。** 原批准的是"编号缩写"一个问题；核实时发现第二个从未被提出的问题——横切保护判据故意不映射到任务，而扫法把"上游有、下游没有"一律当漏引。推荐两个一起修，理由是改的是同一段文字，且第二个问题会改变"差集非空"的含义，分开做会让先做的被后做的重写。
2. **第二个问题为什么更值得修**（不单独裁定，影响优先级判断）：缩写误报一眼可辨；横切误报看起来像真漏引，**尽责的验证者反而会去"修复"一个没坏的东西**，把横切判据硬塞进某个任务，破坏 `specify.md` 已声明的正确结构。

**改变原定方向的发现**：误报率 `4/5` 不是偶发——逐个实跑五个已完成 step，一个真通过、三个缩写导致、一个横切导致。**一条误报率如此之高的规则，实际效果是训练验证者忽略它；而忽略它就等于这条追溯检查不存在。**

**未覆盖**：`go` 那项开着的不动（按既有裁定由人工判）；不改任何已完成工件的写法（沿用"不改证据去迎合判据"）；环 3 当前零处缩写，一并改属"让同一规则在其已声明适用的两处都成立"，非预留能力。

## 未解决反馈

无。

## 用户原话

**所有者答复：「批准」。**

**依 §11 记录对应关系**：本次投影提出两项，其中只有第一项是待裁定项（第二项明写"不单独裁定，影响优先级判断"）。**「批准」一条覆盖该唯一裁定项。**

投影对该裁定项给出了推荐（(a) 两个一起修）与理由（改同一段文字、第二个问题会改变差集判定的含义、分开做先做的会被重写），并写明了判错后果。**答复未提出异议，故取推荐项为裁定内容：范围含两个盲区，一并修。**

**判定无需追问**：与本项目此前出现过的"只答一半"情形不同——本次投影只有一个裁定项且已给出推荐，一条「批准」在语义上完整。

## 结论

**approved。**

确认需求本身，以及**范围含两个盲区**（编号缩写、横切保护判据），一并修。

**本次批准同时确认的三项**（来自追问闭合，答复均与 Agent 推荐一致）：

1. 横切条目加**显式标识**，扫描时排除——不依赖「来源覆盖」表那个从未被规定的自发写法。
2. **同时把允许的编号写法规定进控制面**（完整、区间、逗号三种），不只改扫描。理由是 `2026-08-27-claim-command-evidence` 的直接经验：该需求正是因"写法自发、控制面零规定"才出的问题。
3. **环 2 与环 3 一并改**——§12 里两环由同一句话规定（"环 3 同形"），只改一环会让那句话不再成立。

**step 分解**：一个 step。两个盲区改同一段文字且相互耦合，分开会让先做的被重写。
